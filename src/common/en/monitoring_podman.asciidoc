// -*- coding: utf-8 -*-
// IGNORE Podman mypodmanhost prepended
// NONASCII
include::global_attr.adoc[]
= Monitoring Podman
:title: Monitoring Podman
:description: {CMK} can monitor Podman hosts and the containers running on them. Read here how to set up and use the monitoring.

{related-start}
xref:wato_monitoringagents#[Monitoring agents]
xref:agent_linux#[Monitoring Linux]
xref:intro_setup_monitor#[Setting up monitoring]
xref:monitoring_docker#[Monitoring Docker]
{related-end}


[#intro]
== Introduction

{CMK} can monitor containers that you manage with Podman using the xref:glossar#agent_plugin[agent plug-in] `mk_podman.py`.
This monitors not only general data, such as the status of your Podman host, but also the containers themselves.
As always, you can find a complete list of currently monitorable items in the link:https://checkmk.com/integrations?tags=podman[Catalog of Check Plug-ins^].

The agent plugin can communicate with Podman in two different ways.
Either via the Podman API and thus via the associated Unix socket, or directly via the `podman` command.
You can learn how to configure the agent plugin in the chapters xref:monitoring_podman#setup[Manual Setup] and xref:monitoring_podman#bakery[Setup using the Agent Bakery].

In addition to the status and inventory information that {CMK} can determine via the Podman host—i.e., the host on which Podman is installed and running—{CMK} can also determine detailed status information about the containers themselves.
To do this, each container is created as a standalone host in {CMK} if it is to be monitored.
Its data is transferred to your {CMK} server using the xref:glossar#piggyback[Piggyback] method.

ifdef::onprem[]
Within the commercial editions,
endif::[]
ifdef::saas[]
In {CE}
endif::[]
container hosts can be automatically created or removed using the xref:dcd#[dynamic host management].


[#setup]
== Manual setup

=== Prerequisites

On the Podman host, the {CMK} agent must already be xref:agent_linux#install[installed] and xref:agent_linux#registration[registered].


[#install_agent_plugin]
=== Install the agent plug-in

In addition to the agent, you will also need the agent plugin `mk_podman.py`, which you can find in {CRE} under [.guihint]#Setup > Agents > Linux > Plug-ins#.
In the commercial editions, go to the [.guihint]#Setup# menu via [.guihint]#Agents > Windows, Linux, Solaris, AIX# to first access the xref:wato_monitoringagents#bakery[Agent Bakery], where you will find the baked packages.
From there, use the menu entry [.guihint]#Related > Linux, Solaris, AIX files# to access the list of agent files.

Install the plugin on the Podman host in the agent's xref:glossar#dir_plugins[plugin directory] (by default `/usr/lib/check_mk_agent/plugins`):

[{shell}]
----
{c-root} install -m 0755 mk_podman.py /usr/lib/check_mk_agent/plugins
----

For more detailed information on installing an agent plugin, see the xref:agent_linux#plugins[article on the Linux agent].

If you then perform a xref:wato_services#[service discovery] on the corresponding host in {CMK}, you should already see several services that include the word Podman in their names:

.Result of the service discovery on the Podman host
image::monitoring_podman_first_discovery.png[alt="View of the Podman services newly found in {CMK}.",fullscreen=1]

However, if you do not see any Podman services listed here, it may be because the agent plugin's automatic detection was unable to communicate with your Podman instance through the usual channels.
In this case—and also if you want to change certain parameters—you must provide the agent plugin with a configuration.
You can learn how to do this in the following chapter xref:monitoring_podman#configure[Configure agent plug-in].


[#configure]
=== Configure the agent plug-in

The agent plug-in usually figures out on its own how to communicate with Podman on the respective host.
However, if this automatic detection fails or if you want to specify that only certain sockets—or, for example, only the root socket—should be queried, you can configure the plugin accordingly.

To do this, create a configuration file named `mk_podman.cfg` on the Podman host in the agent's xref:glossar#dir_conf[configuration directory].
By default, this is `/etc/check_mk/`.

As a site user of your {CMK} site, you can find a template for this configuration file with detailed explanations in the directory `~/share/check_mk/agents/cfg_examples/mk_podman.cfg`.
In the {CRE} graphical interface, you can find the sample configuration under [.guihint]#Setup > Agents > Linux > Plug-ins#.
In the commercial editions, you can access the [.guihint]#Setup# menu via [.guihint]#Agents > Windows, Linux, Solaris, AIX# to first enter the xref:wato_monitoringagents#bakery[Agent Bakery].
From there, use the menu item [.guihint]#Related > Linux, Solaris, AIX files# to access the list of agent files.

However, we'd like to briefly touch on a key configuration option here.
This is the `connection_method`, which determines how `mk_podman.py` attempts to communicate with your Podman instance.
By default, the agent plugin attempts to communicate with the Podman API via a Unix socket.
Alternatively, you can instruct the plugin to perform monitoring via the `podman` command.
A corresponding configuration might look like this:

./etc/check_mk/mk_podman.cfg
[{file}]
----
[PODMAN]
connection_method: cli
piggyback_name_method: name_id
----


[#bakery]
== Setup using the Agent Bakery

ifdef::onprem[]
{cee-only}
In the commercial editions,
endif::[]
ifdef::saas[]
In {CE},
endif::[]
setup is greatly simplified by the xref:glossar#agent_bakery[Agent Bakery], as it helps prevent syntax errors in configuration files and makes it easy to adapt to changing environments.

To do this, use the rule set [.guihint]#Podman hosts and containers (Linux)# and enable the option [.guihint]#Deploy Podman plug-in# there.

image::monitoring_podman_podman_hosts_and_containers.png[alt=“View of the default settings for the Podman hosts and containers (Linux) rule.” ]

You can also specify the connection method (API or `podman` command) and the naming convention for the container hosts here.
For details on the available options, see the rule's inline help.


== Monitoring the containers

=== Creating the container hosts

The really interesting part of monitoring Podman is, of course, monitoring the containers.
This data is also provided by the agent plugin.
However, the services are not assigned to the Podman host; instead, {CMK} assumes a separate host for each container.

The assignment is handled via the xref:glossar#piggyback[piggyback] mechanism.
All you need to do is create hosts with the correct names during setup, and the services will be automatically assigned to them.

In the commercial editions, you can have these hosts created automatically.
To do this, use the [.guihint]#Piggyback data# connection type in xref:dcd#[dynamic host management].
You can also create the hosts manually.

Please note the following:

* The host name must exactly match the directory created in `~/tmp/check_mk/piggyback`.
* If the containers do not have their own IP address (which is usually the case), set the attribute in the [.guihint]#Network address# section to [.guihint]#No IP# under [.guihint]#IP address family#.
* Under [.guihint]#Monitoring agents#, be sure to set [.guihint]#{CMK} agent / API integrations# to [.guihint]#No API integrations, no {CMK} agent#.
* You can set the [.guihint]#Parents# field in the [.guihint]#Basic settings# section to the hostname of the Podman host.

After you have created the container hosts and performed service discovery, additional services will appear here.


=== Renaming the container hosts

The agent plugin `mk_podman.py` already generates the names for the container hosts.
By default, the plugin concatenates the name of the Podman host with the name of the container, separated by an underscore.
Alternatively, you can instruct the agent plugin to combine the container name with the container ID, or to use only the container name.
To do this, you must set the `piggyback_name_method` option in the agent plugin's configuration file to either `name` or `name_id`.
However, if you choose one of these two methods, you must ensure elsewhere that no name collisions occur in {CMK}.
In {CMK}, each host name can only exist exactly once.

If you later want to change these names—for example, to extract specific information directly from the host name—you can use the rule set [.guihint]#Host name translation for piggybacked hosts#.
This allows you to define rules quite flexibly for renaming host names contained in piggyback data.
In the following example, the string `mypodmanhost-` is prepended to all containers that are piggybacked via the host [.guihint]#mypodmanhost#.

image::monitoring_podman_hostname_translation.png[alt="Rule for renaming the hostnames contained in the piggyback data."]

For more options and a more detailed description, see the article xref:piggyback#renamehosts[the piggyback mechanism].


=== Monitor the status of container hosts

By default, every {CMK} site already contains a rule in the [.guihint]#Host check command# rule set that ensures each container *host* receives the status of the respective service [.guihint]#Status#.
This rule should remain unchanged or be configured exactly as shown:

image::monitoring_podman_host_status.png[alt=“Rule for the command to check the host status of the container.”]

The service [.guihint]#Status# checks whether the container is running and can therefore be used as a reliable means of determining the host status.
Since every container host is automatically assigned the host label `cmk/podman/object:container`, this is used as a condition here.


[#diagnose]
== Diagnostic options

=== Diagnosis for a Podman host

If the setup does not work, there are several ways to troubleshoot the issue.

First, check the output of the {CMK} agent.
You can access this in the host view in Monitoring via the [.guihint]#Download agent output# entry in the action menu.

image::monitoring_podman_download_agent_output.png[alt=“Host action menu in monitoring with the entry to download the agent output.”, width=50%]

ifdef::onprem[]
Or you can search the agent cache directly.

[{shell-raw}]
----
{c-omd} strings tmp/check_mk/cache/mypodmanhost | grep "<<<podman"
<<<podman_errors:sep(0)>>>
<<<podman_containers:sep(0)>>>
<<<podman_disk_usage:sep(0)>>>
<<<podman_engine:sep(0)>>>
<<<podman_pods:sep(0)>>>
<<<podman_container_inspect:sep(0)>>>
<<<podman_container_stats:sep(0)>>>
<<<podman_container_inspect:sep(0)>>>
<<<podman_container_stats:sep(0)>>>
----
endif::[]

If the agent's output does not contain any sections that begin with `<<<podman`, you'll need to continue troubleshooting on the Podman host.

To do this, manually run the agent plugin on the Podman host:

[{shell}]
----
{c-root} python3 /usr/lib/check_mk_agent/plugins/mk_podman.py
----

The output of `mk_podman.py` will also provide information about what went wrong in the event of an error.
For example, if the agent plugin cannot find Podman on the host, it will state this fact plainly:

[{shell}]
----
{c-root} python3 /usr/lib/check_mk_agent/plugins/mk_podman.py
mk_podman.py: Does not seem to be a podman host. Terminating.
----


=== Diagnosis for a container host

If only a container host is not receiving data or no services are detected for it, first check whether the piggyback data for that host is available.
To do this, review the agent output again to see if there are sections for the container host.
You can search the agent output, for example, by the container's ID.

ifdef::onprem[]
As an instance user, you can check whether Piggyback data has been created for a container.
In the `tmp/check_mk/piggyback` directory, you should find directories that belong to your containers.

If you do not change the host names via [.guihint]#Host name translation for piggybacked hosts#, you should find directories here that consist of the container's name and its ID.

[{shell}]
----
{c-omd} ls tmp/check_mk/piggyback/
exciting_cori_25e6c35a9adb/  quirky_rubin_3900d763658e/
----
endif::[]


[#host_labels]
== Host labels

{CMK} also generates xref:labels#[host Labels] for all Podman objects that can be monitored.
Podman monitoring sets the following automatic labels, among others: 

* the label `cmk/podman/object:node` for the Podman host,
* the labels `cmk/podman/object:container` and `cmk/podman/user` for each container,
* for all containers that are part of a pod, additional labels are also generated to identify the pod.

You can use these labels, for example, in conditions for your xref:wato_rules#[rules] to make your monitoring configuration dependent on the image used in a container.


ifdef::onprem[]
[#files]
==  Files and directories

[cols="30,~"]
|===
|File path |Function 

|`~/tmp/check_mk/piggyback/` |This is where {CMK} stores the piggyback data. A subfolder named after each piggyback host is created. Inside, there is a text file containing the host's data. The filename is the name of the piggyback host that provided the data.
|`~/tmp/check_mk/cache/` |This is where the most recent agent output for each host is temporarily stored. The contents of a file for a host are identical to the output of the command `cmk -d myserver123`.
|`/usr/lib/check_mk_agent/plugins` |Default plugin directory on a Linux host
|`/etc/check_mk/` |default configuration directory on a Linux host
|===
endif::[]