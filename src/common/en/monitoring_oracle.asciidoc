// -*- coding: utf-8 -*-
// IGNORE ARM64 OIC SQL*Plus pipelined oracle_sql
// NONASCII │ ├ └ ─ …​
include::global_attr.adoc[]
= Monitoring Oracle databases
:title: Monitoring Oracle databases
:description: Use the agent plug-in for Windows and Linux to comprehensively monitor Oracle databases. Find configuration details here.

{related-start}
xref:wato_monitoringagents#[Monitoring agents]
xref:agent_linux#[Monitoring Linux]
xref:agent_windows#[Monitoring Windows]
link:https://checkmk.com/de/integrations[Catalog of Check Plug-ins^]
{related-end}

include::include_technical_preview.asciidoc[]


// delete from here
[#introduction]
== Introduction

In this article, you will learn how to monitor Oracle databases using the new agent plug-in `mk-oracle`.

If you are looking for information about the old agent plug-in `mk_oracle`, please refer to the link:https://docs.checkmk.com/2.4.0/en/monitoring_oracle.html[article in the 2.4.0 branch of the User Guide].

With the xref:glossar.html#agent_plugin[agent plugin], you can not only retrieve tablespaces or the active sessions of a database, but also many other metrics.
You can find a complete list of monitoring options with our xref:glossar#check_plugin[check plugins] at link:https://checkmk.com/de/integrations?tags=oracle[Catalog of Check Plug-ins^].

The output of `mk-oracle` is identical to that of the old plugin, so the exact same check plugins can continue to be used.

[.admonition-tip]
****
The new agent plug-in for monitoring Oracle is an executable binary file.
Unlike the old Bash script, it can no longer be run on ARM64 architectures.
We will port the new agent plug-in to ARM64 architectures in one of the upcoming versions of {CMK}.
****

[#overview]
=== Overview of this article

If you don not want to read (almost) the entire long article, here are a few links with shortcuts.
However, if you are setting up Oracle monitoring with {CMK} for the first time, you should work through the entire article to familiarize yourself with all the options.

* To learn how to install the Oracle Instant Client (OIC), see the chapter xref:install_oic[Deploying the Oracle Instant Client (OIC)].

* Users of the commercial editions of {CMK} will find an explanation of the most important options in the chapter xref:configuration_bakery[Creating a Configuration for the Agent Plugin].
In the xref:reference[Reference chapter] of this article, we will go over all the xref:configuration_options[configuration options] again and also show which entries in the configuration file `mk-oracle.yml` correspond to the options from the rule [.guihint]#Unified Oracle plug-in (experimental)#.

* If the data points provided "out-of-the-box" by the agent plug-in are not sufficient for you, you can also monitor your databases using xref:custom_sqls[custom SQL queries].

* Users who wish to switch from `mk_oracle` to `mk-oracle` can use our scripts for rule and configuration migration to make the transition easier.


[#how]
=== How does Oracle monitoring work with {CMK}?

To monitor Oracle instances and databases with {CMK}, the {CMK} agent and the `mk-oracle` agent plug-in must be installed on the respective host.
The agent plug-in requires a configuration file, which you must save as `mk-oracle.yml` in the xref:glossar#dir_conf[configuration directory].
In addition, the agent plug-in relies on libraries from the Oracle Instant Client (OIC).
This must therefore also be available on the host.

You can find out how to install OIC in the chapter xref:prerequisites_oracle_host[Setting Up Prerequisites on the Oracle Host].

[#prerequisites_oracle_host]
== Setting up prerequisites on the Oracle host

On every host where you want to monitor Oracle databases or instances, you must meet two prerequisites.

* The Oracle Instant Client (OIC) must be made available to the agent plugin.
For instructions on how to do this, see the xref:install_oic[corresponding chapter].
* The agent plug-in `mk-oracle` must be granted access to your Oracle instances or databases. You have two different options for this.
** You can create a user and password on the host that is used exclusively for monitoring via {CMK}.
We explain this method in the chapter xref:create_user[Creating a Database User].
** Alternatively, you can use the xref:oracle_wallet[Oracle Wallet].

[#install_oic]
=== Deploying Oracle Instant Client (OIC)

The `mk-oracle` agent plug-in requires the Oracle Instant Client (OIC) libraries to function.
You have three options for making the OIC available.

* You can xref:#install_oic[install the OIC exclusively for the agent plugin]. This is the method we recommend, which we describe here.
* Alternatively, you can xref:#install_oic_globally[install the OIC globally on the host].
* If the OIC is already installed on the host, and you want to use that installation for monitoring via {CMK}, you can tell the agent plug-in where to find OIC by adding an entry to the configuration. Link to the option in the rule.

First, download the package appropriate for your host from the page link:https://www.oracle.com/de/database/technologies/instant-client/downloads.html[Oracle Instant Client Downloads] in ZIP format.

[.tabs]
====
[#tab_oic_linux]#Linux#::
+
--
For a Linux x86-64 system, for example, use the link:https://download.oracle.com/otn_software/linux/instantclient/2326200v2/instantclient-basiclite-linux.x64-23.26.2.0.0.zip[Basic Light Package (ZIP)].

Check the path to the plug-in directory of the {CMK} agent on your host:

[{shell}]
----
{c-local} sudo check_mk_agent | grep "^pluginsdir " | head -1
pluginsdir /usr/lib/check_mk_agent/plugins
----

Unzip the archive file into that very plug-in directory:

[{shell}]
----
{c-local} sudo unzip -j instantclient-basiclite-linux.x64-23.26.2.0.0.zip -d /path/to/agent/plugins/packages/mk-oracle
----

--
[#tab_oic_windows]#Windows#::
+
--
For Windows, use, for example, the link:https://download.oracle.com/otn_software/nt/instantclient/2326300/instantclient-basiclite-windows.x64-23.26.3.0.0.zip[Basic Light Package (ZIP)].

Extract the archive into the `packages\mk-oracle` subdirectory of the xref:glossar#dir_plugins[plugin directory].

By default, on a Windows host, this is `%PROGRAMDATA%\checkmk\agent\plugins\packages\mk-oracle`.
--
====

.Install OIC Globally
[%collapsible]
====
[#install_oic_globally]
On a Linux host, you can alternatively install the Oracle Instant Client in `/opt/`, for example.

[{shell}]
----
{c-local} sudo unzip -j instantclient-basiclite-linux.x64-23.26.2.0.0.zip -d /opt/checkmk/oracle-instant-client
----

On a Windows host, you can also install the Oracle Instant Client in any directory.
You must then specify this directory in the Windows `PATH` environment variable.
====


[#allow_access]
=== Grant access rights for the agent plugin

The agent plug-in `mk-oracle` must be granted access to your Oracle instances or databases.
You can do this either via a database user or the Oracle Wallet.

[.tabs]
====
[#tab_simple_user]#Simple database user#::
+
--
[#create_user]
If you want to set up a user for monitoring, we recommend using that user exclusively for {CMK}.

First, connect to your Oracle host and switch to the `oracle` user.

[{shell}]
----
{c-root} su - oracle
----

Finally, specify the instance to which `sqlplus` should connect:

[{shell}]
----
oracle@linux$ export ORACLE_SID=MYINST1
----

Start `sqlplus`.
Specify the role with which `sqlplus` should start.

[{shell}]
----
oracle@linux$ sqlplus / as sysdba
----

Now create a user that will be used exclusively for monitoring Oracle.
The privileges apply to all existing and future databases.

[{sql}]
----
sqlplus> create user checkmk identified by myPassword;
sqlplus> alter user checkmk set container_data=all container=current;
sqlplus> grant select_catalog_role to checkmk container=all;
sqlplus> grant create session to checkmk container=all;
sqlplus> exit
----
--

[#tab_user]#Database User (multi-tenant)#::
+
--
[#create_user_multi-tenant]
If you want to set up a user for monitoring, we recommend using this user exclusively for {CMK}.

First, connect to your Oracle host and switch to the `oracle` user.

[{shell}]
----
{c-root} su - oracle
----

Finally, specify the instance to which `sqlplus` should connect:

[{shell}]
----
oracle@linux$ export ORACLE_SID=MYINST1
----

Start `sqlplus`.
Specify the role with which `sqlplus` should start.

[{shell}]
----
oracle@linux$ sqlplus / as sysdba
----

Now create a user that will be used exclusively for monitoring Oracle.
The prefix `C##` ensures that the user can view all databases in the instance.
The privileges apply to all existing and future databases.

[{sql}]
----
sqlplus> create user C##checkmk identified by myPassword;
sqlplus> alter user C##checkmk set container_data=all container=current;
sqlplus> grant select_catalog_role to C##checkmk container=all;
sqlplus> grant create session to C##checkmk container=all;
sqlplus> exit
----
--

[#tab_wallet]#Wallet#::
+
--
[#oracle_wallet]
As an alternative to specifying the user directly with a password in a configuration file, you can also use the _Oracle Wallet_.
This has the advantage that you no longer need to store the credentials in plain text on both the {CMK} server and the Oracle host.
Even if you have adjusted the permissions of the configuration file on the Oracle host accordingly, the credentials have still left the server and are located on the {CMK} server, provided you are using the Agent Bakery.

The Oracle Wallet, in turn, stores the credentials in encrypted form on the host being monitored, so that they can be used without having to explicitly disclose any login information.
{CMK} can use this wallet, meaning that, in principle, only the database administrator (DBA) needs to know the credentials.
To do this, you—or the DBA—create a wallet on the relevant server and store it in the  in a file named `oracle_wallet`.

[{shell}]
----
{c-root} mkstore -wrl /etc/check_mk/oracle_wallet -create
----

The agent plug-in will access this file later whenever it needs to establish a connection to an instance.
To ensure that the necessary user data can be found, you must enter it into the wallet once.
In the following example, you add the user `checkmk` for the instance `MYINST1`:

[{shell}]
----
{c-root} mkstore -wrl /etc/check_mk/oracle_wallet -createCredential MYINST1 checkmk myPassword
----


For the agent plug-in to know where to look for the wallet, it must find two files.
The first file is `sqlnet.ora`, which specifies the location of the wallet.
The second file—`tnsnames.ora`—specifies the instance's address so that it can also be accessed via its alias.
To ensure the agent plug-in finds these files, you can set the path on Linux, Solaris, and AIX using the `TNS_ADMIN` environment variable.
This is particularly useful if the files already exist.
Alternatively, you can also create them explicitly.
On Windows, you must specify these paths manually.

First, create the `sqlnet.ora` file.
The agent plug-in will look for the access credentials in this file as a fallback, so you must specify the correct path to the wallet file you just created here.
Be sure to set the `SQLNET.WALLET_OVERRIDE` parameter to `TRUE`:

./etc/check_mk/sqlnet.ora
[{file}]
----
LOG_DIRECTORY_CLIENT = /var/log/check_mk/oracle_client
DIAG_ADR_ENABLED = OFF

SQLNET.WALLET_OVERRIDE = TRUE
WALLET_LOCATION =
 (SOURCE=
   (METHOD = FILE)
   (METHOD_DATA = (DIRECTORY=/etc/check_mk/oracle_wallet))
 
)
----

Now the plug-in knows which credentials to use.
To ensure it connects to the correct address, create a file named `tnsnames.ora`.
You can find the exact syntax in the Oracle documentation, but as an example, the file might look like this:

./etc/check_mk/tnsnames.ora
[{file}]
----
MYINST1
  (DESCRIPTION =
    (ADDRESS = (PROTOCOL = TCP)(HOST = 127.0.0.1)(PORT = 1521))
    
(CONNECT_DATA =
      (SERVER = DEDICATED)
      (SERVICE_NAME = MYINST1_ALIAS)
    )
  )
----


You can, of course, add additional access credentials to the wallet at a later time.
In that case, you may need to update the `tnsnames.ora` file.

Finally, change the permissions of the files and directories created manually in this section so that the access rights are set correctly when the program runs.
The agent plugin, which runs as `root`, changes
 to the owner of the Oracle binaries (e.g., `$ORACLE_HOME/bin/sqlplus`) before executing them.
Therefore, at least the group of the owner of the Oracle binaries requires read access to the manually created files in `/etc/check_mk/`.
In the following example, we assume that the group is `oinstall`.

The following commands change the group to `oinstall`:

[{shell}]
----
{c-root} chgrp oinstall /etc/check_mk/sqlnet.ora /etc/check_mk/tnsnames.ora
{c-root} chgrp -R oinstall /etc/check_mk/oracle_wallet
----

These commands then ensure that the group can read the `oracle_wallet` directory and its contents:

[{shell}]
----
{c-root} chmod g+x /etc/check_mk/oracle_wallet
{c-root} chmod -R g+r /etc/check_mk/oracle_wallet
----

Afterward, the permissions should look something like this:

[{shell}]
----
{c-root} tree -ugpR /etc/check_mk
[drwxr-xr-x root root ] /etc/check_mk
├── [drwxr-x--- root oinstall ] oracle_wallet
│ └── [-rw-r----- root oinstall ] cwallet.sso
│ └── [-rw-r----- root oinstall ] cwallet.sso.lck
│ └── [-rw-r----- root oinstall ] ewallet.p12
│ └── [-rw-r----- root oinstall ] ewallet.p12.lck
├── [-rw-r--r-- root oinstall ] sqlnet.ora
├── [-rw-r--r-- root oinstall ] tnsnames.ora
----

The command output shows only the relevant files and directories.

--
====


After you have set up the prerequisites for monitoring with {CMK} on the Oracle host, you must now create a configuration for the agent plugin.

* Users of our commercial editions and the Agent Bakery will find instructions in the chapter xref:configuration_bakery[Creating a configuration for the agent plug-in via a rule] for instructions.
* If you want to monitor your Oracle instances with {CRE} or prefer to configure them manually for other reasons, please read the chapter xref:manual_configuration[Setting up Oracle monitoring manually].


[#configuration_bakery]
== Creating a configuration for the agent plug-in via a rule

{cee-only} Users of a commercial edition of {CMK} can conveniently configure the agent plug-in via the [.guihint]#Unified Oracle plug-in (Experimental)#.
In this chapter, we will focus on the areas xref:authentication[Authentication], xref:configure_connection_options[Connection Options], and xref:database_specific_authentication[Database-Specific Credentials].
All other options provided by the rule set [.guihint]#Unified Oracle plug-in (Experimental)# either are already sufficiently explained in the inline help or are described in the xref:reference[reference chapter] of this article.

Now open the [.guihint]#Setup# menu of your {CMK} instance and look for [.guihint]#Unified Oracle plug-in (Experimental)# and open the rule set.
Click [.guihint]#Add rule# and begin configuring the authentication method.

In the reference section, you will find information on the xref:additional_options[[.guihint]#Additional options#].

[#authentication]
=== Set the Authentication Type

Depending on which approach you chose in the chapter xref:prerequisites_oracle_host[Setting Up Prerequisites on the Oracle Host], select the appropriate option under [.guihint]#Authentication type# and enter the corresponding data.

image::monitoring_oracle_authentication_user.png[]

Optionally, you can use [.guihint]#Role# to specify which role the agent plug-in should assume during authentication.

And if you want to use a different username for [.guihint]#ASM Authentication#, you can also enter it here.

[#configure_connection_options]
=== Configure Connection Options
In the next block, you can configure details about the connection options.
In addition to the host name and port of your Oracle instance, you can also specify the [.guihint]#TNS_ADMIN directory path#.
If you do not enter anything here, `mk-oracle` assumes that this is the agent's configuration directory.
With the [.guihint]#Oracle Local Registry path# option, you can tell the agent plug-in where the local registry files for your Oracle cluster are located.
The agent plugin's instance detection will then use this information to monitor the instances described therein.

[#database_specific_authentication]
=== Database-Specific Credentials
Especially in larger environments, it may sometimes be necessary for different instances of your Oracle environment to have different credentials and connection options.
The [.guihint]#Databases to monitor# section is provided for this purpose.
Here, you can specify instance-specific login credentials and connection options.
Your settings under [.guihint]#Default settings# will then apply only to the instances that you have not explicitly specified under [.guihint]#Databases to monitor#.

[#additional_configurations_options]
=== Additional Configuration Options
All other options for the [.guihint]#Unified Oracle plug-in (Experimental)# rule are described in the chapter xref:reference[References].

[#set_conditions]
=== Set Conditions
Finally, remember to restrict your new rule [.guihint]#Unified Oracle plug-in# to your Oracle hosts via the [.guihint]#Conditions#.

Now save the rule, xref:wato_monitoringagents#bakery[bake the new agent package], and install it on the Oracle host.
Now transfer the new agent package to your Oracle host and install it.
Afterward, you can perform service discovery on your Oracle host in {CMK}.

[#manual_configuration]
== Setting Up Oracle Monitoring Manually

This chapter is intended for anyone who uses {CRE} or prefers not to use the Agent Bakery.

Once you have met the xref:prerequisites_manual_configuration[prerequisites], you must install the xref:install_agent_plugin[agent plugin] on the Oracle host.

At this point, it's worth noting that this works a little differently than you might be used to with other agent plugins.
Last but not least, you will need to provide the agent plug-in with a xref:create_config[configuration].

First, let's go over the prerequisites again.

[#prerequisites_manual_configuration]
=== Prerequisites for manual setup

Before you can begin the manual setup, verify that the following two prerequisites are met:

* The {CMK} agent must already be installed and registered on the Oracle host.
We explain how to do this in the articles xref:agent_linux#install[Monitoring Linux] and xref:agent_windows#install[Monitoring Windows].
* You must also have already ensured that the xref:install_oic[Oracle Instant Client] is available on the host.

Once all prerequisites are met, you can proceed to install the agent plugin.

[#install_agent_plugin]
=== Installing the agent plug-in

The `mk-oracle` agent plug-in comes with two helper tools, which are ultimately only there to start `mk-oracle` itself in either synchronous or asynchronous mode.
You must also install these two helper tools on the host, and that's where we will start now.

[.tabs]
====
[#tab_install_linux]#Linux#::
+
--
ifdef::onprem[]
In addition to the `mk-oracle` agent plugin, you will also need the `oracle_unified_sync` and `oracle_unified_async` files.
You can find all three files in {CRE} under [.guihint]#Setup > Agents > Other operating systems > Plug-ins#.
In the commercial editions, from the [.guihint]#Setup# menu, go to [.guihint]#Agents > Windows, Linux, Solaris, AIX# to first access the xref:wato_monitoringagents#bakery[Agent Bakery], where you will find the baked packages.
endif::[]
ifdef::saas[]
In addition to the `mk-oracle` agent plugin, you also need the `oracle_unified_sync` and `oracle_unified_async` files.
In {CE}, go to the [.guihint]#Setup# menu and select [.guihint]#Agents > Windows, Linux, Solaris, AIX# to first access the xref:wato_monitoringagents#bakery[Agent Bakery], where you will find the built packages.
endif::[]

From there, use the menu item [.guihint]#Related > Other operating systems# to access the list of agent files.
Transfer the three files to the Oracle host using any method.
If `wget` is available on the host, you could proceed as follows:

[{shell-raw}]
----
{c-root} wget -P /tmp http://mycmkserver/mysite/check_mk/agents/mk-oracle
{c-root} wget -P /tmp http://mycmkserver/mysite/check_mk/agents/oracle_unified_sync
{c-root} wget -P /tmp http://mycmkserver/mysite/check_mk/agents/oracle_unified_async
----

Here, we download the three files to the `/tmp` directory on the Oracle host.
We will assume this was done in the following steps.

Install the plug-in on the Oracle host in the agent's xref:glossar#dir_plugins[plugin directory] (by default, `/usr/lib/check_mk_agent/plugins`).
You can find the name of the plug-in directory on your host in the output of the {CMK} agent.
The following command sets the plug-in directory in the `MK_LIBDIR` variable.
The subsequent commands will then also use `$MK_LIBDIR`.
If the next command does not produce meaningful output for you—for example, because `awk` is not available—simply enter the actual plug-in directory into `MK_LIBDIR` yourself so that you can continue working with the specified commands afterward.

[{shell}]
----
pass:[#] Dynamically determine the path and store it in a variable
{c-root} export MK_LIBDIR=$(cmk-agent-ctl dump | awk '/^PluginsDirectory:/ {print $2; exit}')
pass:[#] Test whether it worked
{c-root} echo pass:[$]MK_LIBDIR
/usr/lib/check_mk_agent/plugins
----

We now have the necessary files on the Oracle host, and `$MK_LIBDIR` contains the path to the plug-in directory.
Now we will finally install `mk-oracle` on the host:

[{shell}]
----
{c-root} install -D -m 755 /tmp/mk-oracle "$MK_LIBDIR/packages/mk-oracle/mk-oracle"
{c-root} install -m 755 /tmp/oracle_unified_sync "$MK_LIBDIR/oracle_unified_sync"
{c-root} install -D -m 755 /tmp/oracle_unified_sync "$MK_LIBDIR/600/oracle_unified_async"
----

In the example above, the file `oracle_unified_async` was installed in the `600` subdirectory.
This means that all sections you set up later for asynchronous retrieval will have a maximum cache age of 600 seconds.
Please adjust the number 600 to suit your needs.

--

[#tab_install_windows]#Windows#::
+
--
ifdef::onprem[]
In addition to the `mk-oracle` agent plugin, you will also need the files `oracle_unified_sync.ps1` and `oracle_unified_async.ps1`.
In the commercial editions, go to the [.guihint]#Setup# menu via [.guihint]#Agents > Windows, Linux, Solaris, AIX# to access the xref:wato_monitoringagents#bakery[Agent Bakery], where you will find the baked packages.
endif::[]
ifdef::saas[]
In addition to the agent, you will also need the `mk-oracle` agent plugin.
In {CE}, go to the [.guihint]#Setup# menu and select [.guihint]#Agents > Windows, Linux, Solaris, AIX# to first access the xref:wato_monitoringagents#bakery[Agent Bakery], where you will find the baked packages.
endif::[]
From there, use the menu item [.guihint]#Related > Other operating systems# to access the list of agent files.

The actual installation of the agent plugin works a little differently than you might be used to with other plugins.

You can find all the included plugins for Windows on the monitored host in the agent's installation directory at `C:\Program Files (x86)\checkmk\service\plugins`.
They are stored there so that they are immediately available.
ifdef::onprem[]
Alternatively, the plugins are also located on the {CMK} server at `~/share/check_mk/agents/windows/plugins`.
endif::[]

Copy the three files from there to the following directories:

[cols="2,8",options="header"]
|===
|Filename |Destination directory
|`mk-oracle` |`%ProgramData%\checkmk\agent\plugins\packages\mk-oracle\`
|`oracle_unified_sync.ps1` |`%ProgramData%\checkmk\agent\plugins\`
|`oracle_unified_async.ps1` |`%ProgramData%\checkmk\agent\plugins\`
|===

Once the files are in place, you must provide the appropriate configuration for the file responsible for executing the asynchronous sections—that is, `oracle_unified_async.ps1`.

We explain how to do this in the section xref:agent_windows#customizeexecution[Customizing the Execution of a Specific Plugin] in the manual article on monitoring Windows.
--
====

The installation of the entire agent plug-in is now complete.
To ensure that the agent plug-in knows what to do, you must now create a xref:create_config[configuration for the plugin].


[#create_config]
=== Create configuration for agent plugin

In the xref:glossar#dir_conf[configuration directory] of the {CMK} agent, you must now create a file named `mk-oracle.yml`.
[.tabs]
====
[#tab_configure_linux]#Linux#::
+
--
You can easily find out the name of the configuration directory on your host using the following command:
[{shell}]
----
{c-local} sudo sudo cmk-agent-ctl dump | grep "^AgentDirectory" | head -1
AgentDirectory: /etc/check_mk
----
In this directory, create the file `mk-oracle.yml` using your preferred editor.
--
[#tab_configure_windows]#Windows#::
+
--
In the directory `%PROGRAMDATA%\checkmk\agent\config`, create the file `mk-oracle.yml` using your preferred editor.
--
====


To avoid having to start from scratch, the chapter xref:sample_configurations[Sample configurations] provides a starting point.

A very simple configuration for a freshly installed Oracle AI Database Free—which you may have set up for testing purposes—could look something like this:

.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    authentication:
      password: mypassword
      type: standard
      username: mymonitoringuser
    cache_age: 600
    connection:
      hostname: localhost
    custom_metrics_cache_age: 600
    instances: []
----

You can start with this example for now.

In the reference section of this article, we explain all other xref:configuration_options[configuration options] that you can use.
Once the agent plug-in has been configured, you can already perform service discovery on the respective host from the {CMK} server.
This completes the basic setup of monitoring.
If the services provided by the agent plug-in are not sufficient for your needs, you can proceed directly to the xref:setup_custom_sqls[Custom SQL Queries].


[#setup_custom_sqls]
== Setting Up Custom SQL Queries

{cce-only}
Custom SQL queries cannot be defined via the [.guihint]#Unified Oracle plug-in (experimental)# rule.
You must perform the necessary configuration directly on the host from which you monitor your Oracle instances and databases.

[#custom_sqls_why]
=== Why use custom SQL queries?

{CMK} already provides a large number of SQL queries via the agent plug-in that you can use to monitor your database instances.
To ensure these queries are suitable for as wide a range of technical and content-related requirements as possible, they are kept general.

To meet the individual requirements of each company for monitoring a specific database, {CMK} the option to create your own custom SQL queries (_Custom SQLs_) and have the agent plug-in execute them.
These are then automatically recognized and monitored as services in {CMK}.

You have two options for passing your queries to the agent plugin:

* Either write the queries directly into the configuration file `mk-oracle.yml`.
* Or, instead, specify in the configuration file where the agent plug-in can find the files in which your custom SQL queries are stored.

The latter approach simplifies maintenance and also makes version control easier, since content is not mixed in with the plugin's configuration file.
However, if you are only dealing with a small number of queries anyway, you may want to skip the extra effort.

we will start with a simple example that is written directly into the plugin's configuration file.

[#custom_sqls_simple]
=== Setting Up Simple Custom SQL Queries

First, we will show you how to quickly get a result that returns real data from an Oracle instance.
This example is only meant to illustrate how easy it *can* be to integrate custom SQL queries.
Just how useful this exact query is in everyday work remains to be seen.

For now, we will pretend we are just feeling our way through things and run the SQL query as a test using the `sysdba` user in a test instance.


[{shell}]
----
oracle@linux$ sqlplus / as sysdba
----

[{sql}]
----
sqlplus> SELECT 'details:Free memory: ' || ROUND(SUM(bytes)/1024/1024, 2) || ' MB' FROM v$sgastat WHERE name = 'free memory';
'DETAILS:FREEMEMORY:'||ROUND(SUM(BYTES)/1024/1024,2)||'MB'
----------------------------------------------------------------
details:Free memory: 230.86 MB
----

We can ignore the header of this output—just as the agent plug-in does.
What's important is the keyword `details` that we specified in the query.
The check plug-in on the {CMK} server will extract the line that begins this way and use it to generate the [.guihint]#Summary# for the service.
But how does {CMK} know what the service is actually supposed to be called?
We specify the name of the service when we transfer the SQL query to the configuration for the agent plugin:

.mk-oracle.yml
[{yaml},highlight=10..12]
----
---
oracle:
  main:
    connection:
      hostname: localhost
    authentication:
      username: mymonitoringuser
      password: mypassword
      type: standard
    custom_metrics:
      - My service:
          
sql: "SELECT 'details:Free memory: ' || ROUND(SUM(bytes)/1024/1024, 2) || ' MB' FROM v$sgastat WHERE name = 'free memory'"
----

In this example, we see three things:

* To include custom SQL queries, you always need a key named `custom_metrics`.
* This is followed by a list (here: `My service`).
In YAML, such a list begins with a `-`.
The name of this list becomes part of the service name in {CMK} and can be chosen freely.
* Next comes the line containing the query.
It must begin with `sql:`, and the query itself must be enclosed in double quotes.
Also note here that the query *must not* end with a semicolon.

After you have inserted this line into the agent plugin's configuration file on your Oracle host, you can return to your {CMK} server.
There, perform a service discovery for the Oracle host.
You will then see this new service:

image::monitoring_oracle_custom_sql_discovery.png[]

The service name consists of several components:

* The name always begins with [.guihint]#ORA#.
* This is followed by the SID (here: [.guihint]#FREE#).
* The subsequent [.guihint]#SQL# indicates that this service was created by a custom SQL query.
* This is followed by the string you were able to choose freely in the configuration file, in uppercase letters.

In the Summary, you will already see data derived from the results of the SQL query.

There will certainly be cases where such a simple query is exactly what you need.
In that case, however, the service is likely still of little use.
That's why, in the chapter xref:custom_sqls_advanced[Advanced Custom SQL Queries], we will show you how to create a more comprehensive service in just a few steps.


[#custom_sqls_advanced]
=== Setting Up Advanced Custom SQL Queries

Building on the example in the chapter xref:custom_sqls_simple[Simple Custom SQL Queries] , we will now show you how to set up a service with a single metric in just a few steps.

To do this, it's a good idea to move the SQL query out of the agent plugin's configuration file.
Instead, specify a file after the `custom_metrics` key where the agent plug-in can find your SQL queries.

.mk-oracle.yml
[{yaml},highlight=10..12]
----
---
oracle:
  main:
    connection:
      hostname: localhost
    authentication:
      username: mymonitoringuser
      password: mypassword
      type: standard
    custom_metrics:
      - My advanced service:
          
path: 'myadvancedservice.sql'
----

If you specify the file without an absolute path, the agent plug-in searches several directories in a specific order.
We explain which directories these are and exactly what happens during this process in the xref:reference[Reference chapter] of this article.

Now, in the {CMK} agent's xref:glossar#dir_conf[configuration directory], create a subdirectory named `orasql` and create a file within it with the `.sql` extension, as specified after `path:`.

In this example, we assume that the configuration directory on a Linux host is `/etc/check_mk/`.
First, create the required `orasql` subdirectory.

[{shell}]
--- -
{c-root} mkdir /etc/check_mk/orasql
----

Next, create the file `myadvancedservice.sql` in that directory.
You can now much more conveniently include multi-line SQL queries in this file.
It is important to ensure that the keywords for the check plug-in are included.

./etc/check_mk/orasql/myadvancedservice .sql
[{sql}]
----
SELECT 'details:Free memory: ' || REPLACE(ROUND(SUM(bytes) / 1024 / 1024, 2), ',', '.') || ' MB' FROM v$sgastat WHERE name = 'free memory'
UNION ALL
SELECT 'perfdata:free_ram_mb=' || REPLACE(ROUND(SUM(bytes) / 1024 / 1024, 2), ',', '.') FROM v$sgastat WHERE name = 'free memory'
UNION ALL
SELECT 'exit:0' FROM dual
----

In addition to the first line, which you already know from the simple example, there is now a line that begins with `perfdata`.
{CMK} will generate a metric from this and automatically create a graph.
You can also use the keyword `exit` to influence the status of the service.
In this example, we set this to `0`, which corresponds to the status {OK} .
The check plug-in requires these keywords on the {CMK} server to generate services and metrics from the supplied data.
We explain the four keywords `details`, `perfdata`, `long`, and `exit` in detail in the chapter xref:format_and_prefixes[Output Formatting and Keywords].

Once you have set all this up and run a service discovery again, the old service will disappear, and a new one—whose name ends with [.guihint]#MY ADVANCED SERVICE#—will be found instead.

image::monitoring_oracle_custom_sql_advanced.png[]


[#migrate]
== Migration from `mk_oracle` to `mk-oracle`

[#migrate_configuration]
=== Migration of the Agent plug-in Configuration

In {CMK} {v25}, the new agent plug-in `mk -oracle` includes a feature that helps users of `mk_oracle` migrate to the new agent plugin.
To do this, copy `mk-oracle` to your Oracle host and run the agent plug-in with the `-M` option.
This might look something like this:

[{shell}]
----
{c-root} /usr/lib/check_mk_agent/plugins/packages/mk-oracle/mk-oracle -M /etc/check_mk/mk_oracle.cfg
# --- Converted from /etc/check_mk/mk_oracle.cfg at 2026-06-29 19:00:00 UTC ---
# # Syntax
# # DBUSER='USERNAME:PASSWORD:ROLE:HOST:PORT:TNSALIAS'
# DBUSER='checkmk:myPassword'
#
# DBUSER_MYINST1='cmk_specific1:myPassword1:SYSDBA: localhost:1521'
# DBUSER_MYINST2='cmk_specific2:myPassword2::localhost::INST2'
#
# ASMUSER='cmk_asm:myASMPassword:SYSASM'
# --- Known environment variables defined in legacy config ---
# DBUSER_MYINST2 cmk_specific2:myPassword2::localhost::INST2
# ASMUSER cmk_asm:myASMPassword:SYSASM
# DBUSER checkmk:myPassword
# DBUSER_MYINST1 cmk_specific1:myPassword1:SYSDBA:localhost:1521
# --- Unified Config ---
---
oracle:
  main:
    connection:
      hostname: localhost
    authentication:
      username: "checkmk"
      password: "myPassword"
      type: standard
      asm_username: 'cmk_asm'
      asm_password: "myASMPassword"
      asm_role: sysasm
    instances:
      
- sid: $ORACLE_SID
        alias: $ORACLE_SID
      - sid: MYINST2
        alias: INST2
        connection:
          hostname: localhost
        authentication:
          username: "cmk_specific2"
          password: "myPassword2"
          type: standard
      - sid: MYINST1
        connection:
          hostname: localhost
          port: 1521
        authentication:
          username: 'cmk_specific1'
          password: "myPassword1"
          type: standard
          role: sysdba
----

By default, `mk-oracle` writes the migrated configuration to `stdout`.
We strongly recommend doing this as your first step so you can check the output first.
The output begins with your old configuration as a comment.
For reference purposes, it's a good idea to keep this comment for a while.

If everything looks good, you can instruct `mk-oracle` with the `--migrate-output` to write the output directly to a file.

[{shell}]
----
{c-root} /usr/lib/check_mk_agent/plugins/packages/mk-oracle/mk-oracle -M /etc/check_mk/mk_oracle.cfg --migrate-output /etc/check_mk/mk_oracle.yml
----

If the migration command is unable to migrate parts of your old configuration, this will be clearly indicated in the output as `#WARNING`.

The following table shows which variables in the `mk_oracle` configuration are translated and how:

[cols="3,7",options="header"]
|===
|Old variable |Migrated to
| `DBUSER` (required) | Top-level `connection:` (host name, port) and `authentication:`, plus the first entry under `instances:`
| `DBUSER_<SID>` | An `instances:` entry for each `SID`, with `connection:` and `authentication:` entries per instance
| `ASMUSER` | `asm_username`, `asm_password`, `asm_role` under `authentication:`
| `REMOTE_INSTANCE_<ID>` | One `instances:` entry per ID, including `piggyback_host:` (Linux/AIX only)
| `SYNC_SECTIONS` / `ASYNC_SECTIONS` | `sections:` entries with `is_async: false` / `true`
| `SYNC_ASM_SECTIONS` / `ASYNC_ASM_SECTIONS` | `sections:` entries with `affinity: "asm"` (`"all"`)
| `CACHE_MAXAGE` | `cache_age:`
| `SQLS_MAX_CACHE_AGE` | `custom_metrics_cache_age:`
| `MAX_TASKS` | `options.threads:` (Only for values ≥ 2, up to a maximum of 8)
| `ONLY_SIDS` | `discovery.include:` (with `detect: true`)
| `SKIP_SIDS`, `EXCLUDE_<SID>="ALL"` | `discovery.exclude:` (with `detect: true`)
| `TNS_ADMIN` | `connection.tns_admin:`
| `OLRLOC` | `connection.oracle_local_registry:`
| `SQLS_SECTIONS` + `SQLS_*` per section | `custom_metrics:` entries
|===

The entries in the following table are not automatically migrated and must be manually adapted to the new plugin:

[cols="3,7",options="header"]
|===
| old variable | Note on manual migration
| `SQLS_DBUSER`, `SQLS_DBPASSWORD`, `SQLS_DBSYSCONNECT` | Login credentials per user-defined SQL; use instance-specific `authentication:` overrides instead
| `SQLS_PARAMETERS` | Passing SQL*Plus parameters is not supported.
| `EXCLUDE_<SID>="<section> ..."` | Exclusion of individual sections per SID; only `EXCLUDE_<SID>="ALL"` is converted
| `ORACLE_HOME`, `REMOTE_ORACLE_HOME` | The OIC runtime environment—specify `use_host_client` if necessary.
| `ID_BY` | Selects `SID=` over `SERVICE_NAME=` in the old connection configuration; use the instance fields `sid:` / `service_name:` instead
|===


[#migrate_sql_queries]
=== Migration of User-Defined SQL Queries

The old agent plug-in `mk_oracle` forwards user-defined SQL queries via the command-line tool `sqlplus`.
The `mk-oracle` plugin, on the other hand, executes SQL queries directly via the Oracle Instant Client driver.
This change in the execution model has two major consequences for existing SQL queries.

==== Basic limitations and execution model

===== No SQL*Plus commands

* Commands such as PROMPT, SET, COLUMN, SPOOL, EXEC/EXECUTE, or VAR/VARIABLE are features of the `sqlplus` client tool and are not part of the SQL database language.
They do not work under the OIC driver.
* No anonymous PL/SQL blocks: Anonymous blocks in the format `DECLARE/BEGIN {...} END;` cannot be executed directly as a script.
Only pure SQL statements are supported.

The agent plug-in in migration mode checks every referenced SQL file and, if discrepancies are found, issues a warning in the terminal as well as in the generated configuration file.
Although the affected sections are migrated, you must manually adjust them; otherwise, the queries will fail at runtime.

===== Features of the new execution model

* Multiple statements: A `.sql` file can contain multiple, top-level SQL statements separated by semicolons.
These are executed sequentially, and their results are concatenated.
* Each returned result must comply with the specifications for xref:format_and_prefixes[Output formatting and keywords].

==== Removing SQL*Plus commands

Formatting commands and interactive commands from `sqlplus` have no equivalent in the Oracle Instant Client driver.
You must therefore delete them without replacement.
The agent plug-in outputs each returned data row directly.

Next, we will first show an SQL file that still worked for `mk_oracle`:

[{sql}]
----
SET PAGESIZE 0
SET FEEDBACK OFF
COLUMN details FORMAT A80
PROMPT collecting session count ...
SELECT 'details:' || COUNT(*) || ' sessions' FROM v$session;
----

You must rewrite such a file for `mk-oracle` as follows so that it works with OIC:

[{sql}]
----
SELECT 'details:' || COUNT(*) || ' sessions' FROM v$session
----


==== Converting PL/SQL blocks to pure SELECT queries

In most cases, anonymous PL/SQL blocks can be replaced with standard SQL constructs:
* PL/SQL variables become WITH clauses (Common Table Expressions).
* `IF/ELSIF` conditions are replaced by CASE statements.
* `DBMS_OUTPUT.PUT_LINE` outputs are represented using `UNION ALL` or separate statements separated by semicolons.

In the following example, we show an SQL file that contains a PL/SQL block:

[{sql}]
----
SET SERVEROUTPUT ON
DECLARE
    invalid_count NUMBER;
BEGIN
    SELECT COUNT(*) INTO invalid_count
      FROM dba_objects
     WHERE status = 'INVALID';
    IF invalid_count > 10 THEN
        DBMS_OUTPUT.
 PUT_LINE('exit:2');
    ELSE
        DBMS_OUTPUT.PUT_LINE('exit:0');
    END IF;
    DBMS_OUTPUT.PUT_LINE('details:' || invalid_count || ' invalid objects');
END;
----

You must rewrite such a file into plain SQL for `mk-oracle`.
It could look something like this, for example:

[{sql}]
----
WITH invalid AS (
    SELECT COUNT(*) AS cnt
      FROM dba_objects
     
WHERE status = 'INVALID'
)
SELECT 'exit:' || CASE WHEN cnt > 10 THEN '2' ELSE '0' END FROM invalid
UNION ALL
SELECT 'details:' || cnt || ' invalid objects' FROM invalid
----


==== Moving complex PL/SQL logic into stored functions

If the logic absolutely requires PL/SQL (e.g., for loops, complex error handling, or temporary states), the logic must be moved into the database itself as a pipelined function.

To do this, you must set up the pipelined function once in your database:

[{sql}]
----
CREATE OR REPLACE FUNCTION checkmk_invalid_objects
    RETURN sys.odcivarchar2list PIPELINED
AS
    invalid_count NUMBER;
BEGIN
    SELECT COUNT(*) INTO invalid_count
      FROM dba_objects
     
WHERE status = 'INVALID';

    -- Any PL/SQL logic is allowed here
    PIPE ROW ('details:' || invalid_count || ' invalid objects');
    
PIPE ROW ('perfdata:invalid_objects=' || invalid_count || ';10;100');
    PIPE ROW (CASE WHEN invalid_count > 10 THEN 'exit:2' ELSE 'exit:0' END);
    RETURN;
END;
/

GRANT EXECUTE ON checkmk_invalid_objects TO checkmk;
----

Once this pipelined function is set up, you can have the agent plug-in call it:

[{sql}]
----
SELECT column_value FROM TABLE(checkmk_invalid_objects)
----

If the function is in a different schema, the schema name must be prefixed: `TABLE(owner.checkmk_invalid_objects)`.


[#dashboards_views]
== Dashboards and views

[#dashboards]
=== Oracle dashboards

ifdef::onprem[]
{cee-only}
The commercial editions of {CMK} are
endif::[]
ifdef::saas[]
{CE} is
endif::[]
shipped with a built-in dashboard for Oracle.
The number of dashboards for Oracle will continue to grow in the coming weeks.
Feel free to follow our link:https://checkmk.com/werks?search=oracle&product=cmk&cmk_version%5B%5D=2.5&level%5B%5D=1&level%5B%5D=2&level%5B%5D=3&class%5B%5D=feature&edition=all[Resources on Oracle at a Glance^].

You always start from the [.guihint]#Oracle databases# dashboard, which you can access via [.guihint]#Monitor > Applications > Oracle databases#:


[#reference]
== References

[#custom_sqls]
=== Custom SQL queries

Using the configuration key `custom_metrics`, you can define ad-hoc SQL queries whose output is evaluated on the {CMK} server by the Check plug-in link:https://checkmk.com/integrations/oracle_sql[oracle_sql^].
Each entry is identified by an item name, which is displayed in {CMK} as part of the service name.

==== Basics and configuration

You can either pass the SQL queries directly as a string or load them from a file.
In the following example configuration, you will find an SQL query in the global section designated by the key `oracle.main`.
Under the key `oracle. main.instances`, you can specify a key `custom_metrics`, which is then, of course, executed only for these instances.

.mk-oracle.yml
[{yaml}]
----
- --
oracle:
  main:
    connection:
      hostname: localhost
    authentication:
      username: mymonitoringuser
      password: mypassword
      type: standard
    custom_metrics:
      - product_price: # Item name -> becomes the service "<SID> SQL PRODUCT_PRICE"
          
sql: "SELECT 'details:Price OK' FROM dual"
    instances:
      - service_name: FREE
        custom_metrics:
          - last_sessions: # Runs only against this specific instance
              sql: "SELECT 'details:per-instance' FROM dual"
----

* An instance always executes both globally defined SQL queries and its own instance-specific SQL queries.
* If a global entry and an instance-specific entry share the same item name, the instance-specific entry always takes precedence.

==== External SQL files (path:)

Instead of writing SQL queries directly into the agent plugin's configuration file via `sql:`, you can use `path:` to reference external `.sql` files.
This works for both `custom_metrics` and predefined sections.

===== Variants of the path specification

.mk-oracle.yml
[{yaml}]
----
custom_metrics:
  
# 1. Absolute path to a file
  - heavy_query:
      path: '/opt/checkmk/sql/heavy_query.sql'
  
# 2. Relative path
  # First searched for in MK_LIBDIR/... and then in MK_CONFDIR/...
  - product_price:
      path: 'queries/product_price.sql'

  # 3. Directory path
  
# The filename is automatically derived from the item name (here: "sessions_stats.sql")
  - sessions_stats:
      path: 'queries/'

  # 4. File with inline fallback
  # If the file cannot be resolved, the inline SQL is used
  - last_resort:
      path: 'queries/ last_resort.sql'
      sql: "SELECT 'details:fallback' FROM dual"
----


==== Resolution rules

Absolute vs. relative::
Absolute paths are adopted exactly as specified.
Relative paths are first searched for under `MK_LIBDIR/plugins/packages/mk-oracle/orasql/` and then under `MK_CONFDIR/orasql/`.
If the file exists in both directories, the file in `MK_LIBDIR` takes precedence.

File vs. directory::
A path can refer to a specific file (with or without a `.sql` extension) or to a directory.
If it refers to a directory, the filename is derived from the item name for `custom_metrics` and from the section name for predefined sections.

Version-dependent variants::
In addition to the base file (`<name>.sql`), you can provide Oracle-specific variants in the format `<name>@<min_version>.sql` (e.g., `sessions@12010000.sql`).
The plug-in automatically selects the file with the highest `min_version` that is less than or equal to the version of the connected Oracle instance.
An 8-digit numeric format is used for versioning (`MMmmRRSSSS` for Major / Minor / Release / Patch).
Example: 12.1.0.2 → 12010002.

Fallback chain::
The order for determining the SQL query is: `path:` > (inline) `sql:` > included default code (only for predefined sections).
If `path:` fails and no `sql:` is defined, the section generates no output.


==== SQL parameters

You can define named parameters that are inserted into the SQL code before execution.
Each placeholder in the format `${<name>}` is replaced literally with the configured value.
This works for both inline SQL and `.sql` files.

.mk-oracle.yml
[{yaml}]
----
---
custom_metrics:
  - test:
      sql: 'SELECT ${parameter_1} FROM dual; SELECT ${parameter_2} FROM dual'
      sql_params:
        parameter_1: 'value_1'
        
parameter_2: '${ENV_VAR_1}'
----

* `${ENV_VAR_1}` is resolved from the environment variables.
* *Literal substitution (no bind variables)*: All values are inserted into the statement unchanged.
You can therefore also pass column or table names.
* *Environment variables*: A value can reference environment variables (format `$VAR` or `${VAR}`). These are resolved when the configuration is read.
If a referenced variable is not set, the parameter is skipped with a warning.
The placeholder remains in the SQL code (which usually results in an SQL error rather than executing a query with empty values).
* *Unused parameters*: Placeholders without a matching parameter value are ignored and remain unchanged in the code.

[#format_and_prefixes]
==== Output formatting and keywords

The `mk-oracle` agent plug-in automatically inserts the section header `pass:[<<<]<<oracle_sql:sep(58)pass:[>>>]` as well as the subsection header `[[[<SID>|<item>]]]`.
The SQL to be executed is responsible solely for the body of the output.
Each SQL query must return rows with a single string column.
The value must begin with one of the following prefixes so that the Checkmk plug-in can interpret it correctly:

* `details`:
Here you can specify what should be displayed in the [.guihint]#Summary# of the generated service.
The line begins with the keyword followed by a colon.
The rest of the line constitutes the output.

* `perfdata`:
Metrics are passed using this keyword.
Within a single line, you can specify any number of metrics—separated by a space.
You can also spread the metric output across multiple lines.
Simply start each line with the keyword `perfdata:`.

* `long`:
If you want the service to have a long output for the [.guihint]#Details# field, you can specify it here.
You can also use this keyword multiple times to generate several lines in the [.guihint]#Details#.

* `exit:`:
If the output should have a specific status, you can specify it here.
The familiar values `0`, `1`, `2`, `3` are available for the statuses {OK}, {WARN}, {CRIT}, {UNKNOWN}.


[#options_ruleset]
[#configuration_options]
=== Configuration options

In this chapter, we explain all the options available for configuring the agent plugin.
This applies whether you configure the agent plugin using the [.guihint]#Unified Oracle Plugin (Beta)# rule set or configure it manually.

The explanations in this manual sometimes go beyond the content of the inline help—for example, when there is interesting background information about an option or when a more detailed explanation is appropriate for specific use cases.
We also specify which entry is created in the `mk-oracle.yml` configuration file as a result of a selected option.

In addition, anyone interested can find a description of all options in `mk-oracle.yml` in our link:https://github.com/Checkmk/checkmk/tree/master/packages/mk-oracle#yaml-configuration[description on GitHub].

[#additional_options]
[discrete]
==== Additional Options > Maximum connections

Maximum number of database connections to open.

.configuration file (Example)
[%collapsible]
====
[#max_connections]
.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    options:
      max_connections: <int>
----
====

[discrete]
==== Additional Options > Maximum queries

Maximum number of queries that may be executed per connection.

.Configuration file (example)
[%collapsible]
====
[#max_queries]
.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    options:
      max_queries: <int>
----
====


[discrete]
==== Additional Options > Ignore database name

.Configuration file (example)
[%collapsible]
====
[#ignore_db_name]
.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    options:
      IGNORE_DB_NAME: "0|1"
----
====


[discrete]
==== Additional Options > Oracle Instant Client options

.Configuration file (example)
[%collapsible]
====
[#use_host_client]
.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    options:
      use_host_client: "auto|never"
----
====


[discrete]
==== Authentication > Authentication type > Oracle Wallet

Oracle Wallet provides a secure way to authenticate to Oracle databases without storing plaintext passwords in configuration files.

.Configuration file (example)
[%collapsible]
====
[#authentication_type_wallet]
.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    authentication:
      type: wallet
----
====


[discrete]
==== Authentication > Type

Here you can choose between `standard` and `wallet`.
If you select `wallet`, no further settings are required here.
`standard` refers to authentication via username and password.
In that case, specifying `username` and `password` is mandatory.

.Configuration file (example)
[%collapsible]
====
[#authentication_type]
.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    authentication:
      type: "standard|wallet"
----
====


[discrete]
==== Authentication > Authentication type > User & password

.Configuration file (example)
[%collapsible]
====
[#authentication_user]
.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    authentication:
      username: 'myuser'
      password: 'mypassword'
      type: 'standard'
----
====


[discrete]
==== Authentication > Role

If you have chosen authentication via username and password, you can optionally specify the user's role.

.Configuration file (example)
[%collapsible]
====
[#authentication_role]
.mk-oracle.yml
[{yaml}]
- ---
---
oracle:
  main:
    authentication:
      username: 'myuser'
      password: 'mypassword'
      type: 'standard'
      role: "sysdba|sysoper|sysasm|szsbackup|sysdg|syskm"
----
====


[discrete]
==== Connection options

This option allows you to define network-level connection parameters that apply to all instances unless overridden in the database-specific settings.

.Configuration file (example)
[%collapsible]
====
[#connection_options]
.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    connection:
       hostname: 'myhostname' # optional, default: 'localhost'
       port: 1521 # optional, default: 1521
       timeout: 5 # optional, default: 5 (seconds)
       tns_admin: '/ path/to/oracle/config/files/' # optional, default: MK_CONFDIR
       oracle_local_registry: '/etc/oracle/olr.loc' # optional, path to the Oracle Local Registry
----
====


[discrete]
==== Cache age

The [.guihint]#Cache age# option allows you to specify the cache validity period for the section you are monitoring asynchronously.

.Configuration file (example)
[%collapsible]
====
[#cache_age]
.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    cache_age: <int> # Default: 600 (seconds)
----
====


[discrete]
==== Custom Metrics cache age

.Configuration file (example)
[%collapsible]
====
[#custom_metrics_cache_age]
.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    custom_metrics_cache_age: <int> # Default: 600 (seconds)
----
====

[discrete]
==== Instance Discovery

Using the options under [.guihint]#Instance discovery#, you can precisely control which instances you want to monitor with {CMK} and which ones you may want to exclude from monitoring.
This option only works if the agent plug-in is located on the same host as the instances.

.Configuration file (example)
[%collapsible]
====
[#instance_discovery]
.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    discovery:
      detect: yes # Enable automatic instance detection
      include: ['PROD', 'DEV'] # Optional; only the instances specified here are monitored
      
exclude: ['TEST'] # optional; the instances specified here are ignored
----
====


[discrete]
==== Sections--data to collect

This option allows you to precisely specify which data about your instances and databases should be queried by the agent plugin.
Additionally, you can specify for each section whether the data should be retrieved synchronously or xref:agent_linux#async_plugins[asynchronously].
The `is_async` settings in the following example show our default configurations.

.Configuration file (example)
[%collapsible]
====
[#sections]
.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    sections:
    - instance:
        is_async: false
    - asm_instance:
        is_async: false
    
- asm_diskgroup:
        is_async: true
    - dataguard_stats:
        is_async: false
    
- locks:
        is_async: false
    - logswitches:
        is_async: false
    - longactivesessions:
        is_async: false
    - performance:
        is_async: false
    - processes:
        is_async: false
    - recovery_area:
        is_async: false
    
- recovery_status:
        is_async: false
    - sessions:
        is_async: false
    - systemparameter:
        is_async: false
    - undostat:
        is_async: false
    - iostats:
        is_async: true
    
- jobs:
        is_async: true
    - resumable:
        is_async: true
    - rman:
        is_async: true
    - tablespaces:
        is_async: true
----
====

[discrete]
==== Databases to monitor

If individual or even all Oracle instances on a host have different login credentials or connection settings, you must configure them individually in the [.guihint]#Databases to monitor# section.
To identify the individual databases, you can choose from [.guihint]#SID#, [.guihint]#Alias#, and [.guihint]#Service Name#.
At this point, you should consult the inline help to learn more about these three options.
You can also find additional configuration examples in our link:https://github.com/Checkmk/checkmk/tree/master/packages/mk-oracle#instances[description on GitHub^].

.Configuration File (Example)
[%collapsible]
====
.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    instances:
      - service_name: MYSERVICE
      - sid: MYSID
      - alias: MYALIAS
----
====

[#sample_configurations]
=== Sample configurations

You can use the following sample configurations as starting points for the xref:create_config[manual setup] of your Oracle monitoring.

[#minimal_configuration]
==== Minimal configuration

.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    authentication:
      password: mypassword
      type: standard
      username: C##checkmk
    cache_age: 600
    connection:
      hostname: localhost
    custom_metrics_cache_age: 600
    instances: []
----

==== (Almost) complete example

Additional options have been added since this example was created.
The example will be updated shortly.



.mk-oracle.yml
[{yaml}]
----
system:
  logging:
    level: 'warn'
    max_size: 1000000
    max_count: 5

oracle:
  main:
    options:
      max_connections: 6
      use_host_client: never
      IGNORE_DB_NAME: 0
    connection:
      hostname: 'localhost'
      port: 1521
      timeout: 5
      tns_admin: '/etc/check_mk'
    authentication:
      username: 'mymonitoringuser'
      password: 'mypassword'
      role: 'sysdba'
      type: 'standard'
    discovery:
      detect: yes
      include: ['PROD', 'DEV']
      exclude: ['TEST']
    instances:
      - service_name: 'ORCL'
        sid: 'ORCL'
      - alias: 'REMOTE_DB'
    sections:
      - instance:
          affinity: 'db'
      - tablespaces:
          is_async: yes
      - performance:
      - sessions:
    cache_age: 600
    custom_metrics_cache_age: 600
    piggyback_host: 'mypiggybackhost'
----
// delete to here












// start translation
////
[#introduction]
== Einleitung

In diesem Artikel erfahren Sie, wie Sie mit dem neuen Agentenplugin `mk-oracle` Oracle-Datenbanken überwachen können.

Wenn Sie Informationen zum alten Agentenplugin `mk_oracle` suchen, schauen Sie bitte in den link:https://docs.checkmk.com/2.4.0/de/monitoring_oracle.html[gleichnamigen Artikel im 2.4.0er-Zweig des Handbuchs].

So können Sie mit dem xref:glossar.html#agent_plugin[Agentenplugin] nicht nur Tablespaces oder die aktiven Sitzungen einer Datenbank abrufen, sondern zusätzlich noch viele andere Metriken.
Eine vollständige Liste der Überwachungsmöglichkeiten mit unseren xref:glossar#check_plugin[Check-Plugins] können Sie im link:https://checkmk.com/de/integrations?tags=oracle[Katalog der Check-Plugins^] nachlesen.

Die Ausgaben von `mk-oracle` gleichen denen des alten Plugins, so das exakt die selben Check-Plugins weiterverwendet werden.

[TIP]
====
Das neue Agentenplugin für die Überwachung von Oracle ist eine ausführbare Binärdatei.
Es wird bereitgestellt für IBM AIX (64 Bit POWER), Linux (`x86_64`), Oracle Solaris (`x86_64`) und Windows (`x86_64`).
Wir werden das neue Agentenplugin in einer der kommenden Versionen von {CMK} auch für Linux auf `aarch64` (64 Bit ARM) anbieten.
Bis dahin müssen Sie auf dieser Architektur zum alten Shell-Skript greifen.
====


[#overview]
=== Überblick über diesen Artikel

Falls Sie nicht (fast) den ganzen langen Artikel lesen möchten, kommen hier ein paar Links mit Abkürzungen.
Wenn Sie allerdings erstmalig das Oracle-Monitoring mit {CMK} aufsetzen, sollten Sie sich den Artikel vollständig durcharbeiten, um alle Möglichkeiten kennenzulernen.

* Wie Sie den Oracle Instant Client (OIC) installieren erfahren Sie im Kapitel xref:install_oic[„Oracle Instant Client (OIC) bereitstellen“].

* Nutzende der Agentenbäckerei in den kommerziellen Editionen von {CMK} bekommen im Kapitel xref:configuration_bakery[„Konfiguration für das Agentenplugin erstellen“] eine Erklärung der wichtigsten Optionen.
Im xref:reference[Referenzteil] dieses Artikels gehen wir dann nochmal auf alle xref:configuration_options[Konfigurationsoptionen] ein und zeigen an dieser Stelle auch, zu welchen Einträgen die Optionen aus der Regel [.guihint]#Unified Oracle plug-in (experimental)# in der Konfigurationsdatei `mk-oracle.yml` führt.

* Sollten Ihnen die Datenpunkte, die das Agentenplugin „out-of-the-box“ mitbringt, nicht genügen, können Sie Ihre Datenbanken auch per xref:custom_sqls[benutzerdefinierten SQL-Abfragen (Custom SQLs)] überwachen.

* Nutzende, die vom `mk_oracle` auf `mk-oracle` umsteigen möchten, können unsere Skripte zur Regel- und Konfigurationsmigration verwenden, um sich den Umstieg zu erleichtern.


[#how]
=== Wie funktioniert das Oracle-Monitoring mit {CMK}?

Um Oracle-Instanzen und deren -Datenbanken mit {CMK} zu überwachen, müssen auf dem jeweiligen Host der {CMK}-Agent und das Agentenplugin `mk-oracle` installiert werden.
Das Agentenplugin benötigt eine Konfiguration, welche Sie unter dem Namen `mk-oracle.yml` im xref:glossar#dir_conf[Konfigurationsverzeichnis] ablegen müssen.
Außerdem ist das Agentenplugin auf Bibliotheken des Oracle Instant Client (OIC) angewiesen.
Dieser muss also ebenfalls auf dem Host verfügbar sein.

Wie Sie OIC installieren können, erfahren Sie im Kapitel xref:prerequisites_oracle_host[Voraussetzungen auf dem Oracle-Host schaffen].

[#prerequisites_oracle_host]
== Voraussetzungen auf dem Oracle-Host schaffen

Auf jedem Host auf dem sie Oracle-Datenbanken bzw. -Instanzen überwachen möchten, müssen Sie zwei Voraussetzungen schaffen.

* Die Client-Bibliothek _Oracle Instant Client (OIC)_ muss für das Agentenplugin bereitgestellt werden.
Wie Sie das erledigen können, lesen Sie im xref:install_oic[entsprechenden Kapitel].
* Dem Agentenplugin `mk-oracle` muss Zugriff auf Ihre Oracle-Instanzen beziehungsweise -Datenbanken gewährt werden. Dafür haben Sie zwei verschiedene Möglichkeiten:
** Sie können auf dem Host einen Benutzer samt Passwort erzeugen, der ausschließlich für das Monitoring durch {CMK} verwendet wird.
Diesen Weg erklären wir im Kapitel xref:create_user[Datenbankbenutzer erstellen].
** Alternativ können Sie die xref:oracle_wallet[Oracle-Wallet nutzen].

[#install_oic]
=== Oracle Instant Client (OIC) bereitstellen

Das Agentenplugin `mk-oracle` benötigt die Bibliotheken des Oracle Instant Client (OIC) um zu funktionieren.
Sie haben drei Möglichkeiten, dass OIC zur Verfügung zu stellen.

* Sie können OIC xref:#install_oic[exklusiv für das Agentenplugin installieren]. Dies ist die von uns empfohlene Methode, die wir hier beschreiben.
* Alternativ können Sie OIC xref:#install_oic_globally[global auf dem Host installieren].
* Falls OIC bereits auf dem Host installiert ist, und Sie diese Installation für die Überwachung durch {CMK} verwenden wollen, können Sie dem Agentenplugin mit einem Eintrag in der Konfiguration mitteilen, wo OIC zu finden ist. Link zu der Option in der Regel.

Laden Sie zuerst das für Ihren Host passende Paket von der Seite link:https://www.oracle.com/de/database/technologies/instant-client/downloads.html[Oracle Instant Client Downloads] im ZIP-Format herunter.

[.tabs]
====
[#tab_oic_linux]#Linux#::
+
--
Für ein Linux x86-64 nehmen Sie beispielsweise das link:https://download.oracle.com/otn_software/linux/instantclient/2326200v2/instantclient-basiclite-linux.x64-23.26.2.0.0.zip[Basic Light Package (ZIP)]. 

Prüfen Sie, wie das Plugin-Verzeichnis des {CMK}-Agenten auf Ihren Host lautet:

[{shell-raw}]
----
{c-root} plugindir=$(cmk-agent-ctl dump | grep "^PluginsDirectory" | head -1 | awk '{print $2}')
{c-root} echo "$plugindir"
/usr/lib/check_mk_agent/plugins
----

Entpacken Sie die Archivdatei in eben jenes Plugin-Verzeichnis:

[{shell-raw}]
----
{c-root} mkdir -p "${plugindir}/mk-oracle"
{c-root} unzip -j instantclient-basiclite-linux.x64-23.26.2.0.0.zip -d "${plugindir}/mk-oracle"
----

Da einige Zip-Dateien von Oracle Linux-User-IDs enthalten und damit möglicherweise ein vorhandener Nutzer mit dieser ID Eigentümer der Client-Bibliothek würde, ändern Sie sicherheitshalber Eigentümer und Zugriffsrechte:

[{shell-raw}]
----
{c-root} chown -R root:root "${plugindir}/mk-oracle"
{c-root} chmod -R go-w "${plugindir}/mk-oracle"
----


--
[#tab_oic_windows]#Windows#::
+
--
Für Windows nehmen Sie beispielsweise das link:https://download.oracle.com/otn_software/nt/instantclient/2326300/instantclient-basiclite-windows.x64-23.26.3.0.0.zip[Basic Light Package (ZIP)]. 

Entpacken Sie die das Archiv in das Unterverzeichnis `packages\mk-oracle` des xref:glossar#dir_plugins[Plugin-Verzeichnisses].

Standardmäßig lautet dieses auf einem Windows-Host lautet `%PROGRAMDATA%\checkmk\agent\plugins\packages\mk-oracle`.
--
====

.OIC global installieren
[%collapsible]
====
[#install_oic_globally]
Auf einem Linux-Host können Sie den Oracle Instant Client alternativ auch beispielsweise auch in `/opt/` installieren.

[{shell}]
----
{c-local} sudo unzip -j instantclient-basiclite-linux.x64-23.26.2.0.0.zip -d /opt/checkmk/oracle-instant-client
----    

Auf einem Windows-Host können Sie den Oracle Instant Client auch in ein beliebiges Verzeichnis installieren.
Dieses Verzeichnis müssen dann in der Umgebungsvariable `PATH` von Windows angeben.
====


[#allow_access]
=== Zugriffsrechte für das Agentenplugin gewähren

Dem Agentenplugin `mk-oracle` muss Zugriff auf Ihre Oracle-Instanzen bzw. -Datenbanken gewährt werden.
Dies können Sie entweder über einen Datenbank-Benutzer oder die Oracle-Wallet machen.

[.tabs]
====
[#tab_simple_user]#Einfacher Datenbank-Benutzer#::
+
--
[#create_user]
Wenn Sie für das Monitoring einen regulären Benutzer einrichten wollen, dann empfehlen wir, eben diesen Benutzer ausschließlich für {CMK} zu verwenden.

Verbinden Sie sich als erstes mit Ihren Oracle-Host und wechseln Sie zu dem Benutzer, unter dessen Kennung die Oracle Datenbank läuft. Meist ist dies `oracle`:

[{shell}]
----
{c-root} su - oracle
----

Hinterlegen Sie die Instanz, mit der sich `sqlplus` verbinden soll in einer Umgebungsvariablen:

[{shell}]
----
oracle@linux$ export ORACLE_SID=MYINST1
----

Starten Sie `sqlplus`.
Geben Sie dabei an, mit welcher Rolle `sqlplus` starten soll.

[{shell}]
----
oracle@linux$ sqlplus / as sysdba
----

Erzeugen Sie jetzt einen Benutzer, welcher ausschließlich für die Überwachung von Oracle verwendet werden soll.
Die Berechtigungen gelten für alle existierenden und zukünftigen Datenbanken.

[{sql}]
----
sqlplus> create user checkmk identified by myPassword;
sqlplus> alter user checkmk set container_data=all container=current;
sqlplus> grant select_catalog_role to checkmk container=all;
sqlplus> grant create session to checkmk container=all;
sqlplus> exit
----
--


[#tab_user]#Datenbank-Benutzer (Multi-tenant)#::
+
--
[#create_user_multi-tenant]
Wenn Sie für das Monitoring einen Benutzer einrichten wollen, dann empfehlen wir, eben diesen Benutzer ausschließlich für {CMK} zu verwenden.

Verbinden Sie sich als erstes mit Ihren Oracle-Host und wechseln Sie zu dem Benutzer, unter dessen Kennung die Oracle Datenbank läuft. Meist ist dies `oracle`:

[{shell}]
----
{c-root} su - oracle
----

Hinterlegen Sie die Instanz, mit der sich `sqlplus` verbinden soll in einer Umgebungsvariablen:

[{shell}]
----
oracle@linux$ export ORACLE_SID=MYINST1
----

Starten Sie `sqlplus`.
Geben Sie dabei an, mit welcher Rolle `sqlplus` starten soll.

[{shell}]
----
oracle@linux$ sqlplus / as sysdba
----

Erzeugen Sie jetzt einen Benutzer, welcher ausschließlich für die Überwachung von Oracle verwendet werden soll.
Das Präfix `C##` sorgt dafür, dass der Nutzer alle Datenbanken der Instanz sehen kann.
Die Berechtigungen gelten für alle existierenden und zukünftigen Datenbanken.

[{sql}]
----
sqlplus> create user C##checkmk identified by myPassword;
sqlplus> alter user C##checkmk set container_data=all container=current;
sqlplus> grant select_catalog_role to C##checkmk container=all;
sqlplus> grant create session to C##checkmk container=all;
sqlplus> exit
----
--

[#tab_wallet]#Wallet#::
+
--

[#oracle_wallet]
Alternativ dazu, den Benutzer direkt und mit Passwort in einer Konfigurationsdatei anzugeben, können Sie auch das _Oracle-Wallet_ nutzen.
Das hat den Vorteil, dass Sie nicht mehr die Zugangsdaten unverschlüsselt sowohl auf dem {CMK}-Server als auch auf dem Oracle-Host ablegen müssen.
Denn selbst wenn Sie die Rechte der Konfigurationsdatei auf dem Oracle-Host entsprechend angepasst haben, haben die Zugangsdaten dennoch den Server verlassen und befinden sich auf dem {CMK}-Server,
sofern Sie die Agentenbäckerei nutzen.

Das Oracle-Wallet wiederum legt die Zugangsdaten verschlüsselt auf dem zu überwachenden Host ab, sodass sie benutzt werden können, aber keine Login-Daten explizit bekannt gemacht werden müssen.
{CMK} kann dieses Wallet nutzen, sodass die Zugangsdaten prinzipiell nur dem Datenbankadministrator (DBA) bekannt sein müssen.
Dazu legen Sie -- oder der DBA -- ein Wallet auf dem entsprechenden Server an und legen diese im xref:glossar#dir_conf[Konfigurationsverzeichnis] in einer Datei namens `oracle_wallet` ab.

[{shell}]
----
{c-root} mkstore -wrl /etc/check_mk/oracle_wallet -create
----

Auf diese Datei wird das Agentenplugin später immer dann zugreifen, wenn eine Verbindung zu einer Instanz hergestellt werden soll.
Damit die nötigen Benutzerdaten auch gefunden werden, müssen Sie diese einmalig in das Wallet eintragen.
In dem folgenden Beispiel fügen Sie also den Benutzer `checkmk` für die Instanz `MYINST1` hinzu:

[{shell}]
----
{c-root} mkstore -wrl /etc/check_mk/oracle_wallet -createCredential MYINST1 checkmk myPassword
----

Damit das Agentenplugin weiß, wo es nach dem Wallet suchen muss, muss es zwei Dateien finden.
Die erste Datei ist `sqlnet.ora` in welcher hinterlegt wird, wo das Wallet zu finden ist.
Die zweite Datei -- `tnsnames.ora` -- bestimmt die Adresse der Instanz, sodass diese auch über ihren Alias angesprochen werden kann.
Damit das Agentenplugin diese Dateien findet, können Sie den Pfad unter Linux, Solaris und AIX über die Umgebungsvariable `TNS_ADMIN` setzen.
Das ist vor allem dann von Vorteil, wenn die Dateien bereits existieren.
Alternativ können Sie sie auch explizit anlegen.
Unter Windows ist es sogar erforderlich, dass Sie diese manuell bestimmen.

Legen Sie zunächst die Datei `sqlnet.ora` an.
In dieser Datei sucht das Agentenplugin alternativ nach den Zugangsdaten, sodass hier also der korrekte Pfad zu der eben erstellten Wallet-Datei angegeben werden muss.
Achten Sie dabei darauf, dass Sie den Parameter `SQLNET.WALLET_OVERRIDE` auf `TRUE` setzen:

./etc/check_mk/sqlnet.ora
[{file}]
----
LOG_DIRECTORY_CLIENT = /var/log/check_mk/oracle_client
DIAG_ADR_ENABLED = OFF

SQLNET.WALLET_OVERRIDE = TRUE
WALLET_LOCATION =
 (SOURCE=
   (METHOD = FILE)
   (METHOD_DATA = (DIRECTORY=/etc/check_mk/oracle_wallet))
 )
----

Jetzt weiß das Plugin, welche Zugangsdaten benutzt werden sollen.
Damit es auch die korrekte Adresse ansteuert, legen Sie als Datei die `tnsnames.ora` an.
Die genaue Syntax können Sie der Oracle-Dokumentation entnehmen, aber als Beispiel könnte die Datei so aussehen:

./etc/check_mk/tnsnames.ora
[{file}]
----
MYINST1
  (DESCRIPTION =
    (ADDRESS = (PROTOCOL = TCP)(HOST = 127.0.0.1)(PORT = 1521))
    (CONNECT_DATA =
      (SERVER = DEDICATED)
      (SERVICE_NAME = MYINST1_ALIAS)
    )
  )
----


Sie können dem Wallet natürlich zu einem späteren Zeitpunkt weitere Zugangsdaten hinzufügen.
Lediglich die Datei `tnsnames.ora` muss dann gegebenenfalls erweitert werden.

Zum Abschluss ändern Sie die Berechtigungen der in diesem Abschnitt manuell erstellten Dateien und Verzeichnisse, damit die Zugriffsrechte bei der Ausführung richtig gesetzt sind.
Das Agentenplugin, das als `root` ausgeführt wird, wechselt zum Eigentümer der Oracle-Binärdateien (z.B. `$ORACLE_HOME/bin/sqlplus`), bevor es diese ausführt.
Zumindest die Gruppe des Eigentümers der Oracle-Binärdateien benötigt daher lesenden Zugriff auf die manuell erstellten Dateien in `/etc/check_mk/`.
Im folgenden Beispiel nehmen wir an, dass es sich bei der Gruppe um `oinstall` handelt.

Die folgenden Befehle ändern die Gruppe zu `oinstall`:

[{shell}]
----
{c-root} chgrp oinstall /etc/check_mk/sqlnet.ora /etc/check_mk/tnsnames.ora
{c-root} chgrp -R oinstall /etc/check_mk/oracle_wallet
----

Diese Befehle stellen dann sicher, dass die Gruppe das Verzeichnis `oracle_wallet` samt Inhalt lesen kann:

[{shell}]
----
{c-root} chmod g+x /etc/check_mk/oracle_wallet
{c-root} chmod -R g+r /etc/check_mk/oracle_wallet
----

Anschließend sollten die Berechtigungen etwa wie folgt aussehen:

[{shell}]
----
{c-root} tree -ugpR /etc/check_mk
[drwxr-xr-x root     root    ]  /etc/check_mk
├── [drwxr-x--- root   oinstall ]  oracle_wallet
│   └── [-rw-r----- root   oinstall ]  cwallet.sso
│   └── [-rw-r----- root   oinstall ]  cwallet.sso.lck
│   └── [-rw-r----- root   oinstall ]  ewallet.p12
│   └── [-rw-r----- root   oinstall ]  ewallet.p12.lck
├── [-rw-r--r-- root   oinstall ]  sqlnet.ora
├── [-rw-r--r-- root   oinstall ]  tnsnames.ora
----

Die Befehlsausgabe zeigt nur Dateien und Verzeichnisse, die für die Konfiguration der Oracle Wallet relevant sind.
--
====


[#plugin_configuration]
== Konfiguration des Agentenplugins

Nachdem Sie auf dem Oracle-Host die Voraussetzungen für das Monitoring mit {CMK} geschaffen haben, müssen Sie nun eine Konfiguration für das Agentenplugin erstellen.

* Nutzer unserer kommerziellen Editionen und der Agentenbäckerei finden im Kapitel xref:configuration_bakery[Konfiguration für das Agentenplugin per Regel erstellen] was zu tun ist.
* Falls Sie Ihre Oracle-Instanzen mit {CRE} überwachen möchten oder die Konfiguration aus anderen Gründen manuell vornehmen möchten, lesen Sie jetzt bitte das Kapitel xref:manual_configuration[Überwachung von Oracle manuell einrichten].


[#configuration_bakery]
=== Per Agentenregel und Agentenbäckerei

{cee-only} Nutzer einer kommerziellen Edition von {CMK} können das Agentenplugin bequem über die Agentenregel [.guihint]#Unified Oracle Plugin (Experimental)# konfigurieren.
In diesem Kapitel konzentrieren wir uns dabei auf die Bereiche xref:authentication[Authentifizierung], xref:configure_connection_options[Verbindungsoptionen] und xref:database_specific_authentication[Datenbankspezifische Anmeldedaten].
Alle weiteren Optionen, die der Regelsatz [.guihint]#Unified Oracle Plugin (Experimental)# werden entweder schon hinreichend in der Inline-Hilfe erklärt oder im xref:reference[Referenzteil] dieses Artikels erläutert.

Öffnen Sie jetzt also das [.guihint]#Setup#-Menü Ihrer {CMK}-Instanz und suchen Sie dort nach [.guihint]#Unified Oracle Plugin (Experimental)# und öffnen Sie den Regelsatz.
Klicken Sie auf [.guihint]#Add rule# und beginnen Sie mit der Konfigurations der Authentifizierungsmethode.

Im Referenzteil finden Sie Hinweise zu den xref:additional_options[[.guihint]#Additional options#].

[#authentication]
==== Authentifizierungstyp einstellen

Je nachdem für welchen Weg Sie sich im Kapitel xref:prerequisites_oracle_host[Voraussetzungen auf dem Oracle-Host schaffen] entschieden haben, wählen Sie unter [.guihint]#Authentication type# die passende Option aus und tragen die zugehörigen Daten ein.

image::monitoring_oracle_authentication_user.png[]

Optional haben Sie über [.guihint]#Role# die Möglichkeit anzugeben, welche Rolle das Agentenplugin bei der Authentifizierung annehmen soll.
Und wenn Sie für die [.guihint]#ASM Authentication# ein anderer Nutzername verwendet werden soll, können Sie auch das hier eintragen.


[#configure_connection_options]
==== Verbindungsoptionen konfigurieren

Im nächsten Block können Sie Details zu den Verbindungsoptionen konfigurieren.
Neben dem Host-Namen und dem Port Ihrer Oracle-Instanz, können Sie auch den [.guihint]#TNS_ADMIN directory path# angeben.
Wenn Sie hier nichts eintragen, geht `mk-oracle` davon aus, das es sich Dabei um das Konfigurationsverzeichnis des Agenten handelt.
Mit der Option [.guihint]#Oracle Local Registry path# können Sie dem Agentenplugin mitteilen, wo die lokalen Registrierungsdateien Ihres Oracle-Cluster liegen.
Die Instanzerkennung des Agentenplugins wird dann diese Informationen heranziehen, und die darin beschriebenen Instanzen überwachen.

[#database_specific_authentication]
==== Datenbankspezifische Anmeldedaten

Gerade in größeren Umgebungen wird es mitunter nötig sein, dass verschiedene Instanzen Ihrer Oracle-Umgebung mit unterschiedlichen Zugangsdaten und Verbindungsoptionen ausgestattet sind.
Dafür ist die Sektion [.guihint]#Databases to monitor# da.
Hier können Sie instanzspezifische Anmeldedaten und Verbindungsoptionen angeben.
Ihre Angaben unter [.guihint]#Default settings# beziehen sich dann nur noch auf die Instanzen, welche Sie unter [.guihint]#Databases to monitor# nicht explizit angegeben haben.

[#additional_configurations_options]
==== Weitere Konfigurationsmöglichkeiten

Alle weiteren Optionen der Regel [.guihint]#Unified Oracle Plugin (Experimental)# werden im Kapitel xref:reference[Referenzen] beschrieben.

[#set_conditions]
==== Bedingungen einstellen

Denken Sie zum Abschluss daran Ihre neue Regel [.guihint]#Unified Oracle plug-in# über die [.guihint]#Conditions# auf Ihre Oracle-Hosts einzuschränken.

Speichern Sie nun die Regel, xref:wato_monitoringagents#bakery[backen Sie das neue Agentenpaket] und installieren Sie dieses auf dem Oracle-Host.
Übertragen Sie das neue Agentenpaket nun auch Ihren Oracle-Host und installieren Sie es.
Danach können Sie in {CMK} die Service-Erkennung auf Ihrem Oracle-Host durchführen.


[#manual_configuration]
=== Manuelle Konfiguration

Dieses Kapitel richtet sich an all diejenigen, die {CRE} verwenden oder auf die Verwendung der Agentenbäckerei verzichten möchten.

Sobald Sie die xref:prerequisites_manual_configuration[Voraussetzungen] erfüllt haben, müssen Sie das xref:install_agent_plugin[Agentenplugin auf dem Oracle-Host installieren].
An dieser Stelle sei bereits der Hinweis erlaubt, dass das etwas anders funktioniert, als Sie es womöglich von anderen Agentenplugins gewohnt sind.

Zu guter Letzt müssen Sie dem Agentenplugin noch eine xref:create_config[Konfiguration mit auf den Weg geben].

Zuerst gehen wir nochmal die Voraussetzungen durch.

[#prerequisites_manual_configuration]
==== Voraussetzungen für die manuelle Einrichtung

Bevor Sie mit der manuellen Einrichtung starten können, prüfen Sie, ob die folgenden beiden Voraussetzungen erfüllt sind:

* Auf dem Oracle-Host muss der {CMK}-Agent bereits installiert und registriert sein.
Wie Sie das bewerkstelligen, erklären wir bereits in den Artikeln xref:agent_linux#install[Linux überwachen] bzw. xref:agent_windows#install[Windows überwachen].
* Außerdem müssen Sie bereits dafür gesorgt haben, dass der xref:install_oic[Oracle Instant Client auf dem Host verfügbar ist].

Sobald alle Voraussetzungen erfüllt sind, können Sie sich daran machen, das Agentenplugin zu installieren.

[#install_agent_plugin]
==== Agentenplugin installieren

Das Agentenplugin `mk-oracle` kommt mit zwei Helfern daher, die letzten Endes nur dafür da sind, um `mk-oracle` selbst im synchronen oder im asynchronen Modus zu starten.
Diese beiden Helfer müssen Sie ebenfalls auf dem Host installieren, was wir zuerst zeigen.

[.tabs]
====
[#tab_install_linux]#Linux#::
+
--
ifdef::onprem[]
Zusätzlich zum Agentenplugin `mk-oracle` benötigen Sie noch die Dateien `oracle_unified_sync` und `oracle_unified_async`.
Sie finde alle drei Dateien in {CRE} über [.guihint]#Setup > Agents > Other operating systems > Plug-ins#.
In den kommerziellen Editionen gelangen Sie im [.guihint]#Setup#-Menü über [.guihint]#Agents > Windows, Linux, Solaris, AIX# zunächst in die xref:wato_monitoringagents#bakery[Agentenbäckerei], wo Sie die gebackenen Pakete finden.
Von dort aus kommen Sie mit dem Menüeintrag [.guihint]#Related > Other operating systems# zur Liste der Agentendateien.

Wenn Sie Dateisystemzugriff haben, finden Sie alle benötigten Dateien im Verzeichnis `~/version/lib/python3/cmk/plugins/oracle/agents/` Ihrer {CMK}-Instanz.
endif::[]
ifdef::saas[]
Zusätzlich zum Agentenplugin `mk-oracle` benötigen Sie noch die Dateien `oracle_unified_sync` und `oracle_unified_async`.
In {CE} gelangen Sie im [.guihint]#Setup#-Menü über [.guihint]#Agents > Windows, Linux, Solaris, AIX# zunächst in die xref:wato_monitoringagents#bakery[Agentenbäckerei], wo Sie die gebackenen Pakete finden.
Von dort aus kommen Sie mit dem Menüeintrag [.guihint]#Related > Other operating systems# zur Liste der Agentendateien.
endif::[]


Übertragen Sie die drei Dateien auf beliebigem Weg auf den Oracle-Host.
Wenn auf dem Host `wget` zur Verfügung steht, könnten Sie so vorgehen:

[{shell-raw}]
----
{c-root} wget -P /tmp http://mycmkserver/mysite/check_mk/agents/mk-oracle
{c-root} wget -P /tmp http://mycmkserver/mysite/check_mk/agents/oracle_unified_sync
{c-root} wget -P /tmp http://mycmkserver/mysite/check_mk/agents/oracle_unified_async
----

Hier laden wir die drei Dateien in das Verzeichnis `/tmp` auf dem Oracle-Host.
Davon gehen wir in den weiteren Schritten jetzt auch aus.

Installieren Sie das Plugin auf dem Oracle-Host in das xref:glossar#dir_plugins[Plugin-Verzeichnis] des Agenten (im Standardfall `/usr/lib/check_mk_agent/plugins`).
Wie das Plugin-Verzeichnis auf Ihrem Host lautet, können Sie in der Ausgabe des {CMK}-Agenten ablesen.
Im folgenden Befehl wird das Plugin-Verzeichnis in die Variable `MK_LIBDIR` geschrieben.
Die weiteren Befehle nutzen dann auch `$MK_LIBDIR`.
Sollte der nächste Befehl bei Ihnen keinen sinnvollen Output liefern, weil beispielsweise `awk` nicht zur Verfügung steht, dann schreiben Sie das tatsächliche Plugin-Verzeichnis einfach selber in `MK_LIBDIR` rein, damit Sie danach wieder mit den angegebenen Befehlen arbeiten können.

[{shell}]
----
pass:[#] Pfad dynamisch ermitteln und in Variable speichern
{c-root} export MK_LIBDIR=$(cmk-agent-ctl dump | awk '/^PluginsDirectory:/ {print $2; exit}')
pass:[#] Testen, ob es geklappt hat
{c-root} echo pass:[$]MK_LIBDIR
/usr/lib/check_mk_agent/plugins
----

Wir haben jetzt die notwendigen Dateien auf dem Oracle-Host und in `$MK_LIBDIR` steht, wo sich das Plugin-Verzeichnis befindet.
Installieren wird dann doch endlich `mk-oracle` auf dem Host:

[{shell}]
----
{c-root} install -D -m 755 /tmp/mk-oracle "$MK_LIBDIR/packages/mk-oracle/mk-oracle"
{c-root} install -m 755 /tmp/oracle_unified_sync "$MK_LIBDIR/oracle_unified_sync"
{c-root} install -D -m 755 /tmp/oracle_unified_sync "$MK_LIBDIR/600/oracle_unified_async"
----

In obigem Beispiel wurde die Datei `oracle_unified_async` in das Unterverzeichnis `600` installiert.
Das bedeutet, dass alle Sektionen, die Sie später für die asynchrone Abholung einrichten, ein maximales Cache-Alter von 600 Sekunden bekommen.
Passen Sie die Zahl 600 also bitte Ihren Bedürfnissen an.
--

[#tab_install_windows]#Windows#::
+
--
ifdef::onprem[]
Zusätzlich zum Agentenplugin `mk-oracle` benötigen Sie noch die Dateien `oracle_unified_sync.ps1` und `oracle_unified_async.ps1`.
In den kommerziellen Editionen gelangen Sie im [.guihint]#Setup#-Menü über [.guihint]#Agents > Windows, Linux, Solaris, AIX# zunächst in die xref:wato_monitoringagents#bakery[Agentenbäckerei], wo Sie die gebackenen Pakete finden.
endif::[]
ifdef::saas[]
Zusätzlich zum Agenten benötigen Sie noch das Agentenplugin `mk-oracle`.
In {CE} gelangen Sie im [.guihint]#Setup#-Menü über [.guihint]#Agents > Windows, Linux, Solaris, AIX# zunächst in die xref:wato_monitoringagents#bakery[Agentenbäckerei], wo Sie die gebackenen Pakete finden.
endif::[]
Von dort aus kommen Sie mit dem Menüeintrag [.guihint]#Related > Other operating systems# zur Liste der Agentendateien.

Die eigentliche Installation des Agentenplugins läuft etwas anders ab, als Sie es womöglich von anderen Plugins kennen.

Die mitgelieferten Plugins für Windows finden Sie alle auf dem überwachten Host im Installationsverzeichnis des Agenten unter `C:\Program Files (x86)\checkmk\service\plugins`.
Sie werden dort abgelegt, damit sie auch direkt zur Verfügung stehen.
ifdef::onprem[]
Alternativ liegen die Plugins auch auf dem {CMK}-Server unter `~/share/check_mk/agents/windows/plugins`, die beiden Hilfsprogramme unter `~/version/lib/python3/cmk/plugins/oracle/agents/`.
endif::[]

Kopieren Sie die drei Dateien von dort aus in die folgenden Verzeichnisse: 

[cols="2,8",options="header"]
|===
|Dateiname |Zielverzeichnis 
|`mk-oracle` |`%ProgramData%\checkmk\agent\plugins\packages\mk-oracle\`
|`oracle_unified_sync.ps1` |`%ProgramData%\checkmk\agent\plugins\`
|`oracle_unified_async.ps1` |`%ProgramData%\checkmk\agent\plugins\`
|===

Sobald Sie Dateien an Ort und Stelle sind, müssen Sie der Datei, die für die Ausführung der asynchronen Sektionen zuständig ist – also `oracle_unified_async.ps1` –, noch eine entsprechende Konfiguration mitgeben.

Wie das geht erklären wir im Kapitel xref:agent_windows#customizeexecution[Ausführung eines speziellen Plugins anpassen] im Handbuchartikel zur Überwachung von Windows.
--
====

Die Installation des gesamten Agentenplugins ist hiermit abgeschlossen.
Damit das Agentenplugin jetzt auch versteht, was es tun soll, müssen Sie noch eine xref:create_config[Konfiguration für das Plugin erstellen].

[#create_config]
==== Konfiguration für Agentenplugin erstellen

Im xref:glossar#dir_conf[Konfigurationsverzeichnis] des {CMK}-Agenten müssen Sie jetzt eine Datei mit dem Namen `mk-oracle.yml` anlegen.

[.tabs]
====
[#tab_configure_linux]#Linux#::
+
--
Wie das Konfigurationsverzeichnis auf Ihrem Host lautet finden Sie leicht mit dem folgenden Befehl heraus:

[{shell}]
----
{c-local} sudo sudo cmk-agent-ctl dump | grep "^AgentDirectory" | head -1
AgentDirectory: /etc/check_mk
----

Legen Sie in diesem Verzeichnis mit Ihrem bevorzugten Editor die Datei `mk-oracle.yml` an.
--

[#tab_configure_windows]#Windows#::
+
--
Legen Sie im Verzeichnis `%PROGRAMDATA%\checkmk\agent\config` mit Ihrem bevorzugten Editor die Datei `mk-oracle.yml` an.
--
====

Um nicht bei Null starten zu müssen, gibt es im Kapitel xref:sample_configurations[Beispielkonfigurationen] etwas, womit Sie erstmal starten können.

Eine ganz einfache Konfiguration für eine frisch installierte Oracle AI Database Free, die Sie womöglich zu Testzwecken aufgesetzt haben, könnte etwa so aussehen:

.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    authentication:
      password: mypassword
      type: standard
      username: mymonitoringuser
    cache_age: 600
    connection:
      hostname: localhost
    custom_metrics_cache_age: 600
    instances: []
----

Mit diesem Beispiel könnten Sie also erstmal starten.
Im Referenzteil dieses Artikel erklären wir alle weiteren xref:configuration_options[Konfigurationsoptionen] aus denen Sie sich bedienen können.

Sobald das Agentenplugin dann auch über eine Konfiguration verfügt, können Sie bereits auf dem {CMK}-Server eine Serviceerkennung auf dem jeweiligen Host durchführen.
Damit ist die grundlegende Einrichtung des Monitoring abgeschlossen.
Wenn Ihnen Sie Services, die das Agentenplugin von selbst mitbringt nicht genügen, können Sie gleich mit den xref:setup_custom_sqls[Benutzerdefinierte SQL-Abfragen] weitermachen.

[#setup_custom_sqls]
== Benutzerdefinierte SQL-Abfragen (Custom SQLs) einrichten

Benutzerdefinierte SQL-Abfragen (Custom SQLs) können nicht über die Regel [.guihint]#Unified Oracle Plugin (Beta)# definiert werden.
Sie müssen die notwendige Konfiguration direkt auf dem Host vornehmen, von dem aus Sie Ihre Oracle-Instanzen und -Datenbanken überwachen.

[#custom_sqls_why]
=== Warum benutzerdefinierte SQL-Abfragen?

{CMK} bietet mit dem Agentenplugin bereits eine große Menge an SQL-Abfragen, mit denen Sie Ihre Datenbankinstanzen überwachen können.
Damit diese für eine möglichst große Menge an technischen und inhaltlichen Anforderungen passend sind, sind diese allgemein gehalten.

Um die individuellen Anforderungen eines jeden Unternehmens an die Überwachung einer konkreten Datenbank erfüllen zu können, bietet {CMK} die Möglichkeit, eigene, benutzerdefinierte SQL-Abfragen (_Custom SQLs_) zu erstellen und mit dem Agentenplugin abfragen zu lassen.
Diese werden dann in {CMK} automatisch als Services erkannt und überwacht.

Um dem Agentenplugin Ihre Abfragen zu übergeben, haben Sie zwei Möglichkeiten:

* Entweder schreiben Sie die Abfragen direkt in die Konfigurationsdatei `mk-oracle.yml`.
* Oder Sie geben in der Konfigurationsdatei stattdessen an, wo das Agentenplugin Dateien finden kann, in denen Ihre benutzerdefinierte SQL-Abfragen gespeichert sind.

Letzteres erleichtert die Wartbarkeit und auch eine Versionskontrolle wird erleichtert, wenn Inhalte in der Konfigurationsdatei des Plugins nicht vermischt werden.
Wenn es allerdings ohnehin nur um eine kleine Anzahl an Abfragen geht, können Sie sich den zusätzlichen Aufwand gegebenenfalls auch sparen.

Wir zeigen zunächst ein einfaches Beispiel, welches direkt in die Konfigurationsdatei des Plugins geschrieben wird.

[#custom_sqls_simple]
=== Einfache benutzerdefinierte SQL-Abfragen einrichten

Den Anfang macht eine Abfrage, welche bereits echte Daten aus einer Oracle-Instanz zurückliefert.
Das Beispiel soll nur verdeutlichen, wie einfach es sein *kann*, benutzerdefinierte SQL-Abfragen einzubinden.
Wie sinnvoll exakt diese Abfrage im Arbeitsalltag ist, sei dahingestellt.

Tasten Sie sich an die später produktiv verwendbare Abfrage heran, indem Sie Ihre Ideen für SQL-Abfragen zunächst als `sysdba` ausführen.
Damit möglicherweise lange laufende Abfragen kein Produktivsystem ausbremsen, empfehlen wir die Verwendung einer Test-Instanz.

[{shell}]
----
oracle@linux$ sqlplus / as sysdba
----

[{sql}]
----
sqlplus> SELECT 'details:Free memory: ' || ROUND(SUM(bytes)/1024/1024, 2) || ' MB' FROM v$sgastat WHERE name = 'free memory';
'DETAILS:FREEMEMORY:'||ROUND(SUM(BYTES)/1024/1024,2)||'MB'
----------------------------------------------------------------
details:Free memory: 230,86 MB
----

Den Kopf dieser Ausgabe können Sie -- genauso wie es das Agentenplugin tut -- ignorieren.
Wichtig ist das in der Abfrage verwendete Schlüsselwort `details`.
Die Zeile, die so beginnt, wird sich das Check-Plugin auf dem {CMK}-Server schnappen und daraus die [.guihint]#Summary# des Services erzeugen.
Aber woher weiß {CMK} denn, wie der Service eigentlich heißen soll?
Den Namen des Service legen Sie fest, wenn wir die SQL-Abfrage in die Konfiguration für das Agentenplugin übertragen:

.mk-oracle.yml
[{yaml},highlight=10..12]
----
---
oracle:
  main:
    connection:
      hostname: localhost
    authentication:
      username: mymonitoringuser
      password: mypassword
      type: standard
    custom_metrics:
      - My service: 
          sql: "SELECT 'details:Free memory: ' || ROUND(SUM(bytes)/1024/1024, 2) || ' MB' FROM v$sgastat WHERE name = 'free memory'"
----

Unser Beispiel zeigt drei Dinge:

* Um benutzerdefinierte SQL-Abfragen einzufügen, benötigen Sie immer einen Schlüssel namens `custom_metrics`.
* Darauf folgt eine Liste (hier: `My service`).
Eine solche Liste beginnt in YAML mit einem `-`.
Der Name dieser Liste wird in {CMK} Teil des Service-Namens und kann frei gewählt werden.
* Dann kommt die Zeile, die die Abfrage enthält.
Sie muss mit `sql:` beginnen und die Abfrage selbst muss in doppelten Anführungsstrichen stehen.
Achten Sie hier auch darauf, dass die Abfrage *nicht* mit einem Semikolon abgeschlossen werden darf.

Nachdem Sie diese Zeile auf Ihrem Oracle-Host in die Konfigurationsdatei des Agentenplugins eingefügt haben, können Sie schon wieder zurück zu Ihrem {CMK}-Server gehen.
Führen Sie dort für den Oracle-Host eine Serviceerkennung durch.
Sie erhalten dann diesen neuen Service:

image::monitoring_oracle_custom_sql_discovery.png[]

Der Service-Name setzt sich dabei aus mehreren Komponenten zusammen:

* Der Name beginnt immer mit [.guihint]#ORA#.
* Es folgt die SID (hier: [.guihint]#FREE#).
* Das anschließende [.guihint]#SQL# ist der Hinweis darauf, dass dieser Service durch eine benutzerdefinierte SQL-Abfrage entstanden ist.
* Darauf folgt der String, den Sie in der Konfigurationsdatei frei wählen konnten in Großbuchstaben.

In der Summary sehen Sie dann bereits Daten, die aus dem Ergebnis der SQL-Abfrage stammen.

Es wird sicherlich Fälle geben, in denen eine solche einfache Abfrage bereits das ist, was Sie brauchen.
In diesem Fall ist der Service aber wahrscheinlich noch wenig hilfreich.
Deshalb zeigen wir Ihnen im Kapitel xref:custom_sqls_advanced[Erweiterte benutzerdefinierte SQL-Abfragen], wie Sie mit wenigen Handgriffen einen umfangreicheren Service erstellen können.


[#custom_sqls_advanced]
=== Erweiterte benutzerdefinierte SQL-Abfragen einrichten

Ausgehend von dem Beispiel im Kapitel xref:custom_sqls_simple[Einfache benutzerdefinierte SQL-Abfragen], zeigen wir Ihnen jetzt, wie Sie mit wenigen Handgriffen einen Service mit einer Metrik erhalten.

Hierfür bietet es sich an, die SQL-Abfrage aus der Konfigurationsdatei den Agentenplugins auszulagern.
Stattdessen geben Sie hinter dem Schlüssel `custom_metrics` eine Datei an, in der das Agentenplugin Ihre SQL-Abfragen finden kann.

.mk-oracle.yml
[{yaml},highlight=10..12]
----
---
oracle:
  main:
    connection:
      hostname: localhost
    authentication:
      username: mymonitoringuser
      password: mypassword
      type: standard
    custom_metrics:
      - My advanced service:
          path: 'myadvancedservice.sql'
----

Wenn Sie die Datei ohne Angabe eines absoluten Pfades eintragen, durchsucht das Agentenplugin in einer bestimmten Reihenfolge einige Verzeichnisse.
Welche das sind und was dabei genau passiert, erklären wir im xref:reference[Referenzteil] dieses Artikels.

Legen Sie jetzt im xref:glossar#dir_conf[Konfigurationsverzeichnis] des {CMK}-Agenten ein Unterverzeichnis namens `orasql` an und erstellen Sie darin eine Datei mit der Endung `.sql`, wie Sie sie hinter `path:` angegeben haben.

In diesem Beispiel gehen wir davon aus, dass das Konfigurationsverzeichnis auf einem Linux-Host `/etc/check_mk/` heißt.
Erzeugen Sie zunächst das unbedingt notwendige Unterverzeichnis `orasql`.

[{shell}]
----
{c-root} mkdir /etc/check_mk/orasql
----

Erstellen Sie darin im Anschluss die Datei `myadvancedservice.sql`.
Darin können Sie jetzt deutlich bequemer auch mehrzeilige SQL-Abfragen unterbringen.
Wichtig bleibt dabei, dass die Schlüsselwörter für das Check-Plugin vorkommen.

./etc/check_mk/orasql/myadvancedservice.sql
[{sql}]
----
SELECT 'details:Free memory: ' || REPLACE(ROUND(SUM(bytes) / 1024 / 1024, 2), ',', '.') || ' MB' FROM v$sgastat WHERE name = 'free memory'
UNION ALL
SELECT 'perfdata:free_ram_mb=' || REPLACE(ROUND(SUM(bytes) / 1024 / 1024, 2), ',', '.') FROM v$sgastat WHERE name = 'free memory'
UNION ALL
SELECT 'exit:0' FROM dual
----

Zusätzlich zu der ersten Zeile, die Sie schon aus dem einfachen Beispiel kennen, gibt das SQL-Skript nun eine Zeile aus, die mit `perfdata` beginnt.
Daraus wird {CMK} eine Metrik erzeugen und automatisch einen Graphen anlegen.
Mit dem Schlüsselwort `exit` können Sie außerdem den Status des Service beeinflussen.
In diesem Beispiel wird er fix auf `0` gesetzt, was für den Status {OK} steht.
Die Schlüsselwörter benötigt das Check-Plugin auf dem {CMK}-Server, um aus den angelieferten Daten Services und Metriken erzeugen zu können.
Die vier Schlüsselwörter `details`, `perfdata`, `long` und `exit` erklären wir im Kapitel xref:format_and_prefixes[Formatierung der Ausgabe und Schlüsselwörter] genau.

Wenn Sie das alles so angelegt haben und erneut eine Serviceerkennung durchführen, wird der alte Service verschwinden und stattdessen ein neuer, dessen Name auf [.guihint]#MY ADVANCED SERVICE# endet, gefunden werden.

image::monitoring_oracle_custom_sql_advanced.png[]

[#migrate]
== Migration von `mk_oracle` zu `mk-oracle`

[#migrate_configuration]
=== Migration der Konfiguration des Agentenplugins

In {CMK} {v25} hat das neue Agentenplugin `mk-oracle` eine Fähigkeit, die Nutzern von `mk_oracle` dabei hilft auf das neue Agentenplugin umzusteigen.
Übertragen Sie dazu `mk-oracle` auf Ihren Oracle-Host und führen Sie das Agentenplugin mit der Option `-M`, gefolgt vom Namen einer Konfigurationsdatei im alten Format, aus.
Das Agentenplugin liest dann die Konfiguration für das alte `mk_oracle` ein und gibt sie in dem Format aus, die das `mk-oracle` erwartet.

Das kann dann beispielsweise so aussehen:

[{shell}]
----
{c-root} /usr/lib/check_mk_agent/plugins/packages/mk-oracle/mk-oracle -M /etc/check_mk/mk_oracle.cfg
# --- Converted from /etc/check_mk/mk_oracle.cfg at 2026-06-29 19:00:00 UTC ---
# # Syntax
# # DBUSER='USERNAME:PASSWORD:ROLE:HOST:PORT:TNSALIAS'
# DBUSER='checkmk:myPassword'
# 
# DBUSER_MYINST1='cmk_specific1:myPassword1:SYSDBA:localhost:1521'
# DBUSER_MYINST2='cmk_specific2:myPassword2::localhost::INST2'
# 
# ASMUSER='cmk_asm:myASMPassword:SYSASM'
# --- Known environment variables defined in legacy config ---
# DBUSER_MYINST2 cmk_specific2:myPassword2::localhost::INST2
# ASMUSER cmk_asm:myASMPassword:SYSASM
# DBUSER checkmk:myPassword
# DBUSER_MYINST1 cmk_specific1:myPassword1:SYSDBA:localhost:1521
# --- Unified Config ---
---
oracle:
  main:
    connection:
      hostname: localhost
    authentication:
      username: "checkmk"
      password: "myPassword"
      type: standard
      asm_username: "cmk_asm"
      asm_password: "myASMPassword"
      asm_role: sysasm
    instances:
      - sid: $ORACLE_SID
        alias: $ORACLE_SID
      - sid: MYINST2
        alias: INST2
        connection:
          hostname: localhost
        authentication:
          username: "cmk_specific2"
          password: "myPassword2"
          type: standard
      - sid: MYINST1
        connection:
          hostname: localhost
          port: 1521
        authentication:
          username: "cmk_specific1"
          password: "myPassword1"
          type: standard
          role: sysdba
----

Standardmäßig schreibt `mk-oracle` die migrierte Konfiguration auf die Standardausgabe.
Wir empfehlen das auch unbedingt im ersten Schritt, damit Sie die Ausgabe erst einmal prüfen können.
Der Output enthält zuerst Ihre alte Konfiguration als Kommentar.
Zu Referenzzwecken bietet es sich an, diesen Kommentar eine Weile vorzuhalten.

Wenn alles passt, können Sie `mk-oracle` mit der Option `--migrate-output` dazu anweisen, den Output direkt in eine Datei zu schreiben.

[{shell}]
----
{c-root} /usr/lib/check_mk_agent/plugins/packages/mk-oracle/mk-oracle \
    -M /etc/check_mk/mk_oracle.cfg \
    --migrate-output /etc/check_mk/mk-oracle.yml
----

Sollte der Migrationsbefehl Teile Ihrer alten Konfiguration nicht migrieren können, so wird dies als `#WARNING` klar in der Ausgabe angezeigt.

Der folgenden Tabelle können Sie entnehmen, welche Variablen der Konfiguration von `mk_oracle` auf welche Weise übersetzt werden:

[cols="3,7",options="header"]
|===
|Alte Variable... | ...wird migriert zu:
| `DBUSER` (required)                        | Top-level `connection:` (Host-Name, Port) und `authentication:`, zusätzlich den ersten Eintrag bei `instances:`
| `DBUSER_<SID>`                             | Ein Eintrag `instances:` für jede `SID` mit Einträgen für `connection:` und `authentication:` pro Instanz
| `ASMUSER`                                  | `asm_username`, `asm_password`, `asm_role` unterhalb von `authentication:`
| `REMOTE_INSTANCE_<ID>`                     | Ein Eintrag `instances:` pro ID inklusive des `piggyback_host:` (nur Linux/AIX)
| `SYNC_SECTIONS` / `ASYNC_SECTIONS`         | `sections:` mit Attribut `is_async: false` / `true`
| `SYNC_ASM_SECTIONS` / `ASYNC_ASM_SECTIONS` | `sections:` mit Attribut `affinity: "asm"` (`"all"` )
| `CACHE_MAXAGE`                             | `cache_age:`
| `SQLS_MAX_CACHE_AGE`                       | `custom_metrics_cache_age:`
| `MAX_TASKS`                                | `options.threads:` (Nur für Werte ≥ 2, maximal jedoch 8)
| `ONLY_SIDS`                                | `discovery.include:` (mit `detect: true`)
| `SKIP_SIDS`, `EXCLUDE_<SID>="ALL"`         | `discovery.exclude:` (mit `detect: true`)
| `TNS_ADMIN`                                | `connection.tns_admin:`
| `OLRLOC`                                   | `connection.oracle_local_registry:`
| `SQLS_SECTIONS` + pro Sektion `SQLS_*`     | `custom_metrics:`
|===

Die Einträge in der folgenden Tabelle werden nicht automatisch migriert und müssen von Ihnen händisch an das neue Plugin angepasst werden:

[cols="3,7",options="header"]
|===
| alte Variable | Hinweis zur manuellen Migration
| `SQLS_DBUSER`, `SQLS_DBPASSWORD`, `SQLS_DBSYSCONNECT` | Anmeldedaten pro benutzerdefiniertem SQL; verwenden Sie stattdessen instanzspezifische Überschreibungen unterhalb des Schlüssels `authentication:`
| `SQLS_PARAMETERS`                                     | Die Übergabe von SQL*Plus-Parametern wird nicht unterstützt.
| `EXCLUDE_<SID>="<section> ..."`                       | Ausschluss einzelner Abschnitte pro SID; nur `EXCLUDE_<SID>="ALL"` wird konvertiert
| `ORACLE_HOME`, `REMOTE_ORACLE_HOME`                   | Die OIC-Laufzeitumgebung -- gegebenenfalls `use_host_client` angeben.
| `ID_BY`                                               | Wählt `SID=` gegenüber `SERVICE_NAME=` in der alten Verbindungskonfiguration aus; verwenden Sie stattdessen die Instanzfelder `sid:` / `service_name:`
|===

[#migrate_sql_queries]
=== Migration von benutzerdefinierten SQL-Abfragen

Das alte Agentenplugin `mk_oracle` leitet benutzerdefinierte SQL-Abfragen über das Kommandozeilen-Tool `sqlplus` weiter.
Das Plugin `mk-oracle` hingegen führt SQL-Abfragen hingegen direkt über den Treiber des Oracle Instant Clients aus.
Aus diesem Wechsel des Ausführungsmodells ergeben sich zwei wesentliche Konsequenzen für bestehende SQL-Abfragen.

==== Grundlegende Einschränkungen und Ausführungsmodell

===== Keine SQL*Plus-Befehle

* Befehle wie PROMPT, SET, COLUMN, SPOOL, EXEC/EXECUTE oder VAR/VARIABLE sind Funktionen des Client-Werkzeugs `sqlplus` und kein Bestandteil der Datenbanksprache SQL.
Sie funktionieren unter dem OIC-Treiber nicht.
* Keine anonymen PL/SQL-Blöcke: Anonyme Blöcke im Format `DECLARE/BEGIN {...} END;` können nicht direkt als Skript ausgeführt werden.
Es werden ausschließlich reine SQL-Statements unterstützt.

Das Agentenplugin im Migrationsmodus überprüft jede referenzierte SQL-Datei und gibt bei Abweichungen eine Warnung im Terminal sowie in der generierten Konfigurationsdatei aus.
Die betroffenen Sektionen werden zwar migriert, müssen jedoch von Ihnen manuell angepasst werden, da die Abfragen sonst zur Laufzeit fehlschlagen werden.

===== Eigenschaften des neuen Ausführungsmodells

* Mehrere Statements: Eine SQL-Datei kann mehrere, durch Semikolon getrennte SQL-Statements auf oberster Ebene enthalten.
Diese werden der Reihe nach ausgeführt und ihre Ergebnisse verkettet.
* Jedes zurückgegebene Ergebnis muss den Vorgaben zur xref:format_and_prefixes[Formatierung der Ausgabe und Schlüsselwörter] entsprechen.

==== Entfernen von SQL*Plus-Befehlen

Formatierungsbefehle und interaktive Befehle aus `sqlplus` besitzen kein Äquivalent im Treiber des Oracle Instant Clients.
Sie müssen diese daher ersatzlos löschen.
Das Agentenplugin gibt jede zurückgegebene Datenzeile direkt aus.

Als Nächstes zeigen wir zuerst eine SQL-Datei, wie sie für für `mk_oracle` noch funktioniert hat:

[{sql}]
----
SET PAGESIZE 0
SET FEEDBACK OFF
COLUMN details FORMAT A80
PROMPT collecting session count ...
SELECT 'details:' || COUNT(*) || ' sessions' FROM v$session;
----

Eine solche Datei müssen Sie für `mk-oracle` folgendermaßen umschreiben, damit sie mit OIC funktioniert:

[{sql}]
----
SELECT 'details:' || COUNT(*) || ' sessions' FROM v$session
----


==== Umwandlung von PL/SQL-Blöcken in reine SELECT-Abfragen

Anonyme PL/SQL-Blöcke lassen sich in den meisten Fällen durch Standard-SQL-Konstrukte ersetzen:
* PL/SQL-Variablen werden zu `WITH`-Klauseln (Common Table Expressions).
* `IF/ELSIF`-Bedingungen werden durch `CASE`-Anweisungen ersetzt.
* `DBMS_OUTPUT.PUT_LINE`-Ausgaben werden über `UNION ALL` oder separate, durch Semikolons getrennte Statements abgebildet.

Im folgenden Beispiel zeigen wir eine SQL-Datei, welche einen PL/SQL-Block enthält:

[{sql}]
----
SET SERVEROUTPUT ON
DECLARE
    invalid_count NUMBER;
BEGIN
    SELECT COUNT(*) INTO invalid_count
      FROM dba_objects
     WHERE status = 'INVALID';
    IF invalid_count > 10 THEN
        DBMS_OUTPUT.PUT_LINE('exit:2');
    ELSE
        DBMS_OUTPUT.PUT_LINE('exit:0');
    END IF;
    DBMS_OUTPUT.PUT_LINE('details:' || invalid_count || ' invalid objects');
END;
----

Eine solche Datei müssen Sie für `mk-oracle` in reines SQL umschreiben.
Das könnte beispielsweise so aussehen:

[{sql}]
----
WITH invalid AS (
    SELECT COUNT(*) AS cnt
      FROM dba_objects
     WHERE status = 'INVALID'
)
SELECT 'exit:' || CASE WHEN cnt > 10 THEN '2' ELSE '0' END FROM invalid
UNION ALL
SELECT 'details:' || cnt || ' invalid objects' FROM invalid
----

==== Auslagern komplexer PL/SQL-Logik in Stored Functions

Erfordert die Logik zwingend PL/SQL (z. B. bei Schleifen, komplexer Fehlerbehandlung oder temporären Zuständen), muss die Logik in die Datenbank selbst als Pipelined Function ausgelagert werden.

Dafür müssen Sie die Pipelined Function einmalig in Ihrer Datenbank einrichten:

[{sql}]
----
CREATE OR REPLACE FUNCTION checkmk_invalid_objects
    RETURN sys.odcivarchar2list PIPELINED
AS
    invalid_count NUMBER;
BEGIN
    SELECT COUNT(*) INTO invalid_count
      FROM dba_objects
     WHERE status = 'INVALID';

    -- Beliebige PL/SQL-Logik ist hier erlaubt
    PIPE ROW ('details:' || invalid_count || ' invalid objects');
    PIPE ROW ('perfdata:invalid_objects=' || invalid_count || ';10;100');
    PIPE ROW (CASE WHEN invalid_count > 10 THEN 'exit:2' ELSE 'exit:0' END);
    RETURN;
END;
/

GRANT EXECUTE ON checkmk_invalid_objects TO checkmk;
----

Sobald Sie diese Pipelined Function eingerichtet ist, können Sie durch das Agentenplugin aufrufen lassen:

[{sql}]
----
SELECT column_value FROM TABLE(checkmk_invalid_objects)
----

Liegt die Funktion in einem anderen Schema, muss der Schema-Name vorangestellt werden: `TABLE(owner.checkmk_invalid_objects)`.


[#dashboards_views]
== Dashboards und Ansichten

[#dashboards]
=== Oracle-Dashboards

ifdef::onprem[]
{cee-only}
Die kommerziellen Editionen von {CMK} werden
endif::[]
ifdef::saas[]
{CE} wird
endif::[]
mit einem eingebauten Dashboards für Oracle ausgeliefert.
Die Zahl der Dashboard für Oracle wird in den kommenden Wochen noch steigen.
Behalten Sie dazu gerne unsere link:https://checkmk.com/werks?search=oracle&product=cmk&cmk_version%5B%5D=2.5&level%5B%5D=1&level%5B%5D=2&level%5B%5D=3&class%5B%5D=feature&edition=all[Werks zum Thema Oracle im Blick^].

Der Einstieg geschieht dabei immer über das Dashboard [.guihint]#Oracle databases#, welches Sie über [.guihint]#Monitor > Applications > Oracle databases# erreichen.


[#reference]
== Referenzen

[#custom_sqls]
=== Benutzerdefinierte SQL-Abfragen (Custom SQLs)

Über den Konfigurationsschlüssel `custom_metrics` definieren Sie Ad-hoc-SQL-Abfragen, deren Ausgaben auf dem {CMK}-Server durch das Check-Plugin link:https://checkmk.com/integrations/oracle_sql[oracle_sql^] ausgewertet werden.
Jeder Eintrag wird durch einen Item-Namen identifiziert, der in {CMK} als Teil des Service-Namens angezeigt wird.

==== Grundlagen und Konfiguration

Die SQL-Abfragen können Sie entweder direkt als String übergeben oder aus einer Datei laden lassen.
In der folgenden Beispielkonfiguration finden Sie eine SQL-Abfrage im globalen Abschnitt, die durch den Schlüssel `oracle.main` bezeichnet wird.
Unterhalb des Schlüssels `oracle.main.instances` können Sie wiederum einen Schlüssel `custom_metrics` angeben, der dann natürlich nur für diese Instanzen ausgeführt wird.

.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    connection:
      hostname: localhost
    authentication:
      username: mymonitoringuser
      password: mypassword
      type: standard
    custom_metrics:
      - product_price: # Item-Name -> wird zum Service "<SID> SQL PRODUCT_PRICE"
          sql: "SELECT 'details:Price OK' FROM dual"
    instances:
      - service_name: FREE
        custom_metrics:
          - last_sessions: # Wird nur gegen diese spezifische Instanz ausgeführt
              sql: "SELECT 'details:per-instance' FROM dual"
----

* Eine Instanz führt immer global definierten und ihre eigenen instanzspezifischen SQL-Abfragen aus.
* Teilen sich ein globaler und ein instanzspezifischer Eintrag denselben Item-Namen, hat der Eintrag der Instanz immer Vorrang.

==== Externe SQL-Dateien (path:)

Anstatt SQL-Abfragen via `sql:` direkt in die Konfigurationsdatei des Agentenplugins zu schreiben, können Sie mit `path:` auf externe `.sql`-Dateien verweisen.
Dies funktioniert sowohl für `custom_metrics` als auch für vordefinierte Sektionen.

===== Varianten der Pfadangabe

.mk-oracle.yml
[{yaml}]
----
custom_metrics:
  # 1. Absoluter Pfad zu einer Datei
  - heavy_query:
      path: '/opt/checkmk/sql/heavy_query.sql'

  # 2. Relativer Pfad
  # Wird zuerst in MK_LIBDIR/... und dann in MK_CONFDIR/... gesucht
  - product_price:
      path: 'queries/product_price.sql'

  # 3. Verzeichnispfad
  # Der Dateiname wird automatisch aus dem Item-Namen abgeleitet (hier: "sessions_stats.sql")
  - sessions_stats:
      path: 'queries/'

  # 4. Datei mit Inline-Fallback
  # Kann die Datei nicht aufgelöst werden, wird das Inline-SQL verwendet
  - last_resort:
      path: 'queries/last_resort.sql'
      sql: "SELECT 'details:fallback' FROM dual"
----

==== Auflösungsregeln (Resolution rules)

Absolut vs. Relativ::
Absolute Pfade werden 1:1 übernommen.
Relative Pfade werden zuerst unter `MK_LIBDIR/plugins/packages/mk-oracle/orasql/` und anschließend unter `MK_CONFDIR/orasql/` gesucht.
Existiert die Datei in beiden Verzeichnissen, gewinnt die Datei im `MK_LIBDIR`.

Datei vs. Verzeichnis::
Ein Pfad kann auf eine spezifische Datei (mit oder ohne `.sql`-Endung) oder auf ein Verzeichnis verweisen.
Verweist er auf ein Verzeichnis, wird der Dateiname bei `custom_metrics` aus dem Item-Namen und bei vordefinierten Sektionen aus dem Sektionsnamen abgeleitet.

Versionsabhängige Varianten::
Neben der Basisdatei (`<name>.sql`) können Sie Oracle-spezifische Varianten im Format `<name>@<min_version>.sql` bereitstellen (z.B. `sessions@12010000.sql`).
Das Plugin wählt automatisch die Datei mit der höchsten `min_version`, die kleiner oder gleich der Version der verbundenen Oracle-Instanz ist.
Als Versionsformat wird ein 8-stelliges numerisches Format genutzt (`MMmmRRSSSS`` für Major / Minor / Release / Patch).
Beispiel: 12.1.0.2 → 12010002.

Fallback-Kette::
Die Reihenfolge zur Ermittlung der SQL-Abfrage lautet: `path:` > (Inline) `sql:` > mitgelieferter Standard-Code (nur bei vordefinierten Sektionen).
Schlägt `path:` fehl und es ist kein `sql:` definiert, generiert die Sektion keine Ausgabe.


==== SQL-Parameter

Sie können benannte Parameter definieren, die vor der Ausführung in den SQL-Code eingesetzt werden.
Jeder Platzhalter im Format `${<name>}` wird rein textuell durch den konfigurierten Wert ersetzt.
Dies funktioniert sowohl für Inline-SQL als auch für `.sql`-Dateien.

.mk-oracle.yml
[{yaml}]
----
---
custom_metrics:
  - test:
      sql: 'SELECT ${parameter_1} FROM dual; SELECT ${parameter_2} FROM dual'
      sql_params:
        parameter_1: 'value_1'
        parameter_2: '${ENV_VAR_1}'
----

* `${ENV_VAR_1}` wird dabei aus den Umgebungsvariablen aufgelöst.
* *Textuelle Ersetzung (keine Bind-Variablen)*: Alle Werte werden unverändert in das Statement eingefügt.
Sie können somit auch Spalten- oder Tabellennamen übergeben.
* *Umgebungsvariablen*: Ein Wert kann auf Umgebungsvariablen verweisen (Format `$VAR` oder `${VAR}`). Diese werden beim Einlesen der Konfiguration aufgelöst.
Ist eine referenzierte Variable nicht gesetzt, wird der Parameter mit einer Warnung übersprungen.
Der Platzhalter bleibt im SQL-Code stehen (was i. d. R. zu einem SQL-Fehler führt, statt eine Abfrage mit leeren Werten auszuführen).
* *Unbenutzte Parameter*: Platzhalter ohne passenden Parameterwert werden ignoriert und bleiben im Code unverändert stehen.

[#format_and_prefixes]
==== Formatierung der Ausgabe und Schlüsselwörter

Das Agentenplugin `mk-oracle` fügt selbstständig den Sektions-Header `pass:[<<<]<<oracle_sql:sep(58)pass:[>>>]` sowie den Subsektions-Header `[[[<SID>|<item>]]]` ein.
Das auszuführende SQL ist ausschließlich für den Body der Ausgabe verantwortlich.
Jede SQL-Abfrage muss Zeilen mit einer einzigen Zeichenfolgen-Spalte (String Column) zurückgeben.
Der Wert muss mit einem der folgenden Präfixe beginnen, damit das Checkmk-Plugin diesen korrekt interpretieren kann:

* `details`:
Hier können Sie bestimmen, was im [.guihint]#Summary# des erzeugten Service ausgegeben werden soll.
Die Zeile wird mit dem Schlüsselwort und einem Doppelpunkt eingeleitet.
Der Rest der Zeile ergibt die Ausgabe.

* `perfdata`:
Metriken werden mit diesem Schlüsselwort übergeben.
Innerhalb einer Zeile können Sie beliebig viele Metriken -- getrennt durch ein Leerzeichen -- erzeugen.
Sie können die Ausgabe der Metriken auch über mehrere Zeilen verteilen.
Beginnen Sie dabei einfach immer mit dem Schlüsselwort `perfdata:`.

* `long`:
Wenn der Service eine lange Ausgabe für das Feld [.guihint]#Details# haben soll, können Sie diese hier angeben.
Auch dieses Schlüsselwort können Sie mehrmals verwenden, um mehrere Zeilen in den [.guihint]#Details# zu erzeugen.

* `exit`:
Soll die Ausgabe einen bestimmten Status haben, können Sie diesen hier bestimmen.
Es stehen Ihnen dabei die bekannten Zuordnungen `0`, `1`, `2`, `3` für die Status {OK}, {WARN}, {CRIT}, {UNKNOWN} zur Verfügung.


[#configuration_options]
=== Konfigurationsoptionen

Im diesem Kapitel erklären wir alle Optionen, die zur Konfiguration des Agentenplugins existieren.
Egal ob Sie das Agentenplugin über den Regelsatz [.guihint]#Unified Oracle Plugin (Beta)# konfigurieren oder ob Sie es manuell konfigurieren.

Die Erklärungen hier im Handbuch gehen mitunter über den Inhalt der Inline-Hilfe hinaus, wenn es beispielsweise interessante Hintergrundinformationen zu einer Option gibt und eine längere Erklärung für bestimmte Anwendungsfälle angebracht sind.
Dazu geben wir jeweils auch an, welche Eintragung durch eine gewählte Option in der Konfigurationsdatei `mk-oracle.yml` entsteht.

Zusätzlich finden alle Interessierten die Beschreibung aller Optionen der `mk-oracle.yml` in unserer link:https://github.com/Checkmk/checkmk/tree/master/packages/mk-oracle#yaml-configuration[Beschreibung auf GitHub].

[#additional_options]
[discrete]
==== Additional Options > Maximum connections

Maximale Anzahl der zu öffnenden Datenbankverbindungen.

.Konfigurationsdatei (Beispiel)
[%collapsible]
====
[#max_connections]
.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    options:
      max_connections: <int>
----
====

[discrete]
==== Additional Options > Maximum queries

Maximale Anzahl der Abfragen, die pro Verbindung ausgeführt werden dürfen.

.Konfigurationsdatei (Beispiel)
[%collapsible]
====
[#max_queries]
.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    options:
      max_queries: <int>
----
====


[discrete]
==== Additional Options > Ignore database name

.Konfigurationsdatei (Beispiel)
[%collapsible]
====
[#ignore_db_name]
.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    options:
      IGNORE_DB_NAME: "0|1"
----
====


[discrete]
==== Additional Options > Oracle Instant Client options

.Konfigurationsdatei (Beispiel)
[%collapsible]
====
[#use_host_client]
.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    options:
      use_host_client: "auto|never"
----
====


[discrete]
==== Authentication > Authentication type > Oracle Wallet

Oracle Wallet bietet eine sichere Möglichkeit, sich bei Oracle-Datenbanken zu authentifizieren, ohne Passwörter im Klartext in Konfigurationsdateien zu speichern.

.Konfigurationsdatei (Beispiel)
[%collapsible]
====
[#authentication_type_wallet]
.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    authentication:
      type: wallet
----
====


[discrete]
==== Authentication > Type

Hier können Sie zwischen `standard` und `wallet` wählen.
Wählen Sie `wallet` sind keine weiteren Einstellungen an dieser Stelle nötig.
`standard` steht an dieser Stelle für die Authentifizierung per Benutzername und Passwort.
Die Angabe von `username` und `password` ist dann auch zwingend notwendig.

.Konfigurationsdatei (Beispiel)
[%collapsible]
====
[#authentication_type]
.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    authentication:
      type: "standard|wallet"
----
====


[discrete]
==== Authentication > Authentication type > User & password

.Konfigurationsdatei (Beispiel)
[%collapsible]
====
[#authentication_user]
.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    authentication:
      username: 'myuser'
      password: 'mypassword'
      type: 'standard'      
----
====


[discrete]
==== Authentication > Role

Haben Sie sich für die Authentifizierung per Benutzername und Passwort entschieden, können Sie optional die Rolle des Benutzers angeben.

.Konfigurationsdatei (Beispiel)
[%collapsible]
====
[#authentication_role]
.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    authentication:
      username: 'myuser'
      password: 'mypassword'
      type: 'standard' 
      role: "sysdba|sysoper|sysasm|szsbackup|sysdg|syskm"
----
====


[discrete]
==== Connection options

In dieser Option legen Sie die Verbindungsparameter auf Netzwerkebene fest, die für alle Instanzen gelten, sofern sie nicht in den datenbankspezifischen Einstellungen überschrieben werden.

.Konfigurationsdatei (Beispiel)
[%collapsible]
====
[#connection_options]
.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    connection:
       hostname: 'myhostname' # optional, Standard: 'localhost'
       port: 1521 # optional, default: 1521
       timeout: 5 # optional, default: 5 (Sekunden)
       tns_admin: '/path/to/oracle/config/files/' # optional, Standard: MK_CONFDIR
       oracle_local_registry: '/etc/oracle/olr.loc' # optional, Pfad zur Oracle Local Registry
----
====


[discrete]
==== Cache age

Mit der Option [.guihint]#Cache age# können Sie die Gültigkeitsdauer des Caches von Sektion bestimmen, welche Sie asynchron überwachen.

.Konfigurationsdatei (Beispiel)
[%collapsible]
====
[#cache_age]
.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    cache_age: <int> # Standard: 600 (Sekunden)
----
====


[discrete]
==== Custom Metrics cache age

.Konfigurationsdatei (Beispiel)
[%collapsible]
====
[#custom_metrics_cache_age]
.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    custom_metrics_cache_age: <int> # Standard: 600 (Sekunden)
----
====

[discrete]
==== Instance discovery

Über die Optionen unter [.guihint]#Instance discovery# können Sie genau steuern, welche Instanzen Sie mit {CMK} überwachen möchten und welche Sie gegebenenfalls von der Überwachung ausschließen möchten.
Die Option auch nur dann eine Funktion, wenn sich das Agentenplugin auf dem selben Host, wie die Instanzen befinden.

.Konfigurationsdatei (Beispiel)
[%collapsible]
====
[#instance_discovery]
.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    discovery:
      detect: yes # So schalten Sie die automatische Instanzerkennung ein
      include: ['PROD', 'DEV'] # optional, es werden nur die hier angegebenen Instanzen überwacht
      exclude: ['TEST'] # optional, die hier angegebenen Instanzen werden ignoriert
----
====


[discrete]
==== Sections -- data to collect

Mit dieser Option können Sie zielgenau festlegen, welche Daten über Ihre Instanzen und Datenbanken überhaupt durch das Agentenplugin abgefragt werden sollen.
Zusätzlich können Sie pro Sektion festlegen, ob die Daten synchron oder xref:agent_linux#async_plugins[asynchron] abgefragt werden sollen.
Die Angaben zu `is_async` im folgenden Beispiel zeigen unsere Standardeinstellungen.

.Konfigurationsdatei (Beispiel)
[%collapsible]
====
[#sections]
.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    sections:
    - instance:
        is_async: false
    - asm_instance:
        is_async: false
    - asm_diskgroup:
        is_async: true
    - dataguard_stats:
        is_async: false
    - locks:
        is_async: false
    - logswitches:
        is_async: false
    - longactivesessions:
        is_async: false
    - performance:
        is_async: false
    - processes:
        is_async: false
    - recovery_area:
        is_async: false
    - recovery_status:
        is_async: false
    - sessions:
        is_async: false
    - systemparameter:
        is_async: false
    - undostat:
        is_async: false
    - iostats:
        is_async: true
    - jobs:
        is_async: true
    - resumable:
        is_async: true
    - rman:
        is_async: true
    - tablespaces:
        is_async: true
----
====

[discrete]
==== Databases to monitor

Wenn einzelne oder gar alle Oracle-Instanzen auf einem Host unterschiedliche Login-Daten oder Verbindungseinstellungen aufweisen, dann müssen Sie diese im Abschnitt [.guihint]#Databases to monitor# einzeln konfigurieren.
Um die einzelnen Datenbanken zu identifizieren, habe Sie die Auswahl aus [.guihint]#SID#, [.guihint]#Alias#, [.guihint]#Service Name#.
Konsultieren Sie an dieser Stelle die Inline-Hilfe in der Regel, um mehr über die drei Optionen zu erfahren.
In unserer link:https://github.com/Checkmk/checkmk/tree/master/packages/mk-oracle#instances[Beschreibung auf GitHub^] finden Sie außerdem einige weitere Konfigurationsbeispiele.

.Konfigurationsdatei (Beispiel)
[%collapsible]
====
.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    instances:
      - service_name: MYSERVICE
      - sid: MYSID
      - alias: MYALIAS
----
====


[#sample_configurations]
=== Beispielkonfigurationen

Die folgenden Beispielkonfigurationen können Sie als Ausgangspunkte für die xref:create_config[manuelle Einrichtung] Ihres Oracle-Monitorings verwenden.

[#minimal_configuration]
==== Minimale Konfiguration 

.mk-oracle.yml
[{yaml}]
----
---
oracle:
  main:
    authentication:
      password: mypassword
      type: standard
      username: mymonitoringuser
    cache_age: 600
    connection:
      hostname: localhost
    custom_metrics_cache_age: 600
    instances: []
----

==== (Fast) vollständiges Beispiel

Seit der Erstellung dieses Beispiels sind noch Optionen hinzugekommen.
Das Beispiel wird demnächst ergänzt.


.mk-oracle.yml
[{yaml}]
----
system:
  logging:
    level: 'warn'
    max_size: 1000000
    max_count: 5

oracle:
  main:
    options:
      max_connections: 6
      use_host_client: never
      IGNORE_DB_NAME: 0
    connection:
      hostname: 'localhost'
      port: 1521
      timeout: 5
      tns_admin: '/etc/check_mk'
    authentication:
      username: 'mymonitoringuser'
      password: 'mypassword'
      role: 'sysdba'
      type: 'standard'
    discovery:
      detect: yes
      include: ['PROD', 'DEV']
      exclude: ['TEST']
    instances:
      - service_name: 'ORCL'
        sid: 'ORCL'
      - alias: 'REMOTE_DB'
    sections:
      - instance:
          affinity: 'db'
      - tablespaces:
          is_async: yes
      - performance:
      - sessions:
    cache_age: 600
    custom_metrics_cache_age: 600
    piggyback_host: 'mypiggybackhost'
----
////
// end translation
