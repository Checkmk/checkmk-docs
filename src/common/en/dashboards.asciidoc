// -*- coding: utf-8 -*-
// IGNORE Filesystem alerters pre % Guihint: $HOSTNAME$ ve
// NONASCII
include::global_attr.adoc[]
= Dashboards
:title: Dashboards
:description: {CMK} dashboard show important information about monitored objects at a glance. Read here how to create and use dashboards.
:experimental:

{related-start}
xref:views#[Host and service views]
xref:graphing#[Measured values and graphing]
{related-end}


[#intro]
== Introduction

=== What are dashboards actually?

Dashboards are the central displays for your monitoring in {CMK}.
They provide you with both overviews and detailed insights into specific areas.
For example, you can visualize the general status of entire network segments, but also simply list which services are generating a load or overload of certain system resources.
{CMK} comes with some standard dashboards, such as for problems, {CMK} server statistics and of course a general overview.
However, you can also create your own completely individual dashboards from scratch.

In this article you will learn which tools are available for this and how exactly you can use them to create your own overviews.
First, we will show you how xref:features[dashboards] work and how to work with them.
Afterwards, we will shed light on the basics of xref:layout[Layout] and xref:dashboard_config[Configuration], in order to build on this and step by step create a complete, simple xref:example[example dashboard].
Then follows a short summary on the topic of xref:filter[filters], as these can be set in a number of different locations.
It continues with a presentation of all of those already built-in xref:builtins[dashboards and widgets] -- the individual building blocks for overviews.
Finally, there are tips on how to deal with errors and problems.

By the way, you can see the most striking dashboard directly on the {CMK} start page, but dashboards can be treated like other views and easily accessed via the xref:user_interface#navigation_bar[navigation bar] and the xref:user_interface#sidebar[sidebar].

Dashboards are, of course, ideally suited to be placed separately on individual monitors, be it for a large control room, as an information display for server rooms or as a simple kiosk display for the conference room.


[#features]
=== What dashboards can do

What sets {CMK} dashboards apart is our very own _anchored layout:_
Using a smart algorithm, dashboards automatically adjust to the dimensions of the display or browser window.
You can specify exactly how each individual element of the dashboard—the widgets—behaves and in which directions they expand when necessary.

Starting with version {v25}, the commercial editions also include an alternative _responsive layout:_ Here, you can easily design your layout using drag-and-drop, and the layout automatically adapts to different screen sizes.

For a comparison of the two systems and further details, see the chapter xref:#layout[Layout.]

Widgets of various categories are available for the content:
Regular views, graphs, metrics, prefabricated elements for various statistics and timelines as well as boxes for static texts and any URLs.
An overview of all widgets can be found xref:builtin_widgets[below].

ifdef::onprem[]
Some of the widgets are available exclusively in the commercial editions.
endif::[]

An important feature of dashboard__ use:__
Using filters, even dashboards that show values for all hosts or services in the network can be broken down into specific areas.
Dashboards are therefore not simply rigid displays, rather they are real tools for finding and analyzing problems and states.


[#usage]
=== Dashboards in operation

==== Interpreting the Main dashboard

On the {CMK} start page you will see the [.guihint]#Main dashboard#, which can be found in the [.guihint]#Monitor# menu and also in the [.guihint]#Views# xref:glossar#snapin[snap-in], each under [.guihint]#Overview > Main dashboard#.

ifdef::onprem[]
{RE} and commercial editions each come with their own individual standard dashboards, so here is a look at the commercial editions variant first:

.The standard dashboard of a commercial edition with the host overview
image::dashboard_main_numbered_01.png[alt="The standard dashboard of the commercial editions with numbers for the detailed description.", fullscreen=1]

[cols="7,~,<.>~", options="header"]
|===
|No. |Title |Function

|1 |[.guihint]#Filter# |Open runtime filter
|2 |[.guihint]#Sharing# |Status and settings for dashboard sharing
|3 |[.guihint]#Settings# |Settings for dashboards and default filters
|4 |[.guihint]#Edit widgets# |Toggle layout mode
|5 |[.guihint]#Host statistics# |Current status of hosts
|6 |[.guihint]#Total host problems# |Timeline of host problems
|7 |[.guihint]#Service statistics# |Current status of services
|8 |[.guihint]#Total service problems# |Timeline of service problems
|9 |[.guihint]#Problem notifications# |Timeline of alerts
|10 |[.guihint]#Percentage of total service problems# |Timeline of active services
|11 |[.guihint]#Host overview# |Visualization of host problems, zoomable with mouse wheel
|12 |[.guihint]#Top alerters (last 7 days)# |Services responsible for alerts
|===



If you move the mouse pointer over the graphs or the host symbols in the [.guihint]#Host overview# widget, you will immediately get more detailed information via a tooltip. The colors correspond to the display in the widgets for the current host and service statistics.

The linked title lines of the widgets take you to more detailed displays.

On {CRE}, however, the main dashboard on the start page is the [.guihint]#Problem Dashboard#, which can also be called up in the commercial editions via [.guihint]#Monitor > Problems > Problems dashboard# and which, quite conventionally, shows unresolved problems and current events in the form of a list:

.The dashboard of the Open Source version showing service issues
image::dashboard_main_numbered_raw.png[alt="The {RE} standard dashboard with numbers for the detailed description.", fullscreen=1]
endif::[]

ifdef::saas[]
The main dashboard shows unresolved problems and current events in the form of a list:

.The standard dashboard of {CE}
image::dashboard_main_numbered_saas.png[alt="The standard dashboard with numbers for the detailed description.", fullscreen=1]
endif::[]

[cols="7,~,<.>~", options="header"]
|===
|No. |Title |Function

|1 |[.guihint]#Filter# |Open filter
|2 |[.guihint]#Sharing# |Status and settings for dashboard sharing
|3 |[.guihint]#Settings# |Dashboard settings
|4 |[.guihint]#Edit widgets# |Toggle layout mode
|5 |[.guihint]#Host statistics# |Current status of hosts
|6 |[.guihint]#Service statistics# |Current status of services
|7 |[.guihint]#Host Problems# |List of unresolved host problems
|8 |[.guihint]#Service Problems# |List of unresolved service problems
|9 |[.guihint]#Events of recent 4 hours# |Events from the last four hours
|===

[TIP]
====
Note on the [.guihint]#Edit widgets#-link for switching to layout mode:
For the xref:builtin_dashboards[built-in dashboards], you will not see this button at first by default!
It only appears, and then permanently, once you have called up the [.guihint]#Dashboard > Clone builtin dashboard# menu item.
Just understand this as a small _protective measure_, because it is recommended to first clone icon:icon_clone[alt="Icon for cloning."] a built-in dashboard and then customize the clone.
====


[#dashboard_filter]
==== Filtering dashboards

As you will see later, dashboards can be created from the outset for a desired selection of hosts or services.
However, you can use filters to temporarily limit each dashboard to a specific selection:

1. Call up the filter function via icon:icon_filter[alt="Icon of a filter."].
2. Add a filter via [.guihint]#Add filter# -- for example [.guihint]#Host name (regex)#.
3. Configure the filter -- for example `myhost`.

The third step varies from filter to filter.
The most important thing here being the handling of the search terms entered, i.e. for host names, for example.
As usual, {CMK} evaluates these as xref:regexes#[regular expressions].
A filter for `myhost` would therefore find `myhost` as well as `2myhost` and `myhost2`.
If you only want to see `myhost` in the dashboard, you must use `^myhost$` as the search term accordingly to include the beginning and end of the line and thus specify an exact match.

Several filters can of course also be combined, which can then reduce the number of hits by using _AND_ links.
Within a filter you may use _OR_ links by means of regular expressions, for example `myhost1|db_server`.

[#layout]
== Layout systems

{CMK} provides two completely different layout systems for its dashboards: our traditional, in-house [.guihint]#Anchored layout# and, in the commercial editions starting with version {v25}, an alternative [.guihint]#Responsive layout#.

{cce-only} The *responsive system* is available exclusively in the commercial editions.
This is more intuitive and adheres to standards: You position and resize widgets manually using the mouse.
If the viewport changes, the layout is automatically optimized—meaning widgets are *automatically arranged and scaled* to fit the viewport size. This is useful, for example, for unusual display sizes or for viewing on different devices, such as dashboards that sometimes need to be displayed on a desktop and at other times on mobile devices.

The *Anchored system* focuses the layout dynamics directly on the widgets rather than on the entire dashboard:
Widgets can be created with fixed heights and widths, but they can also automatically expand in both dimensions to always fill the dashboard optimally—so that widgets are *scaled according to your preferences*, while the overall layout remains intact.

You choose which of the two layout systems to use right at the start when creating a new dashboard—and this choice is final, it cannot be changed later.
By the way, there are no differences in terms of functionality; all widgets are available in both layout systems.

Below you will see how the alternative systems perform in practice.
But the best way to understand their dynamics and differences is still to try them out for yourself!

=== The responsive layout
This layout system is completely intuitive: You place widgets using drag-and-drop, and resize them using the handle in the lower-right corner.
Here is an example, for simplicity’s sake, featuring eight versions of the same [.guihint]#Site overview# widget:

.A responsive layout with manually placed widgets
image::dashboards_responsive_01.png[alt="A responsive layout with manually placed widgets.",fullscreen=1]

For example, if this dashboard is viewed on a mobile device, or if you simply resize the browser window to make it a bit narrower—as shown here for demonstration purposes—the layout and scaling of the widgets will adjust accordingly:

.A responsive layout with automatically positioned and scaled widgets
image::dashboards_responsive_02.png[alt="A responsive layout with automatically adjusted layout.",fullscreen=1]

The big advantage: Everything happens automatically.
The downside: If you want to display a specific widget—such as a graph with a timeline—across the full width at all times, this system doesn't allow for that.


=== Anchored layout
The principle is simple:
any corner of a widget is set as an anchor.
From this fixed point, the widget can then grow in height and/or width as soon as more space is available,
for example, simply on a larger screen, but also when the position or size of other widgets changes.

To illustrate this function, here is an example -- in the center a host matrix widget with manual height and width settings, and in its upper left corner an anchor.
You can recognize the anchor by the green corner, the settings for current height and width can be found in the middle of the widgets.

This is framed by four host-overview widgets, all with automatic height, the side ones also with automatic width
-- the widgets at the top and bottom get the setting [.guihint]#max width#.
By default, the anchor is at the top left, but here the right widget gets the anchor at the top right and the bottom one at the bottom left.

.Manually positioned and scaled widget
image::dashboard_layout_example_1.png[alt="A widget in the dashboard center.", fullscreen=1]

If you now move the widget in the middle further to the left and down, for example, the widgets on the left, right and bottom change
-- because they automatically grow from their anchors towards the central host matrix widget.

The top widget, on the other hand, remains as it is -- after all, it cannot grow downwards because the two side widgets are anchored at the top.

.Widgets with [.guihint]#auto width/height# adjust to fit automatically
image::dashboard_layout_example_2.png[alt="The host matrix widget, moved to the bottom left.", fullscreen=1]

If you now switch the lower widget from [.guihint]#Max width# to [.guihint]#Auto width# it no longer extends over the entire width
-- because the automatic height of the right widget is rendered before the automatic width of the lower widget.

.A question of hierarchy: The automatic height of the right-hand widgets is set before the automatic widths of the bottom widgets are set
image::dashboard_layout_example_3.png[alt="The bottom widget with shortened width.", fullscreen=1]

If widgets with automatic dimensions are competing for the same space, you can use the maximum setting to determine the winner, so to speak
-- but be careful: If two widgets set to maximum compete for the same space, overlapping can occur.

The whole dynamic layout principle is easier to understand if you create such a test setup yourself and try pushing the widgets around a bit.

[#dashboard_config]
== Configurations

[#config_dashboard]
=== Dashboard

You will see the dashboard configuration automatically when you create a new dashboard,
later you can access it via the icons in the dashboard list ([.guihint]#Customize > Visualization > Dashboards#) or the [.guihint]#Dashboard > Properties# menu item in an open dashboard.

.Layout and configuration can be edited separately
image::dashboard_config_dashboard.png[alt="The 'Dashboard' menu with the 'Properties' entry selected.",width=70%]

The properties of the dashboard itself are trivial;
only metadata such as name, menu item or visibility are defined here.


.General settings of a dashboard
image::dashboard_config_example_dashboard.png[alt="General properties of the dashboard."]

[#config_layout]
=== Widgets


You can view the configuration of individual widgets automatically when you add them to a dashboard.
Later,  in the layout mode, you can access their configurations directly via the gear icon on the widgets.

.Opening the widget configuration in the [.guihint]#Anchored Layout#
image::dashboard_config_dashlet.png[alt="The icon for opening a widget's properties.",width=60%]

By the way: In the [.guihint]#Responsive Layout#, the widgets look slightly different, but the icons are the same:

.A widget in the responsive layout view
image::dashboard_config_dashlet_responsive_2.png[alt="A widget in responsive design.",width=60%]

Configuring most widgets is quite simple.
Typically, you will find [.guihint]#Data settings# for limiting and displaying data, as well as [.guihint]#Widget settings# with options for titles, links, and so on—here using the Gauge widget as an example.

.Typical widget options using the Gauge as an example
image::dashboard_config_widget_typical.png[alt="Typical widget options."]
For all widgets that refer to some or specific hosts and services, you will also find corresponding filter options.

A detailed description of the filters can be found in the chapter xref:filter[Filters], and specific examples of widget configurations in the chapter xref:example[Example Dashboard].

ifdef::onprem[]
[#permissions]
=== Permissions

Even away from the dashboard and widget configuration, there are important settings in {CMK}, namely permissions.
Under [.guihint]#Setup > Users > Roles & permissions > Edit role user# you can simply filter for `dashboard` to list all options.
Here for a xref:wato_user#roles[role] you can specify in detail which standard dashboards its assigned users can see and what exactly they are allowed to do with other dashboards.

.Dashboards may contain sensitive information -- not every user role needs all rights here
image::dashboard_config_permissions.png[alt="User role with the permissions for dashboards."]
endif::[]

[#filter]
=== Filter
Filtering dashboards and widgets is a powerful feature available in several forms.
Here is an overview:

* Dashboard configuration
** [.guihint]#Default filters#
** [.guihint]#Runtime filters# -- without values
* Widget configuration
** [.guihint]#Widget filters#
* Dashboard view
** [.guihint]#Runtime filters# -- values set by the user

[.guihint]#Default filters# set specific criteria for the entire dashboard; for example, the [.guihint]#Host name (exact match)# filter with the value `myhost`.

Setting [.guihint]#Runtime filters# in the dashboard configuration sets empty defaults for the entire dashboard—for example, the [.guihint]#Host name (exact match)# filter—and users must then set these themselves at runtime (i.e., when viewing the dashboard).
If [.guihint]#Runtime filters# are set in the configuration, the dashboard starts with the filter dialog open—after all, users *must* fill these filters with values.

[.guihint]#Widget filters# are set for each widget in its respective widget configuration.
Here, filtering is based not only on hosts and services, but also on metrics (from services) if required.

You can access the two dashboard filters from the dashboard via [.guihint]#Settings > Filter settings#, and the widget filters via the gear icon on the widgets themselves.

[.guihint]#Runtime filters# are also available outside the configuration in the regular dashboard view -- for regular users.
You can access these filters via the icon:icon_filter[alt="Filter icon."] [.guihint]#Filter# button right next to the dashboard selection.

It is also important to note how these filters interact with each other: Widget filters *override* runtime filters, which in turn *override* default filters.

The following image illustrates the entire filter spectrum for hosts (points 3 through 5):

.Three competing host filters
image::dashboards_filter_three_filters.png[alt="Competing dashboard filters.",fullscreen=1]

[cols="5,20,<.>~", options="header"]
|===
|No. |Title |Function
|1 |Service filter |Dashboard-wide filter by service [.guihint]#Memory#
|2 |Metric filter |Widget-specific filter on the metric [.guihint]#RAM usage#
|3 |Host filter - Default |Dashboard-wide filter on the host `localhost`, fixed
|4 |Host filter - Runtime |Dashboard-wide filter on the host `myhost`, via user input
|5 |Host filter - Widget |Widget-specific filter on the host `raspi_lan`, fixed -- this is the currently *active* filter!
|===
This dashboard includes a predefined [.guihint]#Metric# widget that always displays the [.guihint]#RAM usage# metric for the [.guihint]#Memory# -- as standard, via the default filter from the host `localhost`, which is overridden here by the runtime filter to the host `myhost`, which in turn is overridden by the widget filter to the host `raspi_lan`.

In other words: When creating dashboards, you can set dashboard-wide defaults that users can either override (runtime filters in the view) or are required to override (runtime filters in the configuration).
Individual widgets, on the other hand, can be configured with fixed settings, preventing users from applying additional filters.

[#sharing]
=== Sharing
Have you spent hours building a powerful dashboard?
It would be a shame to keep it to yourself.
That’s why you can share dashboards, both internally and externally.

.The Status display and access to sharing settings
image::dashboards_sharing_status.png[alt="Sharing status in the dashboard",width=60%]

You can access the corresponding configuration in the respective dashboard via [.guihint]#Share > Configure sharing#.

An internal link to the dashboard is available by default at [.guihint]#Internal access# -- and this does not affect the status shown at the top of the image (here [.guihint]#Sharing paused#).

.Public access can be restricted to specific times
image::dashboards_sharing_config.png[alt="Dashboard sharing configuration"]

This status applies solely to the public link at [.guihint]#Public access#; after all, dashboards may well contain sensitive information that is not necessarily intended for public viewing.

As soon as you enable a link here, the status changes from [.guihint]#Sharing disabled# to [.guihint]#Sharing enabled#.
If public access is enabled and you make changes to the dashboard, you will see the status [.guihint]#Sharing paused#.
To resume sharing, you must share the dashboard again.
The sharing link itself does not change.

The only option when sharing: You can set an expiration date for the share.
In {CRE}, shares are limited to a maximum of 30 days; in the commercial editions, an expiration date is completely optional.

Shared, public dashboards have a few restrictions for security reasons: navigation and menus are hidden, there are no filtering options, and sidebar elements are not available.
Additionally, not all entries may be visible in list views because they are limited by default, and the buttons to remove these limits are missing — again, for security reasons.


[#example]
== An example of a dashboard

This example project will guide you through the necessary steps for setting up a dashboard from scratch.
In doing so, you will basically see examples of all of the available options.
ifdef::onprem[]
In order to completely replicate this example, you will need one of the commercial editions.
endif::[]

Four widgets serve this purpose:

The [.guihint]#Performance graph# widget shows a host's file system usage,
[.guihint]#Gauge# shows the average CPU usage over the last minute,
the [.guihint]#Alert timeline# visualizes alerts for a selection of hosts and services over a timeline,
and the [.guihint]#Scheduled downtimes# view lists xref:glossar#scheduled_downtime[scheduled downtimes].

And this is what the finished dashboard will look like:

.A dashboard with four widgets -- for CPU load, file system, notification statistics, and scheduled maintenance times
image::dashboards_example_final_01.png[alt="The example dashboard created below.", fullscreen=1]

[#example_build]
=== Building an example dashboard

==== Creating a dashboard

First, create a dashboard by going to [.guihint]#Customize > Visualization > Dashboards > Add dashboard#.

You will be taken to a dialog with three separate sections.

.Default settings for each new dashboard
image::dashboards_example_01.png[alt="Default settings for new dashboards."]

First, select the [.guihint]#Dashboard type#.
This type determines which data is generally available to the dashboard.
The default here is [.guihint]#Unrestricted# — meaning there are no restrictions, and you or dashboard users can filter by individual hosts or services later if needed.

The [.guihint]#Specific host# type restricts all dashboard visualizations to a specific host.
You will later specify which host this is; here, the focus is simply on restricting the data source to "a single host."

With the [.guihint]#Custom# type, you can specify exactly which data sources are available to the dashboard.
These could include, for example, specific hosts, services, and even individual objects such as Docker images or fans.

For this example, leave the default setting [.guihint]#Unrestricted# as is.
You can also filter the results later.

Next, move on to the [.guihint]#General properties# box.

All that is required here is a name and an ID for the dashboard; the [.guihint]#Unique ID# can be generated automatically if desired.

However, the [.guihint]#Dashboard layout# option requires special attention: At this point, you must irrevocably choose one of the two layout options described in the chapter xref:#layout[Layout].

For our example, choose the traditional [.guihint]#Anchored# -- simply because there's a bit more to show here.

In the [.guihint]#Visibility# box, you can specify whether your new dashboard appears in the main navigation and, if so, in which category.
You can also configure this setting later.
Additionally, by default, your dashboard appears in the dashboard drop-down menu in every dashboard, as well as in the dashboard sidebar element.

Following this brief initialization, you'll be taken to the new, empty dashboard -- although "empty" isn't quite accurate.
As long as no widgets have been added yet, tiles provide quick access to the available widgets, which can also be accessed in the top-right corner via [.guihint]#Add widget#.

.A new, empty dashboard with placeholder tiles
image::dashboards_example_02.png[alt="Empty dashboard with quick access tiles.",fullscreen=1]

==== Dashboard configuration

Before you start filling out the dashboard, take a look at the general dashboard settings under [.guihint]#Settings > Dashboard settings#.
In addition to the settings you’re already familiar with from the [.guihint]#Add dashboard# dialog, you’ll see the [.guihint]#Access# tab here, which lets you specify exactly who is allowed to access this dashboard—by default -- only you!

.Who can see the dashboard? By default, only you!
image::dashboards_example_02_b.png[alt="Permissions for viewing dashboards."]

Filters should also be a part of the basic configuration; you can find these under [.guihint]#Settings > Filter settings# (more on this in a xref:#filter[separate chapter].
When creating the dashboard, the [.guihint]#Unrestricted# type was selected, so that all monitoring data is available to you.

You don’t necessarily need to set filters here for the sample dashboard.
However, if you’re working in an environment where hundreds of hosts are already being monitored, it may be worth narrowing down the selection — for example, to only Windows hosts, or hosts at a specific location.
You can set filters for individual hosts directly within the widgets.

.Specific filters required at the dashboard level
image::dashboards_example_02_c.png[alt="Filter settings at the dashboard level.",fullscreen=1]



==== Adding a [.guihint]#Graph# widget
The first widget will display the file system usage as a graph.
To do this, first configure the desired data, then set up its visualization.

Start the configuration on the blank dashboard by clicking [.guihint]#Add widget > Metrics & graphs#.

Data selection is done in three steps: Host, Service, Metric.
First, set a host filter: For this example, a single, specific host [.guihint]#myhost# under [.guihint]#Host selection > Single host > Widget filters# is sufficient.

.A widget filter for a single host
image::dashboards_example_03.png[alt="Host filter settings at the widget level."]

Next, specify the desired service; in this example, [.guihint]#Filesystem C:/#.

.Widget filter for a specific service
image::dashboards_example_04.png[alt="Service filter settings at the widget level."]

In the third step, select the desired metric under [.guihint]#Metric selection > Metric (single) > Service metric (required)#, in this case [.guihint]#Used space %#.
This drop-down menu is filtered based on your selection of host and service, so that only relevant metrics will be available.

Note: The filter *below* the service metric selection is only used to filter the drop-down menu and does not override the previously selected host and service — without this filter, the list would be hundreds of entries long!

.Widget filter for the final metric
image::dashboards_example_05.png[alt="Metric filter settings at the widget level."]

To continue, click [.guihint]#Next step: Visualization# at the top of the data configuration, or any visualization at the bottom.

Several visualization types would be suitable for the [.guihint]#Used space %# metric.
In this case, we'll use the [.guihint]#Graph# so that we can also see how usage changes over time.

.Visualization as a graph, including a live preview
image::dashboards_example_06.png[alt="Selecting and previewing the visualization."]

Below the preview, you will now see two panels with settings that appear in many widgets: [.guihint]#Data settings#, which allows you to set the time range, and [.guihint]#Widget settings#, which lets you configure the title, links, and appearance.
Set a [.guihint]#Time range# that displays some meaningful data.

*Tip:* It’s often a good idea to append [.guihint]#$HOSTNAME$# to the titles of widgets so it is clear which host the respective date belongs to.

.Basic configuration of the visualization
image::dashboards_example_07.png[alt="Widget settings for the timeline and display."]

Finally, there is a graph-specific configuration that corresponds to the general xref:graphing#[graphing functions] and which is, of course, entirely optional.

.Fine-tuning the graph display
image::dashboards_example_08.png[alt="Graph-specific settings."]

If you're satisfied with the layout, you can continue by clicking [.guihint]#Add & place widget#.

The new widget can now be positioned and scaled as described in the chapter xref:#layout[Layout].
For the example dashboard shown above, [.guihint]#Manual height# and [.guihint]#Auto width# are suitable options: You specify the desired height, and the algorithm automatically extends the widget from the anchor (top left) to the right, up to the next widget or, in this case, to the edge of the dashboard.

.The green [.guihint]#Anchor# is important as a starting point for resizing
image::dashboards_example_09.png[alt="Widget placement and resizing."]


==== Adding the [.guihint]#Gauge# widget

The [.guihint]#Gauge# widget
ifdef::onprem[]
is available only in the commercial editions and
endif::[]
displays values such as CPU usage in a style similar to a car speedometer.

The configuration is almost identical to that of the graph you have just created; again, filter by `myhost` and now by the `CPU load` service.
Once again, both values are automatically carried over into the [.guihint]#Metric selection# box.
Select [.guihint]#CPU load average of last 15 minutes# as the metric.


.Once again, only metrics that match the host and service will be available
image::dashboards_example_gauge_01.png[alt="Metric selection for the ‘Gauge’ widget."]

The Gauge widget offers some interesting data settings.
You’ve already learned about limiting the time range.
For this type of visualization, however, it’s also useful to limit the data range — for percentage values, a range of 0 to 100 works well.
However, alternatives are possible: For example, here you could enter the maximum value before the service changes from {OK} to {CRIT} to see larger fluctuations during normal operation.
In our example, the [.guihint]#Show colored status label# and [.guihint]#Show colored metric background# options have also been added to attract maximum attention.

.The Gauge widget displays not only the metric but also the status of the associated service
image::dashboards_example_gauge_02.png[alt="Display options for the ‘Gauge’ widget."]

After saving, you will return to layout mode and can place the widget below the file system graph (which will initially be covered by the new widget!).
Here, you can set the width and height manually.
You can set the desired size by dragging the widget’s edges with the mouse.
At this point, you could also set the graph widget to automatic height and simply let the placement of the new Gauge widget determine its height.

.Once the Gauge widget has been placed, the Graph widget could also be set to automatic height and scaled indirectly based on the Gauge's placement
image::dashboards_example_gauge_03.png[alt="The ‘Gauge’ widget in the dashboard's layout mode."]


==== Adding an [.guihint]#Alert Timeline# widget

The third widget is the [.guihint]#Alert Timeline#
ifdef::onprem[]
-- also available exclusively in the commercial editions --
endif::[]
which displays alerts on a timeline.

This widget is intended to display data from multiple hosts and services — which is why there is no dashboard-wide pre-filtering at the top.
With this widget, it often makes sense to disable filters entirely in order to evaluate all hosts.
You can be more precise by using the [.guihint]#Host name (regex)# host filter, which you first add from the filter list on the left.
Here you can work with regular expressions as usual.
For example, to retrieve all alerts from all hosts that start with `my`, set the host name filter to `^my`.
For our example, we want to be a bit more specific, so we’ll limit this to host names containing `myhost` or `localhost`, using the notation `myhost|localhost`.

Just as a reminder -- regardless of which default or runtime filters are set, the widget filters take precedence.

.Regular expressions enable the creation of graphs with precisely -filtered metrics from multiple hosts
image::dashboards_example_alerts_01.png[alt="Context filter for the ‘Alert timeline’ widget."]

For our example widget, the specified layout [.guihint]#Barplot# is retained.

.The bar chart is particularly well-suited for the [.guihint]#Alert timeline#
image::dashboards_example_alerts_02.png[alt="Features of the ‘Alert timeline’ widget."]

The time period, of course, depends on the size of your project; for our example, let’s use [.guihint]#The last 35 days#.
A new feature here is [.guihint]#Time resolution#, which essentially determines the scale of the X-axis — in this case, whole days.

.Alerts per day over a 35-day period, displayed as a bar chart
image::dashboards_example_alerts_03.png[alt="Data configuration for the ‘Alert timeline’ widget."]

After saving, place the widget back on the dashboard.
Here, it’s best to use an automatic width with a manual height to fill the row with the [.guihint]#Gauge# widget.

.In this setup [.guihint]#max width# in the Alert widget would have the same effect
image::dashboards_example_alerts_04.png[alt="The ‘Alert timeline’ widget in the dashboard's layout mode.",fullscreen=1]

==== Adding a widget via a view

Existing xref:glossar#view[views] can also be used as widgets.
This works via [.guihint]#Add widget > Views > Link to existing view#, but also via the views themselves.
Alternatively, you can use copies of views here or create entirely new views.
For our example, however, we’ll take the most elegant approach: using the views themselves.

To add the xref:glossar#scheduled_downtime[scheduled downtimes] view, for example, call it up via [.guihint]#Monitor > Overview > Scheduled downtimes#.
Then add this view to your dashboard via [.guihint]#Export > Add to dashboard#

.All table displays in {CMK} can be exported to dashboards
image::dashboard_example_10.png[alt="The 'Export' menu of the 'Scheduled downtimes' view.",width=55%]

Select your dashboard.

.Dashboard selection by name/ID -- another incentive to use descriptive names
image::dashboard_example_10a.png[alt="Selection of the dashboard for the view."]

Place the widget as the last row.
Here you can now use automatic height and width to eliminate empty areas.

If you now call up the icon:icon_dashlet_edit[alt="Icon for the properties."] widget properties from the layout mode, the settings already known from xref:views#[views] are available to you, for example, to make the widget a little slimmer
-- after all, a click on the widget title takes you to the full scheduled downtimes view anyway.

With that, your example dashboard is ready, shown here again completely in layout mode:

.The exported [guihint]#Scheduled downtimes# view fills the rest of the dashboard
image::dashboards_example_view_01.png[alt="The complete example dashboard in layout mode.", fullscreen=1]

[#builtins]
== Built-in dashboards and widgets

[#builtin_dashboards]
=== Built-in dashboards

A list of all individually-created and built-in dashboards can be found via [.guihint]#Customize > Visualization > Dashboards#.
For your own variants, you can call up the properties via icon:icon_edit[alt="Icon for editing."] and the layout mode via icon:icon_dashboard_edit[alt="Icon of the layout mode for dashboards."].
You cannot edit the factory-set dashboards directly from the list, but you can clone them via icon:icon_clone[alt="Icon for cloning."] and then customize the clone.

ifdef::onprem[]
[TIP]
====
Not all dashboards are integrated in all editions of {CMK}.
The cloud vendor specific dashboards can only be found in {CE}, {RE} is limited to some basic dashboards.
====
endif::[]

Here is an excerpt of the built-in dashboards:

[cols="15,30,~",options="header"]
|===
|Name/ID |Monitor menu item |Content
|`aws_ec2_overview` |Cloud > AWS EC2 instances |Overview of EC2 instances
|`azure_vm_overview` |Cloud > Azure VM instances |Overview of Azure VMs
|`checkmk` |System > Checkmk dashboard |{CMK} servers and sites
|`gcp_gce_overview` |Cloud > GCP GCE instances |Overview of GCP VMs
|`kubernetes_overview` |Applications > Kubernetes |Overview of clusters, resources, nodes etc.
|`main` |Overview > Main dashboard  |Main View
ifdef::onprem[]
|`ntop_alerts` |Network statistics > Alerts |Alerts in xref:ntop#[ntopng]
endif::[]
|`problems` |Problems > Problems dashboard |Problems and statistics from all hosts and services. The [.guihint]#Problems dashboard# is the [.guihint]#Main dashboard# in {RE}.
|`simple_problems` |Problems > Host & service problems |Problems for all hosts and services
|`site` |- |Overview of a site
|===


[#builtin_widgets]
=== Widgets

Here you will first see an overview of all widgets, then we will show you a few special features that were not covered in the example dashboard above.

[cols="15,30,12,~",options="header"]
|===
|Category |Name |{RE} |Function

|[.guihint]#Views# |[.guihint]#View# |yes |Regular views as widgets
|[.guihint]#Custom graph# |[.guihint]#Custom graph# |no |Manually created free-form graphs
|[.guihint]#HW/SW Inventory# |[.guihint]#HW/SW Inventory of host# |no |Data from the inventory
|[.guihint]#Events# |[.guihint]#Event statistics# |yes |Overall event status statistics

.8+|[.guihint]#Metrics & graphs#
|[.guihint]#Single metric graph# |no |Graph for individual metrics over time
|[.guihint]#Performance graph# |yes |Predefined performance graphs for individual hosts/services
|[.guihint]#Combined graph# |no |Graphs with multiple metrics
|[.guihint]#Scatterplot# |no |Metrics from various hosts/services as a scatter plot
|[.guihint]#Barplot# |no |Bar chart for individual services
|[.guihint]#Gauge# |no |Single metric as a _gauge_
|[.guihint]#Single# |no |Single metric as a number
|[.guihint]#Top list# |no |Ranking of a single metric across multiple hosts/services

.4+|[.guihint]#Host & site overview#
|[.guihint]#Host state# |no |Status of a single host
|[.guihint]#Host state summary# |no |Summary of individual statuses
|[.guihint]#Host statistics# |yes |Overall statistics on host status
|[.guihint]#Site overview# |no |Hosts/sites as status hexagons

.3+|[.guihint]#Service overview#
|[.guihint]#Service state# |no |Status of an individual service
|[.guihint]#Service state summary# |no |Summary of individual statuses
|[.guihint]#Service statistics# |yes |Overall statistics on service status

.4+|[.guihint]#Alerts & notifications#
|[.guihint]#Alert overview# |no |Hosts as alert hexagons
|[.guihint]#Alert timeline# |no |Alerts over a timeline
|[.guihint]#Notification timeline# |no |Notifications over a timeline
|[.guihint]#Percentage of service problems# |no |Percentage of service problems via timeline

ifdef::onprem[]
.3+|[.guihint]#Ntop (if available)#
|[.guihint]#Alerts# |no |Alerts in xref:ntop#[ntopng]
|[.guihint]#Flows# |no |Data flows in ntopng
|[.guihint]#Top talkers# |no |Hosts in ntopng generating the most network traffic
endif::[]

.4+|[.guihint]#Other elements#
|[.guihint]#Embed URL# |yes |Internal/external URLs
|[.guihint]#Static text# |yes |Static text for notes
|[.guihint]#Sidebar widget# |yes |Any sidebar widgets
|[.guihint]#User messages# |yes |User notifications
|===

How exactly you design the graphs in the graphing widgets is a bit more complex and is explained in detail in the xref:graphing#[graphing article].

A special role is played by the [.guihint]#Custom URL# widget.
In theory, you can integrate external websites or resources by simply specifying an address
-- in practice, this often fails due to the security measures used by the operators and browsers.
However, this does work with {CMK}'s own resources, for example other dashboards.
You could, for instance, link several host-specific dashboards to one big overview.
What you can also integrate -- with a little experimentation -- are internal resources from the {CMK} server;
for example, any kind of web application, be it a wiki or a small chat program.

ifdef::onprem[]
[#widget_siteoverview]
The [.guihint]#Site overview# widget has two functions:
In the xref:layout[layout] explanations above, you see it as an overview of hosts -- by default on a normal, single {CMK} site.
In xref:distributed_monitoring#[distributed monitoring], on the other hand, this widget shows an overview of the namesake sites.
endif::[]

The widget [.guihint]#Static text# can do a little more than you might expect:
it is suitable for notes and simple labels, but it can also be used to link to other dashboards or other {CMK} components,
as you can see in the following example of a top-down dashboard.


[#examples]
== Dashboard examples

[#dashboard_examples_1]
=== Top-down dashboards

So now you are familiar with all of the available widgets and finished dashboards, know where to access their configurations and layout options, and how to assemble a complete dashboard.
However, dashboards do not necessarily have to stand on their own, but can also build on each other
-- for example, to navigate from a large overview to the smallest detail.

Basically, many dashboards already work exactly like this:
The [.guihint]#Host statistics# visualizes the host states and a click on one of the states redirects to a view that lists the associated hosts
-- and from there it goes on to the individual services of a single host.

For your own dashboards, you can also create links to other dashboards by linking the title bar of widgets via [.guihint]#Link of Title# in the widget options.
Let's take a concrete example:
Here you see a dashboard that shows information on the CPU, RAM and file systems of some hosts.

.Top-down dashboards are implemented via links, here in the graph’s title
image::dashboard_topdown1.png[alt="Example dashboard of all hosts starting with 'my'."]

In this example the title of the CPU graph links to another dashboard that visualizes the CPU information for each host individually.
In this dashboard, there is again a link at the top back to the overview, simply achieved using a [.guihint]#Static text# widget.

.The return path in the top-down dashboard is via manually-edited text links
image::dashboard_topdown2.png[alt="Link to return to the calling dashboard."]

With such cross-links, you can develop complex research tools via dashboards.
By the way, you are not limited to the title bar -- you can certainly use HTML code as static text and thus incorporate entire navigations.
ifdef::onprem[]
Consider, for instance, distributed monitoring and paths such as _General overview > Instance overview > Host overview > Container overview > Services > Problems._
endif::[]


[#troubleshooting]
== Troubleshooting

=== Missing filters

It may occur that you will see only the following warning with a yellow background for a widget:

[.guihint]#Enter runtime filter to load data#

In this situation, the widget should be displaying information for only a single host and/or service -- but for which no filter has been set.
To fix this, you can either use the filters in the dashboard view or in the widget configuration.


=== Empty widgets

There can be several reasons for completely empty widgets without error messages.
Usually it is a misconfiguration of the widget.

Example: You create a widget for CPU load with the metric and the filtered service [.guihint]#CPU load#.
Later you change the filter of the service description to something like [.guihint]#Check_MK Discovery#, but leave the selected metric at [.guihint]#CPU load#.
When creating a widget, this cannot happen to you, because after the filter on the CPU load, the selection of an unsuitable metric is impossible
-- when reconfiguring widgets, however, the originally selected metric is retained.

The solution to this problem is trivial
-- alter the service filter and the selected metric in the widget configuration.
Of course, this also applies to all other widget variants.
