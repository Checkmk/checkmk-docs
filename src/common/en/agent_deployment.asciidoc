// -*- coding: utf-8 -*-
// IGNORE TMPDIR ruleset 64bit
// NONASCII
include::global_attr.adoc[]
= Automatic agent updates
:title: Automatic agent updates - Distribute agents and plug-ins automatically
:description: In this article you will learn how to create custom installation packages for agents in {CMK}, and if required, distribute them automatically.

{related-start}
xref:wato_monitoringagents#[Monitoring agents]
ifdef::onprem[]
xref:update#[Updates and Upgrades]
xref:cmk_commandline#[{CMK} on the command line]
endif::[]
{related-end}


ifdef::onprem[]
{cee-only}
The commercial editions can update their agents on Linux, Windows and Solaris automatically.
endif::[]
ifdef::saas[]
{CE} can update its agents on Linux, Windows and Solaris automatically.
endif::[]
This feature enables easy updating of the agents in the case of new {CMK} versions, and even a changed configuration of the agents can be applied automatically.
In this way you can take advantage of the xref:wato_monitoringagents#bakery[Agent Bakery] to apply individual configurations to hosts.

// This block is new
// start translation
////

[#intro]
== Einleitung

Dieser Artikel beschreibt, wie Sie automatische Agenten-Updates auf Ihren überwachten Hosts einrichten.

Im Abschnitt xref:#setup_automatic_updates[Automatische Updates einrichten] erfahren Sie, welche vorbereitenden Schritte nötig sind und wie Sie die automatischen Agenten-Updates für Hosts aktivieren.

Der Abschnitt xref:#deactivate_automatic_updates[Automatische Updates überspringen oder abschalten] zeigt, wie Sie festlegen und testen können, welche Hosts automatische Updates erhalten.

Der Abschnitt xref:#diagnosis[Diagnose] hilft Ihnen, zu beurteilen, ob alle Updates wie gewünscht verteilt werden.

ifdef::onprem[]
Der Abschnitt xref:#scenarios[Einsatzszenarien] gibt für die beschriebenen typischen Szenarien Tipps zur Umsetzung und zu zusätzlichen Aspekten, die jeweils zu beachten sind.
endif::[]

ifdef::onprem[]
Die Einrichtung von automatischen Updates im verteilten Monitoring wird gesondert im Abschnitt xref:#central_setup[Agenten-Updates im verteilten Monitoring] behandelt.
endif::[]

ifdef::onprem[]
Da wir dringend empfehlen, HTTPS zu verwenden, erhalten Sie hierzu im Abschnitt xref:#https_handling[HTTPS-Handling] konkrete Anleitungen zur Absicherung.
endif::[]

Der Abschnitt xref:#faq[Fehlerbehebung] listet einige typische Fehler und ihre Ursachen und möglichen Lösungen auf.

ifdef::onprem[]
Im Abschnitt xref:#files[Dateien und Verzeichnisse] finden Sie alle relevanten Pfade auf dem {CMK}-Server und auf dem überwachten Host.
endif::[]
ifdef::saas[]
Im Abschnitt xref:#files[Dateien und Verzeichnisse] finden Sie alle relevanten Pfade auf dem überwachten Host.
endif::[]
////
// end translation

[#setup_automatic_updates]
== Setting up automatic updates

To implement the updates, follow these steps:
First open [.guihint]#Setup > Agents > Windows, Linux, Solaris, AIX# and select [.guihint]#Agents > Automatic updates#:

image::agent_deployment_automatic_agent_updates.png[alt="Prerequisites for the automatic agent updates."]

See [.guihint]#Prerequisites# for a list of prerequisites that need to be met for the automatic updates to work correctly.
You can simply tick these off in order.
Do not forget that you can get more information for each of these items via [.guihint]#Help > Show inline help#.
Clicking on icon:icon_edit[alt="Icon for editing."] will take you directly to the setting you need to configure.
Specifically, the settings described in the following five sections must be made and then activated via the [.guihint]#Master switch#.


[#create_signature_key]
=== Creating a signature key

The system is designed in a way that the agents can download updates via HTTP or HTTPS from their central monitoring server.
Because agents contain executable code it is especially important to guard against the possibility of falsified agents coming from an attacker.
Signature keys are used for this purpose.
These keys consist of a pair of public and private keys (the public-key method).

You can create as many signature keys as you like.
Each of these is secured with a passphrase, which you will subsequently need to enter each time you sign.
This prevents, for example, an attacker gaining access to a monitoring server backup from signing agents.

Create a signature key here and record the passphrase.

[IMPORTANT]
====
This signature key can later neither be changed nor restored.
If the key is lost, it can mean that all agents need to be updated manually once again.
====


[#configure_update_plugin]
=== Configuring the update plug-in

The actual update is performed by an agent plug-in named `cmk-update-agent`.
This is called by the agent in a definable cycle (e.g. once per hour).
At this time it asks the deployment server (your central monitoring system) if there is a new package available for this host, and if so it will then perform the update.

The plug-in must of course be present and correctly configured in the agent.
To do this, extend the agents with this plug-in using the [.guihint]#Agent updater (Linux, Windows, Solaris)# rule set.
You can find this rule set by clicking on the [.guihint]#Configuration of update plug-in# entry on the [.guihint]#Automatic agent updates# page.

Note that the rule set here follows the xref:wato_rules#matching_type[‘First rule per parameter wins’] principle.
This allows you to define basic settings in general so that they do not have to be set again and again in the more specific rules.
In addition, the xref:user_interface#inline_help[inline help] provides more information about each item as soon as you activate it.

// This block is new
// start translation
////
.In der Regel zur Konfiguration des Agent-Updater-Plugins legen Sie Details für das Verhalten des Updater-Plugins im Agentenpaket fest
////
// end translation
image::agent_deployment_agent_updater_empty.png[alt="The still empty rule for configuring the agent updater plug-in."]

Below are a few explanations of the individual points.


[#activation]
==== Activation

// This block replaces the following text
// start translation
////
Setzen Sie ein Häkchen für die Option [.guihint]#Activation# und wählen Sie den passenden Wert aus ([.guihint]#Deploy plugin ...#), damit das Plugin zum Agenten hinzugefügt wird.
////
// end translation
// delete from here
This setting must be enabled ([.guihint]#Deploy plugin ...#) to allow the plug-in to be added to the agent.
// delete to here
Here, for example, rule inheritance can be used to set a default in a higher-level folder and override this for individual hosts/folders as needed.


[#update_server_information]
==== Update server information

Optional configuration data for the connection of the agent updater to the {CMK} server can be entered here.
If this entry is not configured, the information must be entered later when registering the agent updater.


[#fixed_update_server]
===== Usage

ifdef::onprem[]
In the case of xref:glossar#central_setup[distributed monitoring with central setup], the agent updater receives its relevant update server by default from the {CMK} site to which it connects.
This option can be used to configure that the update server entered here is used permanently and that the automatic update server will be ignored.
endif::[]
ifdef::saas[]
This setting is only relevant for the on-premises editions of {CMK} and has no effect for {CE}.
endif::[]


[#dns_name_update_server]
===== DNS name or IP address of update server

Here you enter -- with a few exceptions -- the name of the server on which you are currently configuring this rule.
Therefore, you can copy the URL from your current browser window.

An exception to this approach would be if the affected hosts should be ‘moved’ to another {CMK} server.
ifdef::onprem[]
For this situation, once only, enter a different server here.
See also below under xref:scenarios[Application scenarios].
Enter the DNS name under which the {CMK} server can be accessed.
It is important here that the host to be monitored can resolve this DNS name and that it has been configured as a host in {CMK}.
When using HTTPS, make sure that the name of the certificate matches the name of the {CMK} server known to the host.
endif::[]


[#cmk_site_update_server]
===== Name of {CMK} site of update server

Here you enter -- with a few exceptions -- the name of the site on which you are currently configuring this rule.
An exception to this approach would be if the affected hosts should be ‘moved’ to another {CMK} site.
ifdef::onprem[]
In such a case, for one time only, enter a different site here.
See also below under xref:scenarios[Application scenarios].
In a xref:glossar#central_setup[distributed monitoring with central setup], the site where you want to register may also be different from the central site where this rule is configured.
endif::[]


[#update_protocol]
===== Protocol to use for fetching updates

ifdef::saas[]
For {CE} only HTTPS is relevant.
endif::[]

ifdef::onprem[]
If -- as we recommend -- you use HTTPS,
you must ensure that the agent updater also has a CA certificate (CA = Certification Authority) available to verify the connection.

[IMPORTANT]
====
Depending on the server configuration, the use of HTTPS (including forwarding from HTTP to HTTPS) may be forced.
In such a case, configuring this rule to HTTP has no effect.
====
endif::[]


[#certificates_for_https]
==== Certificates for HTTPS verification

ifdef::saas[]
For updates obtained from your {CE} site, this setting is irrelevant and can be left empty.
endif::[]
ifdef::onprem[]

CA or self-signed certificates configured here are available to the agent updater for the verification of HTTPS connections.
Alternatively, in the case of a xref:glossar#central_setup[distributed monitoring with central setup], certificates can also be made available to the agent updater from the {CMK} site,
or these can be imported directly during the connection via command line parameters.

[IMPORTANT]
====
If the server's certificate chain is signed with a public CA, the connection can normally be verified without imported certificates.
However, as soon as imported certificates from one of the mentioned sources are available to the agent updater, all other CA certificate authorities are ignored!
In the case of problems with the configuration of HTTPS, please consult the xref:faq4[following FAQ] below.
====
endif::[]


[#interval_for_update_check]
==== Interval for update check

Here you specify the interval in which the agent updater queries the configured monitoring server whether any updates are available.
As long as the specified interval has not expired, a cached call is returned, so as to burden the network load as little as possible.
It usually makes sense to use an interval of not less than 10 minutes,
otherwise there is the very great danger that your network will be very heavily burdened if you have a large number of {CMK} agents.
If you do not set a value here a default value of 1 hour will apply.


[#proxy_settings]
==== Proxy settings

This rule setting is likewise optional.
The agent updater initially assumes that there is a direct connection to the {CMK} server on the target host, even with configured proxy settings, and ignores all local proxy settings.
If this is the desired behavior this rule setting can therefore be omitted.
Otherwise either enter proxy settings manually, or use the host’s existing environment variables.


[#executable_format]
==== Executable format (Linux)

// This block replaces the following text
// start translation
////

ifdef::saas[]
Belassen Sie diese Einstellung auf [guihint]#packaged binary#.
endif::[]

ifdef::onprem[]
Sie können optional bestimmen, in welcher Form das Plugin dem Installationspaket für den Agenten hinzugefügt wird.
Die Auswahl, die Sie hier vornehmen, beeinflusst nur die Bereitstellung des Updaters für Linux-Hosts.

Für 64-bit-Systeme können Sie die Option [.guihint]#packaged binary# wählen.
Für die Bereitstellung auf 32-bit-Systemen oder in Kontexten, in denen Sie aus anderen Gründen nicht die 64-bit-Binary nutzen möchten, wählen Sie stattdessen die Option [.guihint]#Python script#.
Dabei gelten die folgenden Voraussetzungen für das System:

* Python in der Version 3.7 oder neuer
* Die Python-Module _cryptography_, _requests_ und _pysocks_

Auf Hosts mit anderen Betriebssystemen wird der Agent Updater in einem automatisch ausgewählten Format bereitgestellt.
Bei diesen Hosts wird Ihre Auswahl für die Option [.guihint]#Executable format (Linux)# nicht berücksichtigt.
endif::[]
////
// end translation
// delete from here
ifdef::saas[]
Leave this setting at [guihint]#64bit packaged binary#.
endif::[]

ifdef::onprem[]
You can optionally specify how the plug-in is added to the agent installation package.
How the rule behaves by default depends on the target system:

* Linux (deb, rpm, tgz):
You do not have to manually edit anything for these systems; the agent updater is passed as a 64-bit binary.
You can also optionally select a 32-bit binary for legacy systems, or the old Python script.
*Note*: For the binary you will need the _glibc_ package (minimum the 2.5 version).
Linux distributions have generally met these requirements since 2006.

* Windows:
For Windows hosts {CMK} will always deploy a 32-bit executable.
This rule is therefore ignored by default.
*Note*: Should you find a 64-bit binary of the agent updater on any of your Windows hosts, this version dates back to an older version of {CMK} and is not up to date.

* Solaris:
You do not have to modify anything here either.
{CMK} will use the Python script even if you leave the default value on the 64-bit binary.

* Other architectures:
If you have packages for other architectures such as arm or ppc, set this option manually to [.guihint]#Python#,
since {CMK} does not intercept this automatically and no binaries are offered for these platforms.

If you still need to rely on the old Python script the following requirements apply to the system:

* Python3 in version 3.4 or newer
* The Python-Modules _requests_, _PySocks_ and _pyOpenSSL_
endif::[]
// delete to here


[#signature_keys]
==== Signature keys the agent will accept

Select at least one signature key whose signature should be accepted by the agent updater.
You can also optionally specify multiple keys.
This can be the case if, for example, you want to disable an old key.
For this purpose the host’s agent updater must in the interim accept both keys.

// This block replaces the following text
// start translation
////

[#registration]
==== Registrierung (Registration)

Seit {CMK} {v25} ist es möglich, den Agent Updater gemeinsam mit dem Agent Controller zu registrieren.
Mit der Einstellung für [.guihint]#Registration# legen Sie fest, ob und in welcher Form die gemeinsame Registrierung durchgeführt werden soll.

Falls Sie den Agent Updater beim Registrieren des Agent Controllers xref:#one_registration[mit registrieren] möchten, wählen Sie zwischen den ersten beiden Werten:

* Mit [.guihint]#On first agent controller registration# wird die erste Instanz, bei der Sie den Agent Controller registrieren, auch für die Registrierung des Agent Updaters genutzt.
In diesem Szenario können Sie also später den Host auf einer anderen {CMK}-Instanz registrieren, ohne die Registrierung des Agent Updaters zu überschreiben.
* Mit [.guihint]#On every agent controller registration# wird die jeweils letzte Instanz, bei der der Agent Controller registriert wurde, auch für die Registrierung des Agent Updaters genutzt.

Wenn Sie den Agent Updater lieber grundsätzlich xref:#register_separate[separat vom Agent Controller] registrieren möchten, wählen Sie den letzten Wert, [.guihint]#Manual#.
Mit dieser Einstellung haben Sie trotzdem die Möglichkeit, auf einzelnen Hosts die Registrierung des Agent Updaters zusammen mit der Registrierung des Agent Controllers durchzuführen.

ifdef::onprem[]
.Agent Updater zusammen mit dem Agent Controller oder separat registrieren?
[%collapsible]
====
In der Regel ist die gleichzeitige Registrierung der bequemere Weg.
Die separate Registrierung steht zusätzlich zur Verfügung, falls Sie z.B. wegen Änderungen in der Zuständigkeit verschiedener Instanzen im xref:#central_setup[verteilten Monitoring] den Agent Updater einzeln bei einer anderen Instanz registrieren möchten, ohne die Registrierung des Agent Controllers zu verändern.
====
endif::[]
ifdef::saas[]
Wenn Sie den Agent Controller bereits registriert haben, beispielsweise via xref:hosts_autoregister#[Autoregistrierung], müssen Sie nur noch den Agent Updater einzeln registrieren.
endif::[]

ifdef::onprem[]
.Nachdem Sie alle Einstellungen in der Regel nach Bedarf angepasst haben, sieht die ausgefüllte Regel zum Beispiel so aus wie hier
image::agent_deployment_agent_updater.png[alt="Die ausgefüllte Regel zur Konfiguration des Agent-Updater-Plugins."]
endif::[]
ifdef::saas[]
.Nachdem Sie alle Einstellungen in der Regel nach Bedarf angepasst haben, sieht die ausgefüllte Regel zum Beispiel so aus wie hier. Die abgebildete Verwendung von IP-Adresse statt Host-Name ist nur bei On-Premises-Editionen von {CMK} sinnvoll.
image::agent_deployment_agent_updater.png[alt="Die ausgefüllte Regel zur Konfiguration des Agent-Updater-Plugins."]
endif::[]
////
// end translation
// delete from here
After this last setting, your rule could look like the following screenshot:

ifdef::onprem[]
image::agent_deployment_agent_updater.png[alt="The completed rule for configuring the agent updater plug-in."]
endif::[]
ifdef::saas[]
.The depicted use of an IP address instead of the host name only makes sense for on-premises editions of {CMK}.
image::agent_deployment_agent_updater.png[alt="The completed rule for configuring the agent updater plug-in."]
endif::[]
// delete to here

Save all your entries by clicking on icon:icon_save[alt="Icon for saving."] [.guihint]#Save#.


[#bakery]
=== Baking and signing agents

Next, you can immediately bake and sign the agents configured in this way in a single action.
This is because the newly created and customized rules will not be found in the installation packages until you have created/baked them again.

To do this, click the [.guihint]#Baked agents# entry on the [.guihint]#Automatic agent updates# page, and then click [.guihint]#Bake and sign agents#.
You must now enter the passphrase for the signature key.
After you click [.guihint]#Bake and sign#, the baking procedure will start as a background process.
When this process has completed, you will be informed:

image::agent_deployment_agent_baking_successful.png[alt="Success message about baking the agent packages."]

[#sign_agent]
All agents signed in this way will be assigned to a corresponding icon:icon_user_locked[alt="Icon of a signature key."] symbol.
If you have created multiple keys, sign with these additional keys separately.

It is sufficient for the agent updater on the host to be monitored if the new package has been signed with one of the keys known to the updater.

// This block is new
// start translation
////

Die Eigenschaften jedes verfügbaren gebackenen Agentenpakets erscheinen in der Übersicht der Agent Bakery.
Hier können Sie beispielsweise prüfen, ob das Updater-Plugin im Agentenpaket enthalten ist ([.guihint]#Deploy plug-in that updates agent automatically#).

Der Screenshot zeigt den linken Teil der Übersichtstabelle.
Rechts davon (hier nicht gezeigt) befinden sich die Symbole zum Herunterladen dieses Agentenpakets für verschiedene Zielsysteme.
Außerdem erscheint in der Tabelle noch der Hash für das Agentenpaket.
Damit können Sie auf Hosts installierte Agenten zu Einträgen in der Agent Bakery zuordnen und so nachvollziehen, welche Eigenschaften für genau diesen Agenten konfiguriert wurden.

.Dieser gebackene und signierte Agent enthält das Agent-Updater-Plugin mit der Registrierungs-Einstellung _On first Agent Controller registration_
image::agent_deployment_baked_agent_properties.png[alt="Eigenschaften eines gebackenen Agenten in der Agent-Bakery-Übersicht."]
////
// end translation


[#register_agent]
=== Registering agent updater

In the next step register the hosts to be monitored on the {CMK} server.
Since a new host is not yet trusted by the {CMK} server, and the server does not yet know that the host should be updated automatically, the agent must be installed manually -- once-only -- on the host.

To do this, first go to [.guihint]#Setup > Agents > Windows, Linux, Solaris, AIX# and download the appropriate package for the host from the xref:wato_monitoringagents#bakery_download[Agent Bakery].
// This block replaces the following text
// start translation
////
Achten Sie darauf, dass das Paket auch wirklich das Agent-Updater-Plugin xref:bakery[enthält].

Sowohl der Agent Controller selbst als auch der Agent Updater müssen jeweils einmalig bei Ihrer {CMK}-Instanz registriert werden.
Die nötigen Schritte für die Umsetzung werden in den folgenden Abschnitten erklärt.


[#about_registration]
.Zweck der Registrierung
[%collapsible]
====
Die Registrierung dient dazu, das Vertrauensverhältnis zwischen Host und {CMK}-Server herzustellen.
Der Agent Updater auf einem Host darf nach der Registrierung jederzeit *seinen eigenen* Agenten ohne Passwort vom Server herunterladen.
Ein Herunterladen von Agenten anderer Hosts ist nicht möglich (da diese vertrauliche Daten enthalten könnten).

Damit der {CMK}-Server weiß, welcher Agent für welchen Host bereitgestellt werden muss, vereinbaren Agent und Server bei der Registrierung einen geheimen Schlüssel (_Host Secret_).
Dieser Schlüssel wird zusammen mit dem Namen des Hosts, wie er im Monitoring bekannt ist, lokal auf dem {CMK}-Server gespeichert.

ifdef::onprem[]
Diese Signatur stellt die Authentizität des Agenten unabhängig vom gewählten Protokoll sicher.
Dennoch ist der empfohlene Weg die xref:#https_handling[Verwendung von HTTPS], da HTTP keine Verschlüsselung der Übertragung bietet.
Der Agent kann in Skripten und Konfigurationsdateien Passwörter enthalten.
endif::[]
====
////
// end translation
// delete from here
Make sure that the package really contains the agent updater plug-in.
// delete to here

// This block replaces the following text
// start translation
////
[#user]
==== Benutzer für die Registrierung auswählen

Bei der Registrierung des Agent Controllers und des Agent Updaters müssen Sie die Zugangsdaten eines {CMK}-Benutzers mit den passenden Berechtigungen angeben.
Hierfür bietet sich der standardmäßig vorhandene Benutzer `agent_registration` an.
ifdef::onprem[]
Sie können aber auch einen anderen Benutzer verwenden, vorausgesetzt, er verfügt über mindestens die folgenden Berechtigungen:

* [.guihint]#Register any existing host#
* [.guihint]#Register managed existing host#
* [.guihint]#Use the GUI at all#

Seit {CMK} {v25} hat der Benutzer `agent_registration` alle Berechtigungen, die für die Registrierung des Agent Controllers und des Agent Updaters benötigt werden.
endif::[]

Neben dem Benutzernamen benötigen Sie auch das dazugehörige [.guihint]#Automation secret#.
Informationen zur Konfiguration des Automationsbenutzers und des dazugehörigen Automationspassworts finden Sie im Handbuchartikel xref:wato_user#automation[über Benutzer und Berechtigungen].

Das Automationspasswort wird nur einmalig für die Registrierung benötigt.
Nach erfolgter Registrierung weist der Host sich beim {CMK}-Server mit dem _Host Secret_ aus, das bei der Registrierung automatisch vereinbart wurde.

Bei der Registrierung müssen Sie auch den Namen des Hosts angeben, wie er im Monitoring bekannt ist.
Beachten Sie, dass dieser Name nicht unbedingt mit dem Host-Namen des Rechners selbst identisch ist.

In den folgenden Abschnitten wird die Registrierung mit dem Benutzer `agent_registration` gezeigt.
Beispielhaft wird hier der Host `myhost` beim {CMK}-Server `myserver.example.com` für die {CMK}-Instanz `mysite` registriert.

Beim Konfigurieren des Update-Plugins haben Sie sich bereits entschieden, ob Sie die xref:registration[Registrierung des Agent Updaters] gleichzeitig mit der Registrierung des Agent Controllers ausführen möchten oder die beiden Registrierungen separat erledigen.
Abhängig von der gewählten Konfiguration geht es jetzt weiter mit dem Abschnitt zur xref:one_registration[gleichzeitigen Registrierung] oder mit dem Abschnitt zur xref:register_separate[separaten Registrierung].

[#one_registration]
==== Gleichzeitige Registrierung des Agent Controllers und des Agent Updaters

Wie der Aufruf für die gleichzeitige Registrierung aussieht, hängt davon ab, welchen Wert Sie bei der Einstellung [.guihint]#Registration# in der Regel [.guihint]#Agent updater (Linux, Windows, Solaris)# xref:#registration[ausgewählt haben].
Die beiden Optionen [.guihint]#On first agent controller registration# und [.guihint]#On every agent controller registration# sorgen dafür, dass die Registrierung des Agent Updaters bei der Registrierung des Agent Controllers automatisch mit durchgeführt wird.
Der Unterschied zwischen den beiden Optionen liegt darin, ob die Registrierung des Agent Updaters überschrieben wird, wenn Sie den Agent Controller an mehreren Instanzen registrieren.

Falls Sie den Wert [.guihint]#Manual# ausgewählt haben, können Sie trotzdem für einzelne Hosts den Agent Updater mit registrieren, während Sie die Registrierung des Agent Controllers durchführen.
Ergänzen Sie dazu beim Aufruf die Option `--automatic-updates`.


Die folgenden Aufrufe zeigen ein Szenario, in dem die gleichzeitige Registrierung über die Option [.guihint]#Registration# in der Regel [.guihint]#Agent updater (Linux, Windows, Solaris)# konfiguriert wurde.
Daher wird hier die Option `--automatic-updates` bei der Registrierung _nicht_ benötigt.
Sie führen einfach den Befehl für die xref:agent_linux#register_host[Registrierung des Agent Controllers] aus -- die zusätzliche Registrierung des Agent Updaters ist ja ins Agentenpaket eingebacken und muss beim Aufruf nicht explizit benannt werden:

[.tabs]
====
[#linux_register_once]#Linux#::
+
--
[{shell},highlight=30-31]
----
{c-root} cmk-agent-ctl register \ 
    -s myserver.example.com \
    -i mysite \
    -H myhost \
    -U agent_registration
Attempting to register at myserver.example.com, port 8017. Server certificate details:

PEM-encoded certificate:
-----BEGIN CERTIFICATE-----
MIIFkDCCA3igAwIBAgIUWlP10RwdFF93PtHeU4BAmA/OkXowDQYJKoZIhvcNAQEN
...
U9qelb7ICObY8Ikk+8QX8pmfYeWKT7YHvN27r5JPKsEWiGV3
-----END CERTIFICATE-----

Issued by:
	Site 'mysite' local CA
Issued to:
	mysite
Validity:
	From Tue, 03 Mar 2026 13:44:51 +0000
	To   Mon, 03 Mar 2036 13:44:51 +0000

Do you want to establish this connection? [Y/n]
pass:[>] Y

Password for Checkmk user 'agent_registration':
pass:[>] 

Registration complete.
Attempting to register Agent Updater...
Agent Updater registration complete.
----
--
[#windows_register_once]#Windows#::
+
--
[{cmd},highlight=30-31]
----
PS C:\Windows\system32> & 'C:\Program Files (x86)\checkmk\service\cmk-agent-ctl.exe' register ^
    -s myserver.example.com ^
    -i mysite ^
    -H mywindowshost ^
    -U agent_registration ^
Attempting to register at myserver.example.com, port 8000. Server certificate details:

PEM-encoded certificate:
-----BEGIN CERTIFICATE-----
MIIFijCCA3KgAwIBAgIUaJdZuULIWevTdrW/UjVk8mFWio8wDQYJKoZIhvcNAQEN
...
Ayk0aiwc7ylx3cyz9KUyf0fdKaagz2ZLzofuTdjG
-----END CERTIFICATE-----

Issued by:
        Site 'mysite' local CA
Issued to:
        mysite
Validity:
        From Wed, 11 Mar 2026 11:56:18 +0000
        To   Tue, 11 Mar 2036 11:56:18 +0000

Do you want to establish this connection? [Y/n]
pass:[>] Y

Password for Checkmk user 'agent_registration':
pass:[>] 

Registration complete.
Attempting to register Agent Updater...
Agent Updater registration complete.
----
--
====

Die Ausgabe bei der Registrierung ähnelt der Ausgabe bei der Registrierung des Agent Controllers ohne Agent Updater.
Die hervorgehobenen Zeilen am Ende informieren Sie darüber, dass die Registrierung des Agent Updaters ebenfalls gestartet und erfolgreich durchgeführt wurde.

Um die Ergebnisse der Registrierung zu überprüfen, können Sie beispielsweise die lokale Konfiguration des Agent Updaters auf dem Host xref:#show_config[einsehen].

Sie können nun die Verteilung von automatischen Updates in Ihrer {CMK}-Instanz xref:#master_switch[aktivieren].

[#register_separate]
==== Separate Registrierung des Agent Updaters

Sie können den Agent Updater separat vom Agent Controller registrieren.
Die Voraussetzung dafür ist, dass ein gebackenes und signiertes Agentenpaket auf dem Host vorhanden ist, das die folgenden Eigenschaften hat:

* Es enthält das Agent-Updater-Plugin -- Sie wollen schließlich nur die Registrierung manuell ausführen, nicht das regelmäßige Aktualisieren des Agenten.
* Es enthält für die Einstellung xref:#registration[[.guihint]#Registration#] in der Regel Regel [.guihint]#Agent updater (Linux, Windows, Solaris)# den Wert [.guihint]#Manual#.

Wenn Sie die in {CE} verfügbare Autoregistrierung nutzen, liefern diejenigen Agentenpakete, die dafür konfiguriert sind, _immer_ das Updater-Plugin mit aus, und zwar mit der Einstellung [.guihint]#Manual# für die Registrierung des Updaters.

ifdef::saas[]
Das trifft auch auf die vorkonfigurierten Agentenpakete zu, die in {CE} von Anfang an für die automatische Erstellung von Hosts bereitstehen.
endif::[]

In der Agentenbäckerei können Sie die Eigenschaften eines gebackenen Agentenpakets xref:bakery[einsehen] und so sicherstellen, dass die oben genannten Bedingungen erfüllt sind.

Für das hier beschriebene Szenario wird angenommen, dass der Host bereits im Monitoring bekannt ist und der Agent Controller auf dem Host bereits in Ihrer Instanz registriert ist.

Installieren Sie das heruntergeladene Agentenpaket.
Die Registrierung des Agent Controllers bleibt erhalten und zusätzlich ist nun das Agent-Updater-Plugin installiert -- der Agent Updater ist aber noch nicht registriert.
Sie können das überprüfen, indem Sie die lokale Konfiguration des Agent Updaters auf dem Host xref:#show_config[einsehen].
Auf einem Linux-Host enthält die Datei `cmk-update-agent.state` zu diesem Zeitpunkt nur eine einzige Information:

./var/lib/check_mk_agent/cmk-update-agent/cmk-update-agent.state
[{file}]
----
{'last_error': 'The agent updater is not registered at the deployment server'}
----

Nach der Installation finden Sie das Agent-Updater-Plugin im Verzeichnis `/usr/lib/check_mk_agent/plugins/3600/cmk-update-agent`.
Das Unterverzeichnis `3600` steht dabei für das xref:interval_for_update_check[Intervall für den Update-Check] in Sekunden (hier für den Standardwert von einer Stunde).
Ein gleichnamiges Skript wird auch unter `/usr/bin/` abgelegt, daher steht `cmk-update-agent` auch als Befehl zur Verfügung.
Mit `cmk-update-agent --help` erhalten Sie einen Überblick über alle verfügbaren Optionen und Modi.

Sie können die manuelle Registrierung des Agent Updaters ab jetzt jederzeit durchführen.

Wenn Sie `cmk-update-agent register` ohne weitere Argumente eingeben, wird der interaktive Modus des Agent Updaters gestartet.
Sie werden zur Eingabe von Werten für verschiedene Felder aufgefordert, die in noch keiner Konfigurationsdatei vorhanden sind.
Etwas mehr Kontrolle haben Sie im nicht-interaktiven Modus, da Sie darin alle in der Hilfe angezeigten Felder setzen können.
Dabei überschreiben die von Ihnen angegebenen Werte diejenigen Werte, die in der Datei `cmk-update-agent.state` gespeichert sind.
Werte aus der Datei `cmk-update-agent.cfg` werden allerdings auch bei einem solchen Befehlsaufruf nicht überschrieben.
Mehr Details dazu gibt es weiter unten unter xref:show_config[Einsehen der lokalen Konfiguration].

Im hier vorbereiteten Szenario enthält die Datei `cmk-update-agent.state` noch keine hilfreichen Informationen für die Registrierung.
Die Code-Beispiele unten zeigen den interaktiven Modus, in dem jede notwendige Information von Ihnen eingegeben wird.
Welche Eingaben im Einzelfall nötig sind, hängt davon ab, welche Informationen Sie im Schritt xref:#configure_update_plugin[Konfiguration des Update-Plugins] bereits vorgegeben haben.

[.tabs]
====
Linux::
+
--
[#register_linux]

[{shell}]
----
{c-root} cmk-update-agent register -v
pass:[+]-------------------------------------------------------------------pass:[+]
|                                                                   |
|  Checkmk Agent Updater v{current} - Registration                   |
|                                                                   |
|  Activation of automatic agent updates. Your first step is to     |
|  register this host at your deployment server for agent updates.  |
|  For this step you need a user with the permission                |
|  "Register all hosts" on that Checkmk site.                       |
|                                                                   |
pass:[+]-------------------------------------------------------------------pass:[+]
Deployment server to connect to: 
myserver.example.com

Auto-detected protocol: https
Checkmk site on deployment server: 
mysite

Our host name in the monitoring: 
myhost

Checkmk user with registration permissions: 
agent_registration

Password for Checkmk user: 


Going to register agent at deployment server
Applying new update URL  from deployment server
Successfully registered agent of host "myhost" for deployment.
You can now update your agent by running 'cmk-update-agent -v'
Saved your registration settings to /var/lib/check_mk_agent/cmk-update-agent/cmk-update-agent.state.

Hint: you can do this in scripts with the command:

cmk-update-agent register -s myserver.example.com -i mysite -H myhost -p https -U agent_registration -P ************ -v
----

--

Windows::
+
--
[#register_windows]

Nach der Installation des Agentenpakets ist der Agent Updater in den Windows-Agenten `C:\Program Files (x86)\checkmk\service\check_mk_agent.exe` integriert.
Dem Updater selbst können Sie dann immer mit `check_mk_agent.exe updater` Befehle erteilen.

Der unten gezeigte Aufruf startet den interaktiven Modus.
Unter Windows muss dies in einer Eingabeaufforderung mit Administratorrechten geschehen.

[{cmd}]
[.copybutton]
----
C:\WINDOWS\system32> & 'C:\Program Files (x86)\checkmk\service\check_mk_agent.exe' updater register
pass:[+]-------------------------------------------------------------------pass:[+]
|                                                                   |
|  Checkmk Agent Updater v{current} - Registration                   |
|                                                                   |
|  Activation of automatic agent updates. Your first step is to     |
|  register this host at your deployment server for agent updates.  |
|  For this step you need a user with the permission                |
|  "Register all hosts" on that Checkmk site.                       |
|                                                                   |
pass:[+]-------------------------------------------------------------------pass:[+]
Using previous settings from C:\ProgramData\checkmk\agent\config\cmk-update-agent.state.
Deployment server to connect to:
myserver.example.com

Checkmk site on deployment server:
mysite

Our host name in the monitoring:
mywindowshost

Checkmk user with registration permissions:
agent_registration

Password for Checkmk user:


Successfully registered agent of host "mywindowshost" for deployment.
----


Alternativ können Sie die Registrierung auch im nicht-interaktiven Modus durchführen, indem die benötigten Daten per Befehlszeile übergeben werden.
Eine Übersicht über die möglichen Optionen erhalten Sie mit `"C:\Program Files (x86)\checkmk\service\check_mk_agent.exe" updater --help`.

Der komplette Befehl zur Registrierung sieht wie folgt aus:

++++
<div class="listingblock copybutton">
    <div class="content">
        <pre class="pygments highlight"><code data-lang="doscon"><span></span><span class="tok-gp">C:\WINDOWS\system32&gt;</span> <span class="tok-s2">"C:\Program Files (x86)\checkmk\service\check_mk_agent.exe"</span> updater register ^
-s myserver.example.com ^
-i mysite ^
-H mywindowshost ^
-U agent_registration ^
-v</code></pre>
    </div>
</div>
++++

--
====

Nach erfolgter Registrierung des Agent Updaters geht es weiter mit dem xref:#master_switch[Master switch], mit dem Sie in Ihrer Instanz die automatische Verteilung von Updates aktivieren.
////
// end translation
// delete from here
[#user]
==== Selecting a user for registration

Registration must be performed by a {CMK} user who has the necessary permissions.

[TIP]
====
Note that the `agent_registration` automation user automatically created by {CMK} only has permissions for registering TLS-encrypted data transmission and therefore cannot be used for registering the agent updater.
====

ifdef::saas[]
The xref:glossar#automation_user[automation user] `api_user`, which exists in every site, is suitable for registering the agent updater.
This user is assigned the administrator role, which means that they have _all_ permissions.
On the [.guihint]#Setup > Users > Users# page, you can open the properties of the `api_user` user and generate an automation password under [.guihint]#Automation secret for machine accounts#, which is then copied to the clipboard.
You will be asked for this automation password during registration.
endif::[]

ifdef::onprem[]
You can perform the registration as an administrator, i.e., with a user who has been assigned the administrator role, since an administrator has all permissions.
However, if for security reasons you need to use a user who is only allowed to register an agent updater, you can also do so.
You must create such a user manually, as {CMK} does not currently create it automatically.
To do this, proceed as follows:

First, create a new role on the [.guihint]#Setup > Users > Roles & permissions# page by cloning the existing `no_permissions` role.
Edit the new role by giving it descriptive names in [.guihint]#Internal ID# and [.guihint]#Alias# and assigning only the following permissions to the role.
The quickest way to find these is to search for them using [.guihint]#Find on this page# in the long list on the permissions page:

. [.guihint]#Register host & download monitoring agents of your hosts#.
. [.guihint]#Register all hosts & download all monitoring agents#.
. [.guihint]#Use the GUI at all#

Then, on the [.guihint]#Setup > Users > Users# page, create a new user with a descriptive [.guihint]#Username# (e.g., `agent_updater_registration`) and assign only the newly-created role to this user.
Click [.guihint]#Automation secret for machine accounts# to create the new user as an xref:glossar#automation_user[automation user].
You will be prompted for the corresponding automation password during registration.

The following sections show the registration with an automation user `agent_updater_registration` created in this way.
endif::[]


[#register_do]
==== Completing registration

[.tabs]
====
Linux::
+
--
[#register_linux]
Now copy the agent package to the host and install it with `rpm` or `dpkg` (xref:agent_linux#install_package[Linux package installation]).

After installation, you will find the agent updater plug-in in the directory `/usr/lib/check_mk_agent/plugins/3600/cmk-update-agent`.
The subdirectory's value of `3600` indicates the xref:interval_for_update_check[interval for the update check] in seconds (here for the default value of one hour).
A script of the same name is also stored under `/usr/bin/`, so that `cmk-update-agent` is also available as a command.

Now call the agent updater with the `register` argument.
Enter the required information in sequence.

With the following command, you now register the agent updater from a *Linux host*:

ifdef::onprem[]
[{shell}]
----
{c-root} cmk-update-agent register -v
pass:[+]-------------------------------------------------------------------pass:[+]
|                                                                   |
|  Checkmk Agent Updater v{current} - Registration                   |
|                                                                   |
|  Activation of automatic agent updates. Your first step is to     |
|  register this host at your deployment server for agent updates.  |
|  For this step you need a user with the permission                |
|  "Register all hosts" on that Checkmk site.                       |
|                                                                   |
pass:[+]-------------------------------------------------------------------pass:[+]
Our host name in the monitoring:
myhost

User with registration permissions:
agent_updater_registration

Password:


Going to register agent at deployment server
Applying new update URL from deployment server
Successfully registered agent of host "myhost" for deployment.
Note: The host "myhost" is currently not known in the active monitoring configuration.
You can however add this host to the monitoring later on without having to register again.
Please check the exact spelling if you intended to register an existing host.
You can now update your agent by running 'cmk-update-agent -v'
Saved your registration settings to /var/lib/check_mk_agent/cmk-update-agent.state.

Hint: you can do this in scripts with the command:

cmk-update-agent register -s myserver.example.com -i mysite -H myhost -p https - U agent_updater_registration -P pass:[************] -v
----
endif::[]

ifdef::saas[]
[{shell}]
----
{c-root} cmk-update-agent register -v
pass:[+]-------------------------------------------------------------------pass:[+]
|                                                                   |
|  Checkmk Agent Updater v{current} - Registration                   |
|                                                                   |
|  Activation of automatic agent updates. Your first step is to     |
|  register this host at your deployment server for agent updates.  |
|  For this step you need a user with the permission                |
|  "Register all hosts" on that Checkmk site.                       |
|                                                                   |
pass:[+]-------------------------------------------------------------------pass:[+]
Our host name in the monitoring:
myhost

User with registration permissions:
api_user

Password:


Going to register agent at deployment server
Applying new update URL from deployment server
Successfully registered agent of host "myhost" for deployment.
You can now update your agent by running 'cmk-update-agent -v'
Saved your registration settings to /var/lib/check_mk_agent/cmk-update-agent.state.

Hint: you can do this in scripts with the command:

cmk-update-agent register -s myserver.example.com -i mysite -H myhost -p https - U api_user -P pass:[************] -v
----
endif::[]

Alternatively, you can perform the registration in non-interactive mode by entering the required data via the command line options.
A call to the `cmk-update-agent register --help` here shows the settable options.
--

Windows::
+
--
[#register_windows]
Now copy the agent package to the host and install it with `msiexec` (xref:agent_windows#install_package[Windows package installation]).

After installation, the agent updater is integrated into the Windows agent `C:\Program Files (x86)\checkmk\service\check_mk_agent.exe`.
The updater itself can be controlled with the `check_mk_agent.exe updater` command.

Now call the agent updater with the `register` argument.
Under Windows this must be done in a command prompt with administrator rights.
Enter the required information in sequence.

As the agent updater for *Windows hosts* is integrated into the agent itself, the command for registering it looks like this:

ifdef::onprem[]
[{cmd}]
[.copybutton]
----
C:\WINDOWS\system32> "C:\Program Files (x86)\checkmk\service\check_mk_agent.exe" updater register
Using previous settings from C:\ProgramData\checkmk\agent\config\cmk-update-agent.state.
Our host name in the monitoring:
mywindowshost

User with registration permissions:
agent_updater_registration

Password:


Successfully registered agent of host "mywindowshost" for deployment.
----
endif::[]

ifdef::saas[]
[{cmd}]
[.copybutton]
----
C:\WINDOWS\system32> "C:\Program Files (x86)\checkmk\service\check_mk_agent.exe" updater register
Using previous settings from C:\ProgramData\checkmk\agent\config\cmk-update-agent.state.
Our host name in the monitoring:
mywindowshost

User with registration permissions:
api_user

Password:


Successfully registered agent of host "mywindowshost" for deployment.
----
endif::[]

Alternatively, you can perform the registration in non-interactive mode by entering the required data via the command line options.
The complete command for registration looks like this:

ifdef::onprem[]
++++
<div class="listingblock copybutton">
    <div class="content">
        <pre class="pygments highlight"><code data-lang="doscon"><span></span><span class="tok-gp">C:\WINDOWS\system32&gt;</span> <span class="tok-s2">"C:\Program Files (x86)\checkmk\service\check_mk_agent.exe"</span> updater register ^
-s myserver.example.com ^
-i mysite ^
-H mywindowshost ^
-p https ^
-U agent_updater_registration ^
-S mysecret ^
-v</code></pre>
    </div>
</div>
++++
endif::[]

ifdef::saas[]
++++
<div class="listingblock copybutton">
    <div class="content">
        <pre class="pygments highlight"><code data-lang="doscon"><span></span><span class="tok-gp">C:\WINDOWS\system32&gt;</span> <span class="tok-s2">"C:\Program Files (x86)\checkmk\service\check_mk_agent.exe"</span> updater register ^
-s myserver.example.com ^
-i mysite ^
-H mywindowshost ^
-p https ^
-U api_user ^
-S mysecret ^
-v</code></pre>
    </div>
</div>
++++
endif::[]

The `-S` option is used to pass the automation password of the automation user.
--
====


==== General information about registration

* When registering, the plug-in also needs the name of the host as it is known in the monitoring.
This is not necessarily identical to the computer's host name.
The host name is then stored locally together with the key.

ifdef::onprem[]
* To use HTTPS, HTTPS must also be set up on your monitoring server.
HTTP is much easier here, but does not provide encryption of the transmission.
Since the agent can theoretically contain passwords, HTTPS is the recommended method.
The authenticity of the agent is however ensured independently by the signature.
endif::[]

* Registration is only required once.
On registration the agent and the server agree a secret key (_host secret_) known only to this host.
The password you enter is not stored anywhere.

* While the interactive mode only polls fields that are not yet in any configuration file, the non-interactive mode allows all fields displayed in the help to be set and has the highest priority for this call.
Options that are saved only in `cmk-update-agent.state` will be overwritten -- options from `cmk-update-agent.cfg`, however, won't.
More details on this can be found below at xref:show_config[Viewing the local configuration].

Following a successful registration the host secret is stored at the agent in the `/etc/cmk-update-agent.state` file.
ifdef::onprem[]
On the server it is located in `~/var/check_mk/agent_deployment/myhost`.
endif::[]
From now on the host secret allows the host to *download its own agent* from the server without requiring a password.
It is not possible to download agents from other hosts, since these could contain confidential data.
// delete to here

[#master_switch]
=== Master switch
// This block replaces the following text
// start translation
////
Ihre Hosts, die Sie mit dem Agentenpaket und dem Agent Updater ausgestattet haben, fragen ab jetzt im vorgegebenen Intervall bei der ihnen zugeordneten {CMK}-Instanz nach, ob Agenten-Updates verfügbar sind.
Die automatische Verteilung von Agenten ist in Ihrer Instanz zunächst standardmäßig global deaktiviert.
////
// end translation
// delete from here
The automatic deployment of agents is by default globally disabled.
// delete to here
Finally, activate it by clicking icon:icon_edit[alt="Icon for editing."] [.guihint]#Master switch#.
This changes the [.guihint]#Activation of automatic agent updates# global setting under [.guihint]#Setup > General > Global Settings > Automatic Agent Updates#.

All conditions should should now have been fulfilled.
The [.guihint]#Prerequisites# table should now look like this:

image::agent_deployment_prerequisites_fulfilled.png[alt="Automatic agent updates with the prerequisites met."]

// This block replaces the following text
// start translation
////
Ab sofort wird sich nun der Agent Updater jeweils zum Ende des konfigurierten Update-Intervalls melden und nach einer neuen Version des Agenten Ausschau halten.
Sobald diese bereitsteht _und signiert ist_, wird er sie automatisch herunterladen und installieren.

Gleichzeitig ist der [.guihint]#Master switch# eine Möglichkeit, die Updates xref:#deactivate_automatic_updates[global abzuschalten].

Um zu prüfen, ob die Updates wie gewünscht verteilt werden, können Sie nun zum Abschnitt zur xref:#diagnosis[Diagnose] springen.

[#deactivate_automatic_updates]
== Automatische Updates überspringen oder abschalten

Der Agent Updater wird nach der Einrichtung im konfigurierten Intervall jeden neuen passenden Agenten aus der Agentenbäckerei herunterladen und ihn installieren.
Möchten Sie die automatische Verteilung von Updates für einen, mehrere oder alle Hosts vorübergehend oder permanent deaktivieren, haben Sie die folgenden Möglichkeiten.

=== Agent-Updater-Plugin vollständig von einem Host entfernen

Um den Agent Updater vollständig von einem konkreten Host zu entfernen, passen Sie über den Regelsatz [.guihint]#Agent updater (Linux, Windows, Solaris)# die Einstellung für den Host so an, dass das Update-Plugin dort deaktiviert ist.
Beim nächsten regelmäßigen Update entfernt der Agent seinen Updater dann selbst!

Es versteht sich von selbst, dass das Update dann nur durch die manuelle Installation eines neuen Agentenpakets erneut aktiviert werden kann.
Die Registrierung bleibt aber erhalten und muss nicht erneuert werden.

[#host_selection]
=== Verteilung von Updates auf bestimmte Hosts beschränken

Um in Ihrer Instanz die Verteilung von Agenten-Updates auf bestimmte Hosts einzuschränken, können Sie Bedingungen für die Verteilung konfigurieren.
Das kann zum Beispiel nützlich sein, um einen neuen Agenten zuerst mit einer kleineren Anzahl von Hosts auszuprobieren, bevor Sie ihn auf eine größere Zahl von Hosts ausrollen.
Dieser wichtige Schritt verhindert, dass ein möglicher Fehler große Ausmaße annimmt.

Dazu dient der mittlere Kasten auf der Seite [.guihint]#Automatic agent updates#:

image::agent_deployment_host_selection.png[]

Die Bedingungen werden dabei immer mit einer Konjunktion (_logisches UND_) verknüpft: Nur Hosts, die _alle_ ausgewählten Bedingungen erfüllen, erhalten das Update. 
Nachdem Sie hier Bedingungen für die Auswahl von Hosts festgelegt haben, können Sie in dem Feld [.guihint]#Test hostname before activation# einzelne Host-Namen eintippen und kontrollieren, ob die Updates für diese Hosts nun aktiviert sind oder nicht.


Sie finden die Seite für die Einstellung der Bedingungen auch über [.guihint]#Setup > Global settings > Automatic agent updates > Selection of hosts to activate agent updates for#.

Um alle Bedingungen zu löschen und damit die Verteilung von Updates an alle Hosts wieder zu aktivieren, nutzen Sie auf der Seite [.guihint]#Setup > Global settings > Automatic agent updates > Selection of hosts to activate agent updates for# die Option [.guihint]#Reset to default#.

=== Master switch deaktivieren

Um direkt in Ihrer Instanz die gesamte Verteilung von Agenten-Updates zu stoppen, deaktivieren Sie einfach den xref:#master_switch[[.guihint]#Master switch#].
Alle Einstellungen, die Sie in der Instanz und auf den Hosts vorgenommen haben, bleiben erhalten.

Sie können den [.guihint]#Master switch# jederzeit wieder aktivieren.
////
// end translation
// delete from here
From now on, the agent will report at the end of each configured update interval and look for a new version of the agent.
If a new version is ready -- _and signed_ -- it will be downloaded and installed automatically.

At the same time, the [.guihint]#Master switch# is also one way to deactivate the update process globally.

A step-by-step guide is also provided by the video which originated at the {CMK} Conference #3 (2017), under the following link.
This is not the latest version -- however the basic procedure has not changed:
link:https://youtu.be/S7TNo2YcGpM?t=767[The new automatic agent updates^]


=== Disabling automatic updates for a host

If a host is to be removed from the automatic updates, use the [.guihint]#Agent updater (Linux, Windows, Solaris)# rule set to adjust its setting so that the update plug-in is deactivated there.
The next time a regular update is performed, the agent will remove its own updater!

It goes without saying that the update can subsequently only be reactivated by manually installing a new agent package.
However, the registration remains intact and does not need to be renewed.


[#host_selection]
== Restricting updates to specific hosts

Before rolling out a new agent to a large number of hosts, you will certainly want to first try it out with a smaller number of hosts.
This important step prevents a possible mistake of serious dimensions.

For this function, use the middle box on the [.guihint]#Automatic agent updates# page:

image::agent_deployment_host_selection.png[]

A logical conjunction (_AND_) is applied to the conditions: Only hosts that match _all_ selected criteria will receive the update.
After you have stated the conditions for selecting hosts here, you can use the [.guihint]#Test hostname before activation# field to enter individual host names and check if the updates for these hosts have been enabled or not.
 
*Important:* On hosts that are not yet to be provided with automatic updates, the installed package must not contain the agent updater plug-in
-- otherwise the plug-in will regularly warn you that the host has not yet been registered.
// delete to here

[#diagnosis]
== Diagnoses

There are quite a few sources of information for diagnosing whether all updates work as intended.


[#statistics]
=== Agent deployment statistics

image::agent_deployment_update_status.png[]

// This block replaces the following text
// start translation
////
Diese Übersicht auf der Seite [.guihint]#Automatic agent updates# zeigt, wie sich die einzelnen Hosts im Agenten-Update verhalten.
////
// end translation
// delete from here
This overview shows how the individual hosts in the agent update behave.
// delete to here
The xref:user_interface#inline_help[inline help] gives further explanations.
Clicking on icon:icon_view_20[] provides a detailed list of the individual hosts.
You can also get to the complete list of all registered hosts via the [.guihint]#Monitor > System > Agent update status# view.
There you can then search for individual hosts.

image::agent_deployment_status_view.png[]

In this list you will also find documentation on how the hash of an agent starts, which agent is intended for a host ([.guihint]#Target Agent#),
which agent was most recently downloaded from the host ([.guihint]#Downloaded Agent#),
and which agent is currently installed on the host ([.guihint]#Installed Agent#).
In this way you can always see if the specifications have been met or where the process is currently located.
// This block replaces the following text
// start translation
////
Die Statusinformationen, die im Screenshot zu sehen sind, entstehen direkt bei der Kommunikation zwischen Agentenbäckerei und Agent Updater.
Rechts vom gezeigten Ausschnitt gibt es außerdem die Felder [.guihint]#Update Check# und [.guihint]#Update Check Output#.
Die Werte dieser Felder kommen vom Agent-Updater-Plugin bei der Abfrage des Agenten des Hosts und werden durch die Zwischenspeicherung (definiert durch das Abfrageintervall) ggf. zu einem anderen Zeitpunkt aktualisiert.
////
// end translation
// delete from here
It should be noted here that the status information appears to the left directly in the communication between the Agent Bakery and agent updater, 
while the [.guihint]#Update Check# and [.guihint]#Update Check Output# fields come from the agent updater plug-in when querying the host's agent, and that due to caching (defined by the query interval) these may be updated at a different time.
// delete to here


[#check_mk_agent]
=== The [.guihint]#Check_MK Agent# service

If you have installed the agent updater plug-in on an agent, it will regularly output the current status of the update in the form of monitoring data.
The service discovery generates a new service with the name [.guihint]#Check_MK Agent# on the host.
This again reflects the current state of the update.
This way you can be notified of any problem with the updates.

image::agent_deployment_agent_check.png[]

// This block replaces the following text
// start translation
////
Unter anderem signalisiert der Zustand des Services, wie der Status des Signaturschlüssels beurteilt wird.
Signaturschlüssel, für die möglicherweise Handlungsbedarf besteht, werden je nach Dringlichkeit mit den Monitoring-Zuständen {WARN} oder {CRIT} abgebildet:
////
// end translation
// delete from here
Among other things, the state of the service signals how the status of the signature key is assessed. The following states are possible:
// delete to here

* {WARN}: The signature key's certificate is corrupt, no longer valid, or will become invalid in the next 90 days.
* {CRIT}: There is no valid certificate for the signature key or the certificate will lose its validity within the next 30 days.


[#show_config]
=== Viewing the local configuration

The behavior of the agent updater is governed by the two files `cmk-update-agent.cfg` and `cmk-update-agent.state`.
It always applies that defined values from the `.cfg` file override those from the `.state` file.
If the agent updater shows unexpected behavior, it is sometimes worth having a look in the configuration.
There is also a handy feature if you call the agent updater directly from the command line:

[{shell}]
----
{c-root} cmk-update-agent show-config
Showing current configuration...

Configuration from agent info file (/usr/lib/check_mk_agent/agent_info.json):
hash: e70741b34c1be776
platform: linux_deb
agent_controller_user: cmk-agent

Configuration from config file (/etc/check_mk/cmk-update-agent.cfg):
certificates: []
ignore_update_url: False
interval: 3600
signature_keys: ['-----BEGIN CERTIFICATE-----\n pass:q[[...]] \n-----END CERTIFICATE-----\n']
use_proxy_env: False

Configuration from state file (/var/lib/check_mk_agent/cmk-update-agent/cmk-update-agent.state):
update_url: 
host_secret: OU4yYR1OMIXOXHCaCArryXm23J0zxF11vpKNspY5MeJo8v6TT6BM1CL4OfbgIvuO
server: myserver.example.com
site: mysite
host_name: myhost
protocol: http
user: agent_registration

----

// This block is new
// start translation
////
Unter Windows lautet der entsprechende Befehl `'C:\Program Files (x86)\checkmk\service\check_mk_agent.exe' updater show-config`.
////
// end translation

[#logs_on_host]
=== Log messages

In the event of a problem you will also find log data for the updates on the host to be monitored.
On Linux `cmk-update-agent` logs important information to syslog -- such as warnings and errors:

./var/log/syslog
[{file}]
----
Jul 02 13:59:23 myhost [cmk-update-agent] WARNING: Missing config file at ./cmk-update-agent.cfg. Configuration may be incomplete.
Jul 02 13:59:23 myhost [cmk-update-agent] ERROR: Not yet registered at deployment server. Please run 'cmk-update-agent register' first.
----

A more detailed log file `cmk-update-agent.log` including debug output and possible tracebacks can be found on Linux and on Windows:

./var/lib/check_mk_agent/cmk-update-agent.log
[{file}]
----
2026-01-16 07:49:23,295 [288229] DEBUG: Starting Checkmk Agent Updater v2.4.0p19
2026-01-16 07:49:23,295 [288229] DEBUG: Updating the certificate store "/var/lib/check_mk_agent/cmk-update-agent/cas/all_certs.pem"...
2026-01-16 07:49:23,297 [288229] INFO: Updated the certificate store "/var/lib/check_mk_agent/cmk-update-agent/cas/all_certs.pem" with 1 certificate(s)
...
2026-01-16 07:49:23,326 [288229] DEBUG: Successfully read /var/lib/check_mk_agent/cmk-update-agent/cmk-update-agent.state.
2026-01-16 07:49:23,326 [288229] DEBUG: Successfully read /var/lib/check_mk_agent/cmk-update-agent/cmk-update-agent.state.
2026-01-16 07:49:23,327 [288229] DEBUG: Saved deployment status to /var/lib/check_mk_agent/cmk-update-agent/cmk-update-agent.state.
2026-01-16 07:49:23,327 [288229] INFO: Target state (from deployment server):
2026-01-16 07:49:23,327 [288229] INFO:   Agent available:     True
2026-01-16 07:49:23,327 [288229] INFO:   Signatures:          1
2026-01-16 07:49:23,327 [288229] INFO:   Target hash:         37221b87f5cb46a2
2026-01-16 07:49:23,327 [288229] INFO: Agent 37221b87f5cb46a2 already installed.
2026-01-16 07:49:23,340 [288229] DEBUG: Caught Exception:
Traceback (most recent call last):
  File "cmk_update_agent.py", line 1733, in main
  File "cmk_update_agent.py", line 714, in run
  File "cmk_update_agent.py", line 1372, in _run_mode
  File "cmk_update_agent.py", line 1071, in _do_update_as_command
  File "cmk_update_agent.py", line 1150, in _do_update_agent
  File "cmk_update_agent.py", line 1221, in _check_signatures
Exception: No valid signature found.
----

Under both systems you can also use the command line option `--logfile LOGFILE` to specify an alternate path for the log file.


ifdef::onprem[]
[#scenarios]
== Application scenarios

// The following block will be obsolete after translation
// delete from here
[#deactivate_automatic_updates]
=== Deactivating automatic host updates

If a host is to be removed from the automatic updates, alter its setting with the [.guihint]#Agent updater (Linux, Windows, Solaris)# rule set so that the update plug-in is deactivated there.
At the next regular update the agent itself then removes its own updater!

It goes without saying that the update can then only be reactivated by the manual installation of a new agent package!
The registration is retained and does not have to be renewed.
// delete to here


[#move_to_new_monitoring_server]
=== Migrating to a new monitoring site

Should you want to move to a new {CMK} site in a single-site setup without losing the hosts registered on the server,
it should be noted that for a successful agent update process the following information on server and host must match:

* The name under which the host is monitored and registered.
* The xref:show_config[host secret], which was automatically assigned during registration.
* The signature used to sign the agents.

To achieve this, follow these steps:

. First add all hosts whose registration information is to be migrated to the new site to the monitoring.
Make sure the hosts in the new site are monitored under the same name.
Then copy the `~/var/check_mk/agent_deployment` folder from the old to the new monitoring site.

. Export the signature key(s) that are accepted by the agents installed on the hosts.
The signature keys can be exported and imported using [.guihint]#Setup > Agents > Windows, Linux, Solaris, AIX > Agents > Signature keys#.

. Import these signature keys from step 2 on the new monitoring site.

. Configure the agent updater rule on the new monitoring site according to the instructions, and sign the baked agents with the imported signature key(s).

. Lastly, in the agent updater rule on the old site, configure the fields for the update server and the name of the {CMK} site conforming to your new monitoring site, and bake the agents again.
*Note*: Please check at this point that you have specified everything correctly _before_ you re-bake the agents.

// This block replaces the following text
// start translation
////
Sobald die nächsten automatischen Updates auf den Hosts durchlaufen, wird die Agentenbäckerei der alten Monitoring-Instanz ausgesperrt.
////
// end translation
// delete from here
As soon as the next automatic updates go through the hosts, the old monitoring site will be locked out.
// delete to here
From that time on the hosts to be monitored will only answer to the new {CMK} server.
Following the second automatic update the agent will be installed by the new {CMK} server accordingly.
endif::[]


ifdef::onprem[]
[#automatic_installer]
=== The agent updater as automatic installer

[IMPORTANT]
====
This is not an official feature of the agent updater.
These instructions are therefore intended primarily for more experienced users.
The official way to install a {CMK} Agent on a host is to download and run the agent package appropriate for the system.
It is however also possible to allow the {CMK} Agent to be installed initially by the agent updater, since this also works as a stand-alone program.
====

Proceed as follows:

. Copy the cmk-update-agent binary or the `cmk_update_agent.py` script to the host to be monitored (both can be found at `~/share/check_mk/agents/plugins` on the {CMK} server).
. Register the host on the {CMK} server by invoking `cmk-update-agent register`.
Here it makes sense to pass the required registration information directly via the command line
– especially if you want to use an installation script.
The corresponding options can be displayed when calling `cmk-update-agent register --help`.
. Then, with a final call to the agent updater plug-in, install the agent with all of the configuration details for the host being monitored.
However since there is no local configuration (the agent updater also displays a corresponding warning), and thus no signature for the agent package to be downloaded, call the updater once more with `cmk-update-agent --skip-signatures` to explicitly trust the downloaded package.
The prerequisite for the installation by agent updater is, of course, that the Agent Bakery has a suitable agent package ready for the target host on the {CMK} server.
endif::[]


ifdef::onprem[]
[#central_setup]
== Agent updates in distributed monitoring

Since {CMK} {v20} it has also been possible to distribute baked agents via remote sites.
The prerequisite for this is a xref:glossar#central_setup[distributed monitoring with central setup]
and a connection which allows the central site to be reached from the remote sites via HTTP/HTTPS.

Such a distributed monitoring can -- especially temporarily -- also be operated with different {CMK} versions.
This makes it possible to successively switch larger systems to a new {CMK} version.
In this case, however, be sure to follow the notes on mixed-version monitoring environments in xref:distributed_monitoring#mixed_versions[distributed monitoring].


[#functionality]
=== Functionality

Technically, the feature is implemented in such a way that requests for updates on remote sites are forwarded to the central site
-- so that the entire configuration as well as the baking of the agents takes place exclusively on the central site.
Agent packages that have already been requested at a remote site are cached on the remote site (as long as they are valid),
so that the packages do not have to be downloaded again from the central site.
In addition, the requested data will be checked for consistency on the remote site, so that unnecessary connections to the central site are avoided.

Unlike in a single-site setup, the appropriate update server for the hosts does not originate exclusively from the set of rules for the agent updater,
but is communicated to the requesting agent updater by the contacted {CMK} site.
In this process, a host is provided with its server by the site from which it is being monitored.
The specification of a {CMK} server is therefore only needed for the one-time registration, which can theoretically take place at any site -- that is accessible from the host -- in the entire distributed monitoring system.
If the connection to the automatically determined server fails,
the previously saved server (from the rule configuration or previously-entered manually during registration) is used as a fallback.


[#configuration_distributed_monitoring]
=== Configuration

The distribution of agent packages via remote sites does not have to be activated separately
-- the respective remote site recognizes the situation automatically and accordingly communicates with the central site as well as the requesting agent updater.
If the agents are to report explicitly only to the central site for updates, this can be done via a fixed update server in the xref:fixed_update_server[rule set for the agent updater].

// This block replaces the following text
// start translation
////
Damit die Agenten-Updates im verteilten Monitoring tatsächlich funktionieren, müssen jedoch auf der Zentralinstanz noch einige Einstellungen vorgenommen werden,
die sich alle in [.guihint]#Setup > General > Global settings# im Kasten mit dem Titel [.guihint]#Automatic agent updates# befinden.
////
// end translation
// delete from here
In order for the agent updates to actually work in a distributed monitoring, however, some settings must be made on the central site,
all of which can be found in [.guihint]#Setup > General > Global Settings > Automatic Agent Updates#.
// delete to here
If the settings differ for each remote site, they can be edited separately as well.
To do so, go to [.guihint]#Setup > General > Distributed monitoring# and choose the gear icon icon:icon_site_globals[] in the desired site's row to access that site's [.guihint]#Site specific global settings#.


[#connection_to_central_bakery]
==== Connection to central agent bakery

At this point, the URL through which the central site can be accessed from the remote site must be specified, including its protocol and the character string `/check_mk/`, for example `\https://myserver.example.com/mysite/check_mk/`.
While the {CMK} site will try to identify all other missing settings itself, the specification of this URL is not optional, as this connection direction will otherwise not be established in the case of a central configuration.
If the protocol is HTTPS, the remote site automatically uses the CA or self-signed certificates available in the setup for the verification of the connection [.guihint]#Setup > General > Global Settings > Site management > Trusted certificate authorities for SSL#.


[#connection_to_remote_bakery]
==== Connection to remote agent bakery

Since the remote sites are not necessarily accessible from the respective monitored hosts via the same URL as from the central site,
a URL can be configured here for this purpose.
This URL is then automatically communicated to the host (or the requesting agent updater) when a connection is made to a {CMK} site.
The configuration as [.guihint]#Site specific global settings# makes particular sense here.
If no URL is specified, it is assumed that the remote sites are accessible from both the central site and from the monitored hosts under the identical URL.
If it is an HTTPS connection, the appropriate certificate can also be automatically made available to the host.
Since the central CA store cannot be used for this, appropriate certificates can be specified at this point.
Alternatively, the certificates can also be specified in the agent updater rules.
endif::[]


ifdef::onprem[]
[#https_handling]
== Working with HTTPS

At various points in this article there are references to securing the respective connections via HTTPS.
Here, we will once again provide a general overview of what needs to be done to fully secure connections via HTTPS.
Both the connection from a remote to its central site, and from a host to a {CMK} site can and should be secured via TLS, i.e. using HTTPS.
This is independent of whether it is a single site setup or a xref:glossar#distributed_monitoring[distributed monitoring].


[#https_usage]
=== Configuring HTTPS

In order to be able to connect to a {CMK} site via HTTPS, first the monitoring server must be configured for HTTPS. 
This can be achieved, for example, via a suitable xref:omd_https#[configuration of the system Apache], or most simply via the HTTPS settings on the {CMK} appliance.

Whether a {CMK} server is then addressed via HTTP or HTTPS is determined by the URL configured in each case.
If this begins with `https://`, the server is addressed via the HTTPS protocol using port 443. 
This also applies to the protocol you configured with the xref:update_server_information[[.guihint]#Update server information#] setting.
In principle, you can force forwarding from HTTP to HTTPS on the Apache side and (initially) exclude individual paths for this.
For configuration details, see the Apache documentation for the `mod_rewrite` and `mod_redirect` modules.


[#provide_certificates]
=== Providing certificates

In order for an HTTPS connection to be established, it must be possible to verify the contacted server's certificate chain or the self-signed certificate (depending on how the server is configured).
The provision of suitable CA or self-signed certificates makes this possible and can be realized in various ways.


[#connection_to_cmk_server]
==== Connections from a host to a {CMK} server

The agent updater always attempts to verify HTTPS connections and terminates these if a verification is  not possible.
Certificates for verification are available to the agent updater from the following sources:

*The agent updater:*
By default, the agent updater comes with a Python runtime environment including any required modules.
Also included is the certificate bundle of the link:https://pypi.org/project/certifi/#[Certifi module^], which in turn is based on the Mozilla project's certificate collection.
This ensures that public CAs are made known to the agent updater in a timely manner, even when operating system updates are pending.

*Certificates via the Agent Bakery:*
Certificates contained in the Certifi module are ignored once one or more certificates have been imported via one of the following mechanisms.
Certificates from the following sources are stored locally on the host and used only by the agent updater:

. Using the xref:certificates_for_https[[.guihint]#Certificates for HTTPS verification#] setting, certificates can be baked into an agent package and will be available to the agent updater from installation (or an update).

. When configuring the connection to the remote site via [.guihint]#Setup > General > Global settings > Automatic agent updates > Connection to remote agent bakery#, you can specify certificates that can be used to verify the HTTPS connection to the respective remote site.
This is especially useful if it is not yet clear at configuration time which host will be assigned to which site.
This import option can also be used to reduce the number of agents to be baked, since the correct certificates for the respective update servers do not have to be host-specifically configured.

*Certificate Store:*
the agent updater _may_ use the operating system's Certificate Store only if the agent updater has been configured to use the xref:executable_format[System-Python] instead of the supplied Python runtime environment, and no Certifi module is configured in the System-Python.
Since many factors such as the installation parameters or the `_PIP_STANDALONE_CERT` environment variable are involved here, we do not officially support such a configuration.

*Certificate via command line - Import certificate:*
You can also call the agent updater with the command line argument `--trust-cert`.
This retrieves and imports the server certificate.
This is done without taking into account the type of certificate:
The certificate is trusted without further checking of a possible chain - regardless of whether it is a self-signed certificate, a certificate at the end of a public certificate chain or a certificate signed with an internal CA.

[IMPORTANT]
====
. If a certificate is imported in this way, you must ensure the authenticity of the server itself, since the certificate does not come from an independent source.
. As only the server certificate is imported, which is sometimes very short-lived, you must provide xref:certificate_change[new certificates] via the Agent Bakery in good time before expiry.
====

*Certificate via command line - Ignore certificate:*
If there is no valid certificate available to the agent updater at all, the certificate validation can be bypassed by using the `--insecure` command line argument for a call.
This can be useful if the valid certificate is already waiting to be retrieved from the server on the next connection, 
but the agent updater will be 'locked out' by this missing certificate.

[IMPORTANT]
====
This actually completely deactivates the certificate check for this call.
Communication still takes place in encrypted form, so the use of this argument is 'better than nothing'.
====


[#connection_from_remote_to_central_site]
==== Connection from a remote site to a central site

The distribution of certificates is simpler when connecting from a remote to the central site, since the setup procedure is not exited at all.
The remote site can use the {CMK} certificate store under [.guihint]#Global settings > Site management > Trusted certificate authorities for SSL#.
It is therefore sufficient to import the certificate(s) via the central site, if necessary also via [.guihint]#Site specific global settings# when the central site is accessible under multiple URLs.


[#certificate_change]
==== Procedure for replacing a certificate

If you work with your own certification infrastructure, you ideally use a root certificate that is valid for a very long time and whose associated key is regularly used to create intermediate certificates. These are then in turn used to sign the server certificates.
In this case, you roll out intermediate certificates as a certificate chain on the {CMK} server.
The hosts that receive automatic agent updates now only need to be made aware of the root certificate.

If you are unsure whether a new server certificate requires a new root certificate, use the following command.
Use it to determine the identifier of the root key used to sign a server certificate:

[{shell}]
----
{c-root} openssl x509 -noout -text -in cert.pem | grep -A1 'X509v3 Authority Key'
            X509v3 Authority Key Identifier: 
                14:2E:B3:17:B7:58:56:CB:AE:50:09:40:E6:1F:AF:9D:8B:14:C2:C6
----

If the displayed identifier is identical for both the old and the new certificate, no further action is required.
Hosts that trust this root certificate can continue to obtain updates even if the certificate chain changes -- provided that the chain was correctly stored in the system Apache.

If a self-signed certificate or a short-lived root certificate from an internal CA has been used up to now, or if the previous root key of one of your internal CAs has been compromised, proceed as follows when replacing the certificate:

. Add the new certificate using the [.guihint]#Certificates for HTTPS verification# parameter (in the [.guihint]#Agent updater (Linux, Windows, Solaris)# rule).
. Re-bake the agent packages and update all hosts in the monitoring.
Ensure that this update has been run for all hosts before proceeding.
. Now replace the server certificate.
. Test with a few hosts in which it is easy to reinstall the agent manually if it fails, to see if it is possible to update again using the new certificate.
. If the preceding step (4) could be performed successfully -- you _can_ (the certificate will expire), or _must_ (the key was compromised) -- remove the old certificate and perform another agent update.
endif::[]


[#faq]
== Troubleshooting

This chapter first deals with typical, general errors and their causes:

.Errors already fixed in the [.guihint]#Check_MK Agent# service
[%collapsible]
====
[#faq1]

The agent updater will really only be run once within the update interval,
so an error will be continuously-displayed until either you call the plug-in manually, or the next interval is pending.
====


.Registration fails after a manual reinstallation of the {CMK} agent
[%collapsible]
====
[#faq2]

The agent updater creates its own status file `cmk-update-agent.state` independently (under Linux/Unix in `/etc`, and under Windows in the `config` folder).
This file remains on the host after uninstallation, so that the registry information does not get lost.
A new installation will find the file and continue using it.
If this situation is undesirable, simply delete the `cmk-update-agent.state` file manually after an uninstall.
====


ifdef::onprem[]
.Update status for hosts with no automatic updates active
[%collapsible]
====
[#faq3]

The [.guihint]#Monitor > System > Agent Update Status# page displays all of the hosts that are are in the monitoring and for which a status file exists on the {CMK} server.
It does not matter if the host actually reports to the {CMK} server for automatic updates.
Should an unexpected host be displayed here, it is worth taking a look in the `/omd/sites/mysite/var/check_mk/agent_deployment` folder
-- the cause will probably be an old or accidentally-created registry.
====
endif::[]


ifdef::onprem[]
.The connection over SSL/TLS does not function
[%collapsible]
====
[#faq4]

The agent updater is designed to explicitly trust only the certificates
which are usually specified under [.guihint]#Agent updater (Linux, Windows, Solaris)# in the xref:certificates_for_https[HTTPS configuration].
In particular locally-installed certificates are ignored.
It can also occur that the {CMK} server is accessible through the browser, while the agent updater cannot connect due to an incorrect configuration.

In the HTTPS configuration of the agent updater rule a _root certificate_ must be specified with which the connection to the {CMK} server can be verified.
In other words:
the _certificate chain_ included in the {CMK} server’s _server certificate_ must be verifiable by the certificate given here.
Often the server certificate is specified here instead -- this is however not suitable for this purpose.

Take a look at the {CMK} server’s certificate chain with the _OpenSSL_ tool.
Due to the chain’s length, for clarity here only a relevant section is shown and the omitted sections marked by `pass:q[[...]]`:
 
[{shell}]
----
{c-root} openssl s_client -connect mymonitoring.example.net:443
pass:q[[...]]
subject=/CN=mymonitoring.example.net
issuer=/C=DE/O=Deutsche Telekom AG/OU=T-TeleSec Trust Center/CN=Deutsche Telekom Root CA 2
---
No client certificate CA names sent
Peer signing digest: SHA512
Server Temp Key: ECDH, P-256, 256 bits
---
SSL handshake has read 3832 bytes and written 302 bytes
Verification: OK
---
pass:q[[...]]
----

For the last entry -- in our case `subject=/CN=mymonitoring.example.net` -- you need a valid root certificate.
This must not necessarily be -- as in this example --  the issuer of the certificate.
It will usually be a chain of issuers.

Then look at the certificate used.
Here too due to the length it will be shortened as in the above example:

[{shell}]
----
{c-root} openssl x509 -text -noout -in myca.pem
Certificate:
    Data:
        Version: 3 (0x2)
        Serial Number: 38 (0x26)
    Signature Algorithm: sha1WithRSAEncryption
        Issuer: C = DE, O = Deutsche Telekom AG, OU = T-TeleSec Trust Center, CN = Deutsche Telekom Root CA 2
        Validity
            Not Before: Jul  9 12:11:00 1999 GMT
            Not After : Jul  9 23:59:00 2019 GMT
        Subject: C = DE, O = Deutsche Telekom AG, OU = T-TeleSec Trust Center, CN = Deutsche Telekom Root CA 2
        pass:q[[...]]
        X509v3 extensions:
            pass:q[[...]]
            X509v3 Basic Constraints:
                CA:TRUE, pathlen:5
            pass:q[[...]]
----

The top certificate -- seen in the above excerpt -- is not permitted to have a dependency on another certificate.
You can see that the `Issuer` and the `Subject` are identical and that the option `CA:TRUE` is included.
In addition the issuer’s chain that authenticates an object must be consistent up until the final entry.
You therefore also need all intermediate certificates if the issuer of the last certificate should not be a CA.
====
endif::[]


ifdef::onprem[]
.RPM installation fails on Red Hat Enterprise Linux/CentOS
[%collapsible]
====
[#faq7]

It has occasionally occurred -- especially on Red Hat/CentOS systems -- that the call to `rpm` triggered by the automatic update repeatedly fails,
while a manual call to `cmk-update-agent` processes successfully.
The cause in these cases was a SELinux policy that prevented an error-free call if `rpm` was called by a child process of `xinetd`.
You can solve the problem, i.e., get to the bottom of it by analyzing the SELinux logs, and adjusting the policy accordingly using the `audit2allow` tool.
====
endif::[]


The following section deals with problems that are indicated by specific error messages:

.Error message: Cannot open self cmk-update-agent or archive cmk-update-agent.pkg
[%collapsible]
====
[#faq5]

On some Linux systems the program _Prelink_ is installed and a cronjob is activated
which regularly examines all binary files on the system, and adapts them if necessary to speed up the programs.
However the agent updater plug-in is packaged with the _PyInstaller_ program which is not compatible with such actions, and is therefore _broken_.
{CMK} therefore has a blacklist entry for deb/rpm packages which is stored under `/etc/prelink.conf.d`,
and -- if prelink exists -- sets an entry in the existing `/etc/prelink.conf` file.
Since this problem is difficult to handle, it can still happen that these measures do not take effect -- especially in the case of a subsequent setup of prelink.

Therefore, if you install prelink later, set the entry yourself and add the following line to the file with the following command:

[{shell}]
----
{c-root} echo "-c /etc/prelink.conf.d/cmk-update-agent.conf" >> /etc/prelink.conf
----
====


.Error message cmk-update-agent: error while loading shared libraries: libz.so.1: failed to map segment from shared object
[%collapsible]
====
[#faq6]

This error message occurs when the `/tmp` directory with the flag `noexec` was mounted in the system.
To fix this problem, you can either remove the flag, or -- if you deliberately set and require the flag --
on the {CMK} server in [.guihint]#Setup# create a rule under [.guihint]#Agents > Windows, Linux, Solaris, AIX > Agents > Agent rules > Installation paths for agent files (Linux, Unix)#.
There you can define the tmp directory in the [.guihint]#Directory for storage of temporary data (set TMPDIR environment variable)# option yourself.
The agent updater plug-in will then in future write temporary files in the defined directory.
That works even if you call the plug-in manually with the helper script in `/usr/bin/cmk-update-agent`.
====


.Error message: No valid signature found
[%collapsible]
====
[#faq8]

If the [.guihint]#Check_MK Agent# service displays the warning `No valid signature found`, this means that the agent package intended for the host in the Agent Bakery has *not* been signed with one of the xref:signature_keys[accepted keys].

image::agent_deployment_no_valid_signature.png[]

In the simplest case, all you need to do is sign your agents using the [.guihint]#Sign agents# function in the Agent Bakery with one of the keys displayed under [.guihint]#Signature keys the agent will accept#.

image::agent_deployment_sign_agent.png[]

As soon as the agent updater makes its next report from the affected host to the {CMK} server, *and* the cache interval for the service has expired, the warning will disappear.

However, if the host does not have (or no longer has) a single signature key that is located on the {CMK} server, you must repeat the xref:bakery[bake and sign] with a key displayed under [.guihint]#Signature keys the agent will accept#, then copy the baked agent to the affected host and reinstall it there.

On an affected host, you can run `cmk-update-agent -v` or `check_mk_agent.exe updater -v` to get more details about this error.
The detailed error message explicitly lists the signatures from the xref:files[agent updater plug-in's configuration file] that do not have an accepted (i.e. configured) counterpart on your {CMK} server.

[{shell}]
----
Ignoring signature #1 for certificate: certificate is unknown.
No valid signature found.
----
====


[#files]
== Files and directories

ifdef::onprem[]
=== File paths on the {CMK} server

[cols="40,~",options="header"]
|===
|File path |Description
|`~/var/check_mk/agents/` |Contains the baked agents, sorted first in subdirectories by operating system (e.g. `linux_rpm`) and below by hosts via soft link.
|`~/var/check_mk/agent_deployment/` |Contains files with the names of the registered hosts. One such file contains the time of the last registration and the _host secret_.
|===
endif::[]


=== File paths on the monitored host

[.tabs]
====
Linux::
+
--
[cols="60,~",options="header"]
|===
|File path |Description
|`/usr/lib/check_mk_agent/plugins/3600/cmk-update-agent` |The agent updater plug-in as binary or script, depending on the configuration in xref:executable_format[executable format].
The subdirectory `3600` stands for the xref:interval_for_update_check[interval for the update check] in seconds (here for the default value of one hour).
|`/usr/bin/cmk-update-agent` |Script to call the agent updater plug-in and register the agent with the command `cmk-update-agent register`.
|`/etc/check_mk/cmk-update-agent.cfg` |Configuration file for the agent updater plug-in, which contains the settings of the [.guihint]#Agent updater (Linux, Windows, Solaris)# rule.
Do not edit this file, because it is written to during installations and updates.
|`/etc/cmk-update-agent.state` |File with registry information, including the _host secret_.
|`/var/lib/check_mk_agent/cmk-update-agent.log` |Extensive log file with debug messages.
|===
--

Windows::
+
--
[cols="60,~",options="header"]
|===
|File path |Description
|`C:\ProgramData\checkmk\agent\plugins\cmk_update_agent.checkmk.py` |The agent updater plug-in as a Python file.
|`C:\Program Files (x86)\checkmk\service\check_mk_agent.exe` |Agent program to register the agent with the command `check_mk_agent.exe updater register`.
|`C:\ProgramData\checkmk\agent\config\cmk-update-agent.cfg` |Configuration file for the agent updater plug-in, which contains the settings of the [.guihint]#Agent updater (Linux, Windows, Solaris)# rule.
Do not edit this file, because it is written to during installations and updates.
|`C:\ProgramData\checkmk\agent\config\cmk-update-agent.state` |File with registry information including the _host secret_.
|`C:\ProgramData\checkmk\agent\log\cmk-update-agent.log` |Extensive log file with debug messages.
|`C:\ProgramData\checkmk\agent\bakery\check_mk.bakery.yml` |Configuration file created by the Agent Bakery, which among other things defines the interval for the update check.
|===
--
====
