// -*- coding: utf-8 -*-
// IGNORE: 
include::global_attr.adoc[]
= The user interface
:experimental:
:title: The user interface
:description: The graphical user interface of {CMK} consists of various areas. Read here how to use the monitor menu, the setup menu and views on the main page.

{related-start}
xref:intro_setup#[Setting up {CMK}]
xref:monitoring_basics#[Basic principles of monitoring with Checkmk]
xref:wato.html#[Configuring Checkmk]
{related-end}


// This block is outdated
// delete from here
[TIP]
====
This article is incomplete as it is currently being revised for publication. +
Please be patient. Thank you! +
By the way, the https://docs.checkmk.com/master/de/user_interface.html[German version] of this article has already been updated -- if that is an alternative for you.
====
// delete to here


[#intro]
== Introduction

{CMK}’s graphical user interface (GUI) has two functions:

* It displays the current status of all hosts, services and events in the *monitoring environment*.
* It is used in the *configuration environment* to _setup_ and to manage the monitoring environment.

For both of these functions {CMK} provides two views of the hosts and services -- and also two menus for getting started with both functions: the [.guihint]#Monitor# and the [.guihint]#Setup# menus.

ifdef::onprem[]
.The {CMK} user interface with the [.guihint]#Main dashboard# of {RE}
image::intro_dashboard.png[alt=The 'Main dashboard' of {RE}.",fullscreen=1]
endif::[]
ifdef::saas[]
.The {CMK} user interface with the [.guihint]#Main dashboard#
image::intro_dashboard_saas.png[alt=The 'Main dashboard'.",fullscreen=1]
endif::[]

// This block is outdated
// delete from here
In this article, we will look at all of the elements you see on this start page -- working our way through the {CMK} user interface from left to right: from the navigation bar to the xref:main_page[main page] to the xref:sidebar[sidebar].
// delete to here

// This block is new
// start translation
////
Wir werden uns in diesem Artikel mit allen Elementen beschäftigen, die Sie im obigen Bild der {CMK}-Benutzeroberfläche sehen -- und arbeiten uns dabei von links nach rechts durch die {CMK}-Benutzeroberfläche:
von der Navigationsleiste über die xref:main_page[Hauptseite] zur xref:sidebar[Seitenleiste].
Dabei werden nur die wichtigsten Einträge der Navigationsleiste in eigenen Abschnitten vorgestellt, wobei die drei Menüs am Anfang stehen, die Sie wahrscheinlich am häufigsten nutzen werden.
////
// end translation


[TIP]
====
The visible menus and menu entries on the navigation bar depend on your permissions.
If you cannot see individual menus or menu entries in your {CMK}, you do not have the corresponding user permissions.
====


[#navigation_bar]
== The Navigation bar

include::include_navigation_bar.asciidoc[]


[#monitor_menu]
=== Monitor menu

include::include_monitor_menu.asciidoc[]


[#customize_menu]
=== Customize menu

You can customize and extend elements of the graphical user interface that are useful for monitoring in the [.guihint]#Customize# menu:

ifdef::onprem[]
.The [.guihint]#Customize# menu of {RE}
image::customize_menu.png[alt="Customize menu of {RE} in the navigation bar.",width=57%]
endif::[]
ifdef::saas[]
.The [.guihint]#Customize# menu
image::customize_menu_saas.png[alt="Customize menu in the navigation bar.",width=76%]
endif::[]


Here you have access to, among other things 
xref:bookmarks[bookmarks],
xref:views#[views],
xref:dashboards#[dashboards],
ifdef::onprem[]
-- and in the commercial editions additionally to
endif::[]
xref:graphing#[graphs],
xref:forecast_graphs#[forecast graphs],
xref:reporting#[reports] and
xref:sla#[extended availabilities (SLA)].

When you call up one of the menu items, a list of currently existing objects is displayed. 
In some lists you will find _built-in_ objects delivered with {CMK} that you can edit or use as templates for your own objects.


[#setup_menu]
=== Setup menu

Your entry point to the xref:wato#[configuration of {CMK}] is the [.guihint]#Setup# menu, which you can open via the navigation bar:

ifdef::onprem[]
.The [.guihint]#Setup# menu of {RE} in show less mode
image::setup_menu.png[alt="Setup menu in the navigation bar."]
endif::[]
ifdef::saas[]
.The [.guihint]#Setup# menu in show less mode
image::setup_menu_saas.png[alt="Setup menu in the navigation bar."]
endif::[]

In this menu you will find the tools with which you can set up and configure {CMK}. 
The menu is divided into _topics_. 
Below each topic you will find the menu entries. 
For [.guihint]#Hosts#, [.guihint]#Services# and [.guihint]#Agents# you get access to different categories of rule sets. 
The concept of xref:wato_rules#[rule-based configuration] is central to {CMK} and very powerful.

[TIP]
====
You can also load the contents of the [.guihint]#Setup# menu into the sidebar with the [.guihint]#Setup shortcuts# snap-in.
====


[#search]
=== Searching

include::include_search.asciidoc[]
// This block is new
// start translation
////
Die Besonderheiten bei der Suche in den einzelnen Menüs werden in den folgenden Abschnitten erläutert.
////
// end translation


[#search_monitor]
==== Searching in the [.guihint]#Monitor# menu

// This block is new
// start translation
////

Im [.guihint]#Monitor#-Menü (der Monitoring-Umgebung) werden Hosts, Services und Einträge des Menüs selbst gesucht.
ifdef::onprem[]
Die Suche erfolgt mit xref:glossar#livestatus[Livestatus]-Abfragen.
endif::[]

Abseits der Freitextsuche gibt es für die Suche im [.guihint]#Monitor#-Menü einige spezielle Suchoperatoren (Filter).
Mit diesen können Sie gezielt nach bestimmten Host-Namen, Service-Namen, Service-Zuständen und anderen Dingen suchen.
Welche Filter Sie nutzen können, wird Ihnen bei der Suche auf zwei Wegen angezeigt, sofern Sie den Suchbereich [.guihint]#All# oder [.guihint]#Monitoring# ausgewählt haben.
Geben Sie in das leere Eingabefeld einen Schrägstrich (`/`) ein oder klicken Sie auf das icon:icon_help[alt="Symbol eines Hilfetexts."]-Symbol rechts unterhalb des Suchfelds.
Die Liste der verfügbaren Filter wird angezeigt:

.Durch Anklicken kann einer der angezeigten Filter in das Suchfeld übernommen werden
image::gui_search_filters.png[alt="Anzeige der Suchoperatoren (Filter).",width=87%]

So können Sie zum Beispiel nach Mustern von Host-Namen suchen (mit `h:`), nach Mustern von Service-Namen (mit `s:`) und Sie können beide kombinieren.
Eine Suche nach `s:boot` zeigt Ihnen alle Services an, die `boot` enthalten und eine Suche nach `h:win s:cpu` zeigt Ihnen alle Services an, die `cpu` enthalten, auf Hosts, die ihrerseits `win` enthalten.

Die Filter können Sie auch mehrfach und kombiniert nutzen. 
Mehrere Instanzen eines Filters werden dabei mit *ODER* verknüpft, unterschiedliche Filter mit *UND*.
Ausnahme: Mehrere Host-Merkmalsfilter (`tg:`) werden immer mit *UND* verbunden.
Beispielsweise liefert die Suche nach `st:warn st:crit` alle Services im Zustand {WARN} oder {CRIT} zurück (die anderen möglichen Zustände können Sie mit den vordefinierten Werten `ok`, `unkn`, `pend` finden).

.Filter mit regulären Ausdrücken kombinieren
[%collapsible]
====
Diese Suchoperatoren in Kombination mit xref:regexes#[regulären Ausdrücken], erlauben es Ihnen, präzise, komplexe Filter zu erstellen, etwa:

`h: ^my.\*Host$ s: ^my.*(\d|test)$ tg: mytag tg: mytest`

Damit werden gefunden: 
Services, die mit `my` beginnen und mit einer Ziffer oder `test` enden, auf Hosts laufen, die mit `my` beginnen und mit `Host` enden, sowie letztlich die beiden Host-Merkmale `mytag` und `mytest` vorweisen.

Sie können reguläre Ausdrücke für jeden einzelnen Filter verwenden -- mit den folgenden Ausnahmen: 
Für Host- und Service-Label sind keine regulären Ausdrücke erlaubt, d.h. das Label muss in der Syntax `Schlüssel:Wert` exakt so eingegeben werden, wie es heißt, z.B. `os:windows`. 
Sobald Sie Gruppen- oder Host-Merkmalsfilter zusätzlich zu Filtern für Name, Adresse oder Alias setzen, müssen erstere explizit angegeben werden, also beispielsweise `hg: Webserver s: apache`. 
Was entsprechend *nicht* geht: `hg: Web.* s: apache`. 
Hintergrund: Die unterschiedlichen Filter sprechen unterschiedliche Quellen mit unterschiedlichen Datenstrukturen und Funktionen an. 
Freilich können Sie die konkreten Host-Gruppen mit Muster heraussuchen (`hg: .*server`), um das Ergebnis dann in der ursprünglichen Suche zu nutzen.
====

Wenn Sie beim Suchen gar keinen Filter setzen, wird standardmäßig zuerst in den Einträgen des [.guihint]#Monitor#-Menüs gesucht und anschließend nacheinander in den Filtern für Host-Name, Host-Alias, Host-Adresse und Service-Name - und zwar in dieser Reihenfolge. 
Sie können die Filter und deren Reihenfolge unter [.guihint]#Setup > General > Global settings > User interface > Quicksearch search order# anpassen.

Standardmäßig liefert eine Suchabfrage in der Monitoring-Umgebung maximal 80 Treffer zurück.
Sie können diesen Wert jedoch unter [.guihint]#Setup > General > Global settings > User interface > Number of elements to show in Quicksearch# verändern.

Wenn Sie sich fragen, warum die beiden genannten globalen Einstellungen so ungewöhnliche Namen haben:
Die Suchfunktion im [.guihint]#Monitor#-Menü ist identisch mit derjenigen, die Ihnen das Snapin [.guihint]#Quicksearch# bietet -- und das Snapin gab es in {CMK} schon vor dem Menü. 
////
// end translation

ifdef::onprem[]
[#livestatus]
To conclude this section, here is a brief digression for those interested in what happens behind the scenes at {CMK}  when a search query is made in the monitoring environment.

.The Livestatus query for searching
[%collapsible]
====
Each search query generates a xref:glossar#livestatus[Livestatus] query.
You can see which one this is in the {CMK} GUI by enabling the global setting [.guihint]#Setup > General > Global settings > User interface > Debug livestatus queries#.

If you enter your search term for the [.guihint]#Monitor# menu and submit it with kbd:[Enter], you will receive a table view listing the hosts/services that match your search.
If the [.guihint]#Debug livestatus queries# setting is enabled, the Livestatus query resulting from your search query will be displayed at the bottom of this results page.
As a site user, you can also use this and similar queries on the command line.

For example, entering `h: localhost s: mem s: cpu h: switch-intern` for the search in the [.guihint]#Monitor# menu looks something like this as a Livestatus query on the command line:

[{shell}]
----
{c-omd} lq
GET services
Columns: service_description host_name
Filter: service_description ~~ (cpu|mem)
Filter: host_name ~~ (switch-intern|localhost)

Memory;localhost
CPU load;localhost
CPU utilization;localhost
Memory;switch-intern
CPU load;switch-intern
----

How to work with Livestatus on the command line is described in the xref:livestatus#lql_shell[Livestatus article].
Here is a brief description of the above example:
Start the interactive input stream with the command `lq`.
Then enter the Livestatus query in the following blank line, as displayed by the debug mode of the Livestatus queries in the {CMK} GUI: in the example, these are the following four lines.
For better readability, we have limited the `Columns` to the two columns that are also used by the following two filter lines.
Complete the entry by pressing the kbd:[Enter] key twice.
The blank line is then followed by the five lines of output as the result.
====
endif::[]


[#search_customize]
==== Searching in the [.guihint]#Customize# menu

// This block is new
// start translation
////
Im [.guihint]#Customize#-Menü werden nur die Einträge des Menüs selbst gesucht, nicht die Einträge, die sich auf den Seiten befinden, die man über das Menü aufrufen kann.
////
// end translation


[#search_setup]
==== Searching in the [.guihint]#Setup# menu

// This block is new
// start translation
////
Mit der Suche im [.guihint]#Setup#-Menü können Sie die Konfigurationsumgebung von {CMK} durchsuchen, d.h. das [.guihint]#Setup#-Menü selbst, mit allen seinen sichtbaren Einträgen und zusätzlich mit einigen verborgenen.
Der Suchbereich umfasst unter anderem alle Regelsätze, die im Menü zu Themen zusammengefasst sind, und die globalen Einstellungen ([.guihint]#Global settings#).
Gesucht wird in den Titeln und den Parameternamen.
ifdef::onprem[]
Die Suche erfolgt mit Redis, einer In-Memory-Datenbank, die in der {CMK}-Instanz als xref:cmk_processes#[Instanzdienst] läuft.
endif::[]
////
// end translation


[#user_menu]
=== User menu

In the [.guihint]#User# menu you can change personal settings that are valid for your user account:

ifdef::onprem[]
.Changing personal settings in the [.guihint]#User# menu
image::user_menu.png[alt="User menu in the navigation bar.",width=57%]
endif::[]
ifdef::saas[]
.Changing personal settings in the [.guihint]#User# menu
image::user_menu_saas.png[alt="User menu in the navigation bar.",width=57%]
endif::[]

Frequently-used settings can be toggled by clicking directly in the [.guihint]#User# menu ([.guihint]#User interface#), some by calling up a specific page, e.g. to display the notification rules. 
Most settings, however, can be accessed and changed centrally via the [.guihint]#Edit profile# page. 

// start proofreading
|===
|Setting |Note

|[.guihint]#Color theme# or [.guihint]#User interface theme#
|Switching between [.guihint]#Dark# and [.guihint]#Light# is possible directly in the [.guihint]#User# menu.

|[.guihint]#Sidebar position#
|Here, too, you can switch between [.guihint]#Right# and [.guihint]#Left# directly in the [.guihint]#User# menu.

|[.guihint]#Received messages#
|Displays a page with the xref:wato_user.html#messages[messages] addressed to your username.

// ifdef::saas[]
// |Visibility of the [.guihint]#Getting started# button ([.guihint]#Toggle 'Getting started' button#)
// |Here, too, you can switch between [.guihint]#Visible# and [.guihint]#Invisible# in the [.guihint]#User# menu.
// endif::[]

|[.guihint]#Language# of the user interface
|You can choose between the languages officially supported by {CMK}, German and English.
In addition, the link:https://translate.checkmk.com/[languages translated by our community^] are offered for selection, but these are not officially supported and are therefore marked with [.guihint]#(not supported)#.

|[.guihint]#Temperature unit#
|You can choose between Celsius and Fahrenheit for display in graphs and Perf-O-Meters.

|[.guihint]#Visibility of hosts/services#
|Show all hosts and services, or only those to which you are assigned as a contact.

|[.guihint]#Disable notifications#
|Switch off all notifications for a selectable period of time.

|[.guihint]#Start URL to display in main frame#
|Set the start page.
There are menu entries for two special pages:
[.guihint]#Default start URL# for the default xref:dashboards[dashboard] and [.guihint]#Welcome page# for the xref:intro_welcome#[welcome page].
With the [.guihint]#Custom start URL# entry, you can also make any other {CMK} page the start page.
Open the desired page (for example, [.guihint]#Monitor > Overview > All hosts#) and determine the entry for the input field from the URL displayed in the browser (for example, `view.py?view_name=allhosts`).

|[.guihint]#Navigation bar icons#
|Show icons with title, or only icons.

|[.guihint]#Default behavior for 'Changes'#
|The [.guihint]#Changes# icon in the navigation bar shows either the slideout with the most important information or the full view with all information.

|[.guihint]#Mega menu icons#
|Show (green) icons by the topic or (colored) icons by each menu item.

|[.guihint]#Show more / Show less#
|By default xref:intro_gui#show_less_more[show less or show more] or always show all.

|[.guihint]#Contextual help icon#
|Show or hide the icon:icon_help[alt="Icon for a help text."] icon for context sensitive help.

|[.guihint]#Notification rules#
|Rules for xref:personal_notifications#[user-defined notifications].

ifdef::onprem[]
|[.guihint]#Change password#
|You must enter the existing password once and the new password twice.
Password requirements for local accounts can be set globally: [.guihint]#Setup > General > Global settings > Password policy for local accounts#
endif::[]

ifdef::onprem[]
|[.guihint]#Two-factor authentication#
|Activation of increased security through xref:wato_user#2fa[two-factor authentication].
endif::[]

|[.guihint]#Logout#
|Possible only directly in the [.guihint]#User# menu
|===

[TIP]
====
For some settings there are default values that can be changed globally for all users via [.guihint]#Setup > General > Global settings#, mostly under the [.guihint]#User Interface# topic. 
In addition, the xref:wato_user#roles[role with its permissions] assigned to your user account determines whether certain settings are visible and can be changed.
====
// end proofreading


[#main_page]
== Main page

// This block is outdated
// delete from here
What you will see on the main page depends on where you are in Checkmk. 
After logging in, you will first see the standard or main dashboard.

The content of the main page changes depending on your selection in the navigation bar and also in the sidebar. 
For example, if you select [.guihint]#Hosts > All hosts# in the [.guihint]#Monitor# menu, the list of hosts will be displayed on the main page:

.Opening the host overview in the [.guihint]#Monitor# menu
image::mainpage_all_hosts.png[alt="Main page with list of all hosts."]
// delete to here

// This block is new
// start translation
////
Die Hauptseite ist der Bereich zwischen der xref:navigation_bar[Navigationsleiste] auf der linken und der xref:sidebar[Seitenleiste] auf der rechten Seite.
Nach der Anmeldung sehen Sie dort die xref:intro_gui#start_page[Startseite].
Was Sie auf der Hauptseite sehen, hängt davon ab, wo Sie in {CMK} gerade unterwegs sind.
Der Inhalt der Hauptseite ändert sich abhängig von Ihrer Auswahl in der Navigationsleiste oder auch der Seitenleiste.
So können Sie zum Beispiel im [.guihint]#Monitor#-Menü [.guihint]#Hosts > All hosts# auswählen:

.Die Hauptseite mit der Tabellenansicht aller Hosts
image::intro_monitor_all_hosts.png[alt="Die Hauptseite mit der Tabellenansicht 'All hosts'."]
////
// end translation

Regardless of which page is currently displayed, you will usually find the following elements on the main page:

* 'Breadcrumb' path: 
Above the page title you see the path to the current page -- always starting with the name of the menu from the navigation bar. 
These breadcrumbs prevent you from getting lost in the user interface. 
This way you know where you are in {CMK} even after complex actions -- in this example, on the [.guihint]#All hosts# page in the monitoring.

* Menu bar: 
Below the breadcrumb path, the menu bar is displayed, which summarizes the possible actions on this page in menus and menu entries. 
The menus are always context-specific in {CMK}, i.e. you only find menu entries for actions applicable to the current page.

[#inline_help]
* Inline help:
You can use the [.guihint]#Help# menu to show context-sensitive help texts on the current page with [.guihint]#Help > Show inline help#, which is very helpful on pages that contain many parameters. 
The selected mode also remains active for other pages that you call up until you deactivate it again.

* Action bar: 
Below the menu bar you will find the action bar, in which the most important actions from the menus are offered as buttons for direct clicking. 
You can hide the action bar with the icon:button_hide_toolbar[alt="Icon for hiding the action bar."] button to the right of the [.guihint]#Help# menu and show it again with icon:button_show_toolbar[alt="Icon for showing the action bar."]. 
When the action bar is hidden, icons for the hidden buttons are added to the right of the [.guihint]#Help# menu.

* Countdown for page update: 
In the monitoring environment, the update interval for the page is displayed in seconds at the top right of the page and the remaining time to the next update is visualized on the surrounding circle line. +
By the way, you can change the refresh interval in the menu [.guihint]#Display > Modify display options#.

After selecting one of the entries in the [.guihint]#Monitor# menu, the main page will usually show you the requested information either as a xref:dashboards[dashboard] or as a xref:views[view], which we will discuss in more detail in the following sections.


[#dashboards]
=== Dashboards

include::include_dashboards.asciidoc[]


[#views]
=== Views

include::include_views.asciidoc[]

The views have many more possibilities -- both for customization and for creating your own views. 
You can find out how to do this in the xref:views#[article on views].


[#sidebar]
== Sidebar

include::include_sidebar.asciidoc[]


// This block is new
// start translation
////
[#welcome]
=== Welcome to {CMK}

Das Snapin [.guihint]#Welcome to {CMK}# öffnet ein Slideout, dass die [.guihint]#Topics to explore# der xref:intro_welcome#[Willkommens-Seite] zeigt.
Es wird das erste Thema angezeigt, das noch nicht abgeschlossen ist, Sie können also dort weitermachen, wo Sie aufgehört haben.

.Mit dem obersten Snapin kommen Sie zurück zu den Themen der Willkommens-Seite
image::intro_snapin_welcome_2steps.png[alt="Das Snapin Welcome to {CMK}.",width=50%]
////
// end translation


[#overview]
=== Overview

include::include_overview_snapin.asciidoc[]


[#bookmarks]
=== Bookmarks

include::include_bookmarks_snapin.asciidoc[]


[#bookmarks_edit]
==== Editing bookmarks

{CMK} manages bookmarks in *lists*. 
Each list contains one or more bookmarks. 
In addition, each bookmark is assigned a _topic_.
All bookmarks on the same topic are collected in the [.guihint]#Bookmarks# snap-in.

Why so complicated? 
So that you can make a list of bookmarks visible to other users. 
You can then use the topics to define how the bookmarks will be presented to other users in the [.guihint]#Bookmarks# snap-in.
In this way, for your organization you can build a navigation structure for selected {CMK} pages and even to external pages. 
Every user can nonetheless still create and manage their own bookmarks.

With the [.guihint]#Bookmarks# snap-in you can access the management of the bookmark lists:

.The view for managing bookmarks
image::user_interface_bookmark_lists.png[alt="View for managing bookmarks."]

You can now either edit an existing list with icon:icon_edit[alt="Icon for editing a list entry."] -- or create a new one with [.guihint]#Add list#:

.The general properties for the bookmark list
image::user_interface_new_bookmark_list_general_properties.png[alt="Dialog with properties when creating a bookmark list."]

In the [.guihint]#General properties# box you enter the internal ID and the title of the list -- whereby the [.guihint]#Title# is not important for the display of the bookmarks, it is only used for management. 
Via [.guihint]#Make this Bookmark list available for other users# you can make the bookmarks in this list visible to other users. 
For this, however, your current user account needs a permission, which by default is held only by the [.guihint]#Administrator# xref:wato_user#roles[role].

[TIP]
====
The principle of visibility is used by {CMK} in many locations, and is explained in more detail in the xref:views#edit[Views] article.
====

In the [.guihint]#Bookmarks# box you can now define the new bookmarks with a title and URL:

.Set new bookmarks
image::user_interface_new_bookmark_list_bookmarks.png[alt="Dialog for setting the properties for the new bookmarks.",fullscreen=1]

Where the bookmarks are sorted is decided by the [.guihint]#Default topic# in the list, or respectively by a bookmark’s [.guihint]#Individual topic# -- if this has been set.
By the way, you can also give bookmarks their own icons.
Finally, save the new list with [.guihint]#Save & view list#.

The new list created in this example now supplements the personal bookmarks under [.guihint]#My Bookmarks# for all users with three bookmarks on two new topics:

.The [.guihint]#Bookmarks# snap-in displays the newly created bookmarks
image::user_interface_bookmarks.png[alt="Bookmarks snap-in with the three newly created bookmarks.",width=50%]


[#master_control]
=== Master control

include::include_master_control_snapin.asciidoc[]
